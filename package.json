{
  "name": "platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "e2e": "playwright test --config e2e/playwright.config.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "turbo": "^2.0.0"
  },
  "packageManager": "npm@10.0.0"
}
