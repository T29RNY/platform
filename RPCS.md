# In or Out — RPC Inventory
*Last updated: Jun 17 2026 (session 141 — MULTI-CONTEXT NAV Phase 1, migs 349–351 (all read-only/additive, no breaking change): mig 349 adds `team_type`,`is_competitive`,`club_id`,`club_name` to the top-level return shape of BOTH `get_team_state_by_player_token` AND `get_team_state_by_admin_token` (so deriveContext works for anon players who can't use player_get_teams_by_token); `is_competitive` reuses the mig-153 EXISTS(active league competition_teams) rule; grants preserved anon+authenticated; mappers `getTeamStateByPlayerToken`/`getTeamStateByAdminToken` expose `teamType`/`isCompetitive`/`clubId`/`clubName` (Hard Rule #12). mig 350 NEW read RPC `guardian_list_children_sessions()` (SECURITY DEFINER, anon+auth) — every accepted child of the caller (member_guardians) × all upcoming training+matches across ALL the child's clubs/cohorts/teams + the child's RSVP status; eligibility mirrors member_list_upcoming_sessions (a/b/c) with NO guardian-own-membership requirement; wrapper `guardianListChildrenSessions`. mig 351 per-team kill-switch: NEW column `teams.multi_context_nav` (default false → ships dark) + NEW reader RPC `get_team_feature_flags(p_team_id)` (anon+auth, wrapper `getTeamFeatureFlags`, fails safe to off) + `member_get_self.active_clubs[].pass_token` added (wrapper picks it up; powers the club-nav Pass deep-link). CONSUMERS (Hard Rule #14): apps/inorout deriveContext()/NavBar/ContextSwitcher/ClubNavBar/ParentHomeScreen (guardian feed) — designed so future apps reading team-state inherit the descriptor fields. VERIFY: mig 349 shape confirmed live (casual vs competitive, player+admin paths); mig 350 full seed-and-rollback EV PASS (1 child/1 eligible session/correct club+type+RSVP) + leak-check 0; mig 351 flag RPC verified default-off; all 8 monorepo apps build clean. ⛔ real-iPhone walk OWED (Hard Rule #13 — App.jsx routing/PlayerView/NavBar touched). session 139 — mig 348 H2H/league-table by player token: TWO new read-only SECURITY DEFINER RPCs `get_head_to_head_raw_by_player_token(p_token,p_me_id,p_them_id,p_period)` + `get_player_league_table_raw_by_player_token(p_token,p_period)` — exact mirrors of the mig-041/042 admin-token variants but resolve team via `players.token`→`team_players` (same pattern as `get_team_state_by_player_token`). WHY: `getHeadToHead`/`getPlayerLeagueTable` only had admin-token RPC paths; their fallback read `player_match` directly, but that table has RLS on with NO anon/authenticated select policy → 0 rows → Head-to-Head showed the empty state for every player on the /p/<token> player route. Both granted anon+authenticated, `search_path=public`, return `jsonb` identical-shape to the admin variants. No write, no audit_events (read-only). CONSUMERS (Hard Rule #14): `apps/inorout` HeadToHead.jsx via the `getHeadToHead` (now 6-arg, `playerToken` 6th) + `getPlayerLeagueTable` (now 4-arg, `playerToken` 4th) wrappers — token threaded App.jsx AND PlayerView.jsx (Stats tab, the real entry) →StatsView→HeadToHead. mig 347 match result note: NEW column `matches.result_note`. `admin_save_match_result` gains a 15th arg `p_result_note text DEFAULT NULL` (old 14-arg signature DROPPED, grant re-applied anon+authenticated — token-gated internally); writes `result_note = NULLIF(p_result_note,'')` on UPDATE + INSERT (re-save sends the textbox value, so blank clears it — ScoreScreen pre-fills from the existing note to preserve on edit). `get_team_state_by_admin_token` match shape exposes `result_note`; player-token state RPC already returns it via `to_jsonb(m.*)`. Wrapper `saveMatchResult` passes `p_result_note: match.resultNote`; `dbToMatch.resultNote`. Surfaced on the HistoryView result card (visible to all). EV 4/4 (note persist/edit/clear + cascade intact) + leak 0, rpc-security-sweep PASS (secdef/search_path/overload=1/grants preserved). ⛔ real-iPhone walk OWED (ScoreScreen/HistoryView/PlayerView touched). mig 346 plus-one approvals: NEW column `players.pending_approval` (default false). `add_guest_player` gains optional 3rd arg `p_admin_token` — a valid admin token for the team auto-approves the +1 straight in (status='in', squad-full guard kept); no admin token → PENDING (status='none', pending_approval=true, no spot taken, squad-full guard skipped) + fires `notify_team_change(...,'guest_pending_approval')`. OLD 2-arg signature DROPPED (overload safety). NEW `admin_approve_guest(p_admin_token,p_guest_id)` → status='in', or 'reserve' if squad full; NEW `admin_decline_guest(p_admin_token,p_guest_id)` → dormant. Both admin-token authed, SECURITY DEFINER, audit_events INSERT, granted anon+authenticated. `remove_guest_player` now also clears pending_approval (host cancel drops it from the queue). State RPCs `get_team_state_by_admin_token`/`get_team_state_by_player_token` expose `pending_approval` on every squad shape (mapper `dbToPlayer.pendingApproval`). CONSUMERS (Hard Rule #14): AdminView approvals banner, PlayerView "waiting for approval" + returning-guest picker exclusion, `/api/notify` type `guestPendingApproval` (admin-targeted push, dormant until admins enable push). EV 8/8 + leak 0, rpc-security-sweep (4) PASS, hygiene 7/7, inorout build PASS. ⛔ real-iPhone PWA walk OWED (Hard Rule #13 — PlayerView/AdminView touched). session 88 — mig 268 lineup lock + team integrity: NEW internal helper `is_lineup_locked(team_id)→bool` (true once schedule.game_date_time reached on the active live game; the casual kick-off lock point — internal-only, no PUBLIC grant); the four self-service lineup RPCs now raise `lineup_locked` post-kickoff — `set_player_injured`, `set_player_status` (scoped to drawn players so a non-drawn player can still act), `add_guest_player` (unscoped), `remove_guest_player` (scoped to a drawn guest); `set_player_injured` + `admin_set_player_injured` now restore a still-drawn player to 'in' on un-injure; `admin_save_match_result` reconciles orphan player_match rows (attended=true/result NULL) before the flat W/L/D bump. All six signatures unchanged → grants preserved. session 80 — mig 241 post-game lifecycle: admin_save_match_result now CLOSES the game (game_is_live=false) + resets EVERY squad status incl reserves (not just attendees) + preserves paid for already-ledger-paid players; set_player_status gains a server-side sign-up-window gate (raises game_not_live unless game_is_live AND NOT is_cancelled). POTM voting window extended 1h→2h (cron). session 76 — mig 230: notify_spot_opened trigger + inorout.bulk_reset GUC guard added to the go-live RPCs)*

All client writes go through these SECURITY DEFINER RPCs. Raw SQL names appear
only inside `supabase.rpc()` calls in `packages/core/storage/supabase.js`.

> **Session 59 (Phase 9 cont.) — no RPC change.** The new league reminder crons
> (`availabilityRequestJob`/`fixtureReminderJob` in `api/cron.js`) read
> `fixtures`/`team_players`/`players` with the **service role** (the cron.js convention,
> like `lineupLockJob`) and write only `notification_log` push rows via `/api/notify` direct
> mode — no new RPC, no migration. `api/_sms.js` (Twilio transport core) is unwired. See
> FEATURES "PHASE 9 (cont.)".

**Rule:** If an RPC doesn't exist for a write you need, create it in Supabase SQL
editor first, then add the JS wrapper. See CLAUDE.md RPC CHECKLIST.

---

## PATTERN

- Admin RPCs: derive `team_id` from `p_admin_token` server-side — never trust client
- Player RPCs: derive context from `p_token`
- Auth RPCs: use `auth.uid()` — no identity params needed
- All return `jsonb`
- All use `SECURITY DEFINER`

---

## CONSUMERS — FORWARD DEPENDENCY TRACKING

When an RPC is consumed by multiple apps (and especially when it's
designed for FUTURE apps that don't exist yet), record the consumers
in the Notes column so a later return-shape change doesn't silently
break a downstream app. Format:

> *Consumers: `apps/inorout` (PlayerView), `apps/ref` (LiveMatch), Phase 4 reception display (planned)*

Hard-rule #12 enforces that adding a field to an RPC requires the
same-commit mapper update for current consumers. This convention
extends that discipline FORWARD — when Phase 4 finally builds the
reception display, this column tells it which RPCs it can reuse
rather than rebuild.

Phase 5 cycles 5.3 and 5.4 introduce RPCs designed for multiple
future consumers — track them here as they land.

### Phase 2 RPCs — Team registration (venue/league)

*The Phase 2 venue/league RPC layer is otherwise uncatalogued here (dashboard, fixture generation, standings, etc.). These three are the team-registration trio (migs 098–100), added session 55. Not exhaustive.*

| SQL function | JS wrapper | Grant | Notes |
|---|---|---|---|
| `join_register_team(p_league_code, p_competition_id, p_team)` | `joinRegisterTeam(leagueCode, competitionId, team)` | authenticated only | Migration 098 (Cycle 2.5a); **behaviour changed in mig 158**. SECURITY DEFINER, search_path locked. Requires `auth.uid()`. Self-serve registration into a league competition. `p_team` is either `{name, short_name?, primary_colour?, secondary_colour?, admin_email?}` → creates a NEW competitive team + claims caller as team_admin; OR `{existing_team_id}`. **Mig 158 — a league team is ALWAYS a separate squad:** `existing_team_id` is accepted ONLY if that team is already `team_type='competitive'` (e.g. a league team also entering a cup, Phase 11); a casual team is rejected with `casual_team_cannot_register` (no in-place casual→competitive promotion). Inserts `competition_teams` row status `pending`; audit `team_registration_submitted`; broadcasts `team_registration_pending` to venue + league. Mig 158 also revoked a stale anon EXECUTE grant (Supabase default-privileges leftover) → authenticated only. **Consumers (hard-rule #14)**: `/join/LEAGUE_CODE` self-serve registration wizard (LEAGUE_MODE_SCOPE Phase 2G — NOT yet built in apps/inorout; RPC + wrapper only); venue approval dashboard (downstream of the pending row). |
| `venue_approve_team_registration(p_venue_token, p_competition_team_id)` | `venueApproveTeamRegistration(venueToken, competitionTeamId)` | anon + authenticated | Migration 099 (Cycle 2.5a). SECURITY DEFINER, search_path locked. Venue admin flips a `competition_teams` row `pending → active`. Caller resolved via `resolve_venue_caller`; the registration's competition→season→league must belong to the caller's venue (`registration_not_in_venue`). **Idempotent**: re-approving an already-active row is a no-op success (`noop:true`) so a double-click doesn't 500. Clears `rejection_reason`. Audit `team_approved`; broadcasts `team_approved` to venue + league. (Team-admin email/push delivery is owned by Cycle 2.7.) **Consumers**: apps/venue operator dashboard pending-registrations screen. |
| `venue_reject_team_registration(p_venue_token, p_competition_team_id, p_reason)` | `venueRejectTeamRegistration(venueToken, competitionTeamId, reason)` | anon + authenticated | Migration 100 (Cycle 2.5a). SECURITY DEFINER, search_path locked. Venue admin flips a `pending` `competition_teams` row → `rejected`. `p_reason` required, non-empty (`rejection_reason_required`), stored in `competition_teams.rejection_reason` (col added mig 097). Only `pending` is rejectable (`only_pending_can_be_rejected`); same venue-ownership gate as approve. Audit `team_rejected` (reason in metadata); broadcasts `team_rejected` to venue + league. **Consumers**: apps/venue operator dashboard pending-registrations screen. |

### Phase 11 RPCs — Cups & knockouts

| SQL function | JS wrapper | Grant | Notes |
|---|---|---|---|
| `get_cup_bracket(p_competition_id)` | `getCupBracket(competitionId)` | anon + authenticated | **Migration 188 (Cycle 11.3).** STABLE, read-only. Returns the full single-elim bracket: `{competition, champion, rounds:[{round_number, round_name, ties:[…]}]}`. Each tie carries teams + colours, the linked fixture's schedule/score/`decided_by`/ET/pens, `winner_team_id`, and `status` (pending/ready/scheduled/decided). Public match data (shown on the no-login display board), so keyed by competition_id with no token gate. **Extended mig 194 (Cycle 11.4b):** for `format='group_stage'` comps also returns `groups` (= `get_group_standings` payload), `all_groups_complete`, and `knockout_seeded` (additive; knockout `rounds`/`champion` unchanged). **Consumers**: apps/venue `BracketView` (+ schedules `ready` ties, Build-knockout gate); apps/inorout player bracket; apps/display bracket zone. |
| `ref_record_knockout_decider(p_ref_token, p_aet_home, p_aet_away, p_pens_home, p_pens_away, p_winner_team_id)` | `refRecordKnockoutDecider(refToken, {aetHome, aetAway, pensHome, pensAway, winnerTeamId})` | anon + authenticated | **Migration 187 (Cycle 11.2).** SECURITY DEFINER. Completes a cup knockout that was level at full time. Resolves the fixture by ref_token; requires `type='cup'`, `cup_tie_id` set, status `in_progress`, regulation level. Penalties take precedence over ET; the picked winner must match the higher of whichever decider is given (else `winner_pens_mismatch`/`winner_aet_mismatch`); a level ET with no pens raises `extra_time_level_needs_pens`. Sets scores + `decided_by` + `ko_winner_id`, status→completed → the `cup_advance_after_result` trigger advances the bracket. Audit `ref_knockout_decider`. **Consumers**: apps/ref `LiveMatch` DeciderModal (shown when `ref_confirm_full_time` returns `needs_decider`). |
| `venue_schedule_cup_tie(p_venue_token, p_tie_id, p_scheduled_date, p_kickoff_time, p_playing_area_id?)` | `venueScheduleCupTie(venueToken, tieId, scheduledDate, kickoffTime, playingAreaId=null)` | anon + authenticated | **Migration 187 (Cycle 11.2).** SECURITY DEFINER. Operator schedules a `ready` next-round cup tie (both teams known). Venue-ownership gate; requires tie status `ready`, no existing fixture, date in season. Creates the fixture (linked `cup_tie_id`), flips tie → `scheduled`, raises the fee charge. Audit `cup_tie_scheduled`; notify venue + league. **Consumers**: Cycle 11.3 venue bracket scheduling UI (wrapper shipped 11.2). |
| `ref_confirm_full_time(p_ref_token)` | `refConfirmFullTime(refToken)` | anon + authenticated | **Behaviour changed mig 187 (Cycle 11.2).** For a cup knockout (`cup_tie_id` set) that is **level**, it no longer completes — returns `{needs_decider:true, home_score, away_score}` and leaves the fixture `in_progress` (the ref then calls `ref_record_knockout_decider`). Decisive cup ties complete + stamp `decided_by='regulation'`; **league fixtures behave exactly as before**. Terminal completion fires the bracket-advance trigger. **Consumer**: apps/ref `LiveMatch` (handles `needs_decider`). |
| `_cup_advance(competition_id)` / `tg_cup_advance()` | *(none — trigger only)* | postgres/service_role only | **Migration 187 (Cycle 11.2).** Internal. `_cup_advance` is the idempotent sweep: resolve decided ties from terminal fixtures (walkover/forfeit/ko_winner/score), propagate each winner into its parent slot via the 11.1 feeder edges, mark pending ties whose both sides are known as `ready`. `cup_advance_after_result` (AFTER UPDATE on fixtures, WHEN `cup_tie_id` set) runs it on completion/walkover/forfeit. Only touches `cup_ties` (no fixtures recursion). Not client-callable. |
| `venue_persist_group_stage(p_venue_token, p_competition_id, p_num_groups, p_qualifiers_per_group, p_scheduled_date, p_kickoff_time, p_playing_area_ids uuid[], p_group_assignments jsonb?)` | `venuePersistGroupStage(venueToken, competitionId, numGroups, qualifiersPerGroup, scheduledDate, kickoffTime, playingAreaIds, groupAssignments=null)` | anon + authenticated | **Migration 192 (Cycle 11.4a).** SECURITY DEFINER. Group-stage equivalent of `venue_persist_cup_bracket`. Requires `type='cup'` AND `format='group_stage'` (`not_group_stage_cup`); refuses if fixtures/ties exist. Draws active teams into N groups (snake by `registered_at`, or operator `p_group_assignments {team_id:'A'}` override), writes `competition_teams.group_label`+`seed`, generates a single round-robin per group server-side (circle method, `fixtures.group_label` tagged, weekly rounds from `p_scheduled_date`), raises fee charges identically to `venue_generate_fixtures`, stores `competitions.config {num_groups, qualifiers_per_group, knockout_seeded:false}`. Guards `invalid_group_config`/`more_groups_than_teams`/`group_too_small_for_qualifiers`. Audit `group_stage_generated`; notify venue+league. EV-verified (6 teams→2 groups×3, 6 fixtures, 12 charges, leak 0). **Consumers (hard-rule #14)**: apps/venue SeasonWizard (group_stage branch); 11.4b `venue_seed_knockout_from_groups` (reads group results + config). |
| `get_group_standings(p_competition_id)` | `getGroupStandings(competitionId)` | anon + authenticated | **Migration 193 (Cycle 11.4a).** STABLE, read-only. Per-group mini-league tables for a group_stage cup. Aggregation mirrors `get_league_standings_for_player` (W=3/D=1/L=0, GD, GF, walkover/forfeit 3-0), scoped by `competition_id`+`group_label`. Returns `{competition_id, groups:[{group_label, qualifiers_per_group, standings:[{team_id, team_name, primary_colour, played, w, d, l, gf, ga, gd, pts, rank, qualifying}]}], all_groups_complete}`. Rank order pts→gd→gf→seed (deterministic; **no head-to-head/best-3rd tiebreak in v1**). Returns `groups:[]` for non-group comps (safe to call on any cup). Keyed by competition_id, no token (display board). **Consumers (hard-rule #14)**: apps/venue BracketView, apps/inorout BracketOverlay, apps/display BracketZone; 11.4b seed RPC + `get_cup_bracket` extension read this shape. |
| `venue_seed_knockout_from_groups(p_venue_token, p_competition_id, p_scheduled_date, p_kickoff_time, p_playing_area_ids uuid[])` | `venueSeedKnockoutFromGroups(venueToken, competitionId, scheduledDate, kickoffTime, playingAreaIds=[])` | anon + authenticated | **Migration 194 (Cycle 11.4b).** SECURITY DEFINER. Operator "Build knockout": seeds the bracket from final group standings. Requires `format='group_stage'`, `config.knockout_seeded=false`, no existing cup_ties, and **all group fixtures complete** (`group_stage_incomplete` else). Builds `v_ordered` = qualifiers in cross-group seeded order (all rank-1 by group, then rank-2, …) so group winners avoid each other early; calls `_cup_build_bracket`; flips `config.knockout_seeded=true`. Audit `knockout_seeded_from_groups`; notify venue+league. EV-verified (2 groups→final, qualifiers=winners, reject-before-complete + reject-re-seed; single-elim regression intact; leak 0). **Consumer**: apps/venue BracketView Build-knockout modal. |
| `_cup_build_bracket(p_competition_id, p_ordered text[], p_scheduled_date, p_kickoff_time, p_playing_area_ids uuid[], p_venue_id, p_league_id)` | *(none — internal)* | postgres/service_role only | **Migration 194 (Cycle 11.4b).** Internal builder extracted from `venue_persist_cup_bracket`: takes an already-ordered team list and creates `cup_rounds` + round-1 ties/fixtures/byes + rounds-2+ feeder ties + round-1 fee charges (ON CONFLICT skips pre-existing group-stage charges). Shared by `venue_persist_cup_bracket` (single-elim) and `venue_seed_knockout_from_groups` (group qualifiers) — no copy-paste. Not client-callable. |
| `venue_persist_cup_bracket(p_venue_token, p_competition_id, p_scheduled_date, p_kickoff_time, p_playing_area_ids uuid[], p_seed_team_ids text[])` | `venuePersistCupBracket(venueToken, competitionId, scheduledDate, kickoffTime, playingAreaIds, seedTeamIds)` | anon + authenticated | **Migration 185 (Cycle 11.1); refactored mig 194 (11.4b) to a thin caller over `_cup_build_bracket` — behaviour byte-identical, EV-regression-verified.** SECURITY DEFINER, search_path locked. Cup equivalent of `venue_generate_fixtures` — owns the WHOLE single-elim bracket, not just round 1. Caller via `resolve_venue_caller` (venue-ownership gate). Requires competition `type='cup'` AND `format='single_elimination'` (`not_single_elim_cup`); refuses if fixtures/ties already exist (`bracket_already_exists`). Computes canonical seeding order (textbook mirror), creates `cup_rounds` (one/round) + `cup_ties` (one/slot, with feeder edges), round-1 fixtures (round-robin across `p_playing_area_ids`, status `scheduled`, linked both ways via `cup_ties.fixture_id` ↔ `fixtures.cup_tie_id`); bye slots → `cup_ties.status='decided'` (no fixture). Raises round-1 fee charges identically to `venue_generate_fixtures`. Rounds 2+ left as `pending` ties with feeder pointers (advancement is Cycle 11.2). Audit `cup_bracket_generated`; notify venue + league. EV-verified (5 teams → 8-size, 3 byes, 3 rounds, 7 ties, leak 0). **Consumers (hard-rule #14)**: `apps/venue` SeasonWizard submit (single-elim branch); Cycle 11.2 advancement (reads ties/feeders); Cycle 11.3 `get_cup_bracket` display (venue/player/display). |

### Phase 5 RPCs (shipped)

| SQL function | JS wrapper | Grant | Notes |
|---|---|---|---|
| `team_admin_submit_lineup(p_admin_token, p_fixture_id, p_lineup, p_override_player_ids text[])` | `submitTeamLineup(adminToken, fixtureId, lineup, overridePlayerIds=[])` | anon + authenticated | Migration 159 (Cycle 5.6); **rewritten mig 162 (Cycle 5.7) — authoritative eligibility gate**. SECURITY DEFINER, search_path locked. Caller via `resolve_admin_caller` (admin_token OR VC player_token; mig 162 fixed the 5.6 VC bug). Asserts team-in-fixture (`not_your_fixture`), every picked player in squad (`players_not_in_squad`), no starting∩bench overlap (`player_in_starting_and_bench`). **Cycle 5.7 gates, all before any write:** squad size — `too_few_starters` / `too_many_subs` vs `league_config.min_starting`/`max_subs` (NULL = unbounded); double-reg — `player_double_registered` (hard, also writes a `lineup_double_registration_blocked` audit row); suspended/ineligible — `player_ineligible` unless the id is in `p_override_player_ids`. **Submit registers**: auto-upserts `player_registrations(active)` (ON CONFLICT DO NOTHING). Upserts `fixture_lineups`. Returns `{ok, starting_count, bench_count, registered_count, overridden[], warnings[]}` (`warnings` now always `[]` — former soft cases are hard gates). Audits `lineup_submitted` with `metadata.override_player_ids` (hard-rule #9). `p_lineup` = `{starting:[player_id], bench:[player_id], shirt_numbers:{player_id:int}}`. **DROP+CREATE** in mig 162 (added 4th arg → new overload; single-overload rule). **Consumers (hard-rule #14)**: writes `fixture_lineups`, consumed by `get_fixture_state_by_ref_token` (ref pre-match) + `get_team_next_fixture_lineup` (admin); Phase 4 reception display (future). |
| `team_admin_check_eligibility(p_admin_token, p_fixture_id, p_player_ids text[])` | `checkTeamLineupEligibility(adminToken, fixtureId, playerIds)` | anon + authenticated | Migration 162 (Cycle 5.7). **Read-only** (STABLE, SECURITY DEFINER, search_path locked). Caller via `resolve_admin_caller`. Returns `{ok, team_id, fixture_id, min_starting, max_subs, players:[{player_id, name, in_squad, double_registered, suspended, registration_status, suspension_until}]}` for the given candidate ids — per-player flags are properties of (player, fixture), independent of who's currently assigned. Powers the pre-submit UI badges + squad-size hints; submit (above) is the authoritative gate. **Consumers (hard-rule #14)**: `TeamsheetScreen` only; Phase 4 reception display (future). |
| `get_team_next_fixture_lineup(p_admin_token)` | `getTeamNextFixtureLineup(adminToken)` | anon + authenticated | Migration 159 (Cycle 5.6); **mig 162 swapped the caller resolution to `resolve_admin_caller`** (VC parity — paired-read rule). SECURITY DEFINER, search_path locked. Returns `{team_id, fixture, lineup}` — the team's next upcoming league fixture (`scheduled`/`allocated`, type `league`, ordered by date/kickoff) with opponent/date/venue/competition, plus the existing `fixture_lineups` row (or null). `fixture:null` for casual teams. **Consumers**: AdminView Teamsheet card + `TeamsheetScreen` (gating + prefill). |
|---|---|---|---|
| `get_player_competition_fixtures(p_token, p_filter)` | `getPlayerCompetitionFixtures(playerToken, filter='all')` | anon + authenticated | Migration 155 (Cycle 5.3). SECURITY DEFINER, search_path locked. Token → player → active competitions → that team's fixtures. `p_filter` ∈ `upcoming`/`past`/`all` (unknown → `all`). Per-row player perspective: `is_home`, `opponent_name`, `my_score`, `opponent_score`, `result` (`W`/`D`/`L` for completed; `W`/`L` for walkover+forfeit; null otherwise — reports status truthfully, no phantom 3-0; standings (mig 087) owns the 3-0 rule). Self-gating: casual token → `fixtures: []`. **Consumers (hard-rule #14)**: Cycle 5.3 player my-view `CompetitionFixturesCard` (built); Phase 4 reception "upcoming fixtures" panel (future); Phase 6 HQ "tonight's fixtures" feed (future). Return-shape additions for those won't break 5.3. |
| `get_player_fixture_detail(p_token, p_fixture_id)` | `getPlayerFixtureDetail(playerToken, fixtureId)` | anon + authenticated | Migration 156 (Cycle 5.4). SECURITY DEFINER, search_path locked. **Stricter gate than ref RPC**: fixture must be in one of the player's OWN active competitions AND involve one of the player's teams, else `fixture_not_visible`. Mirrors the mig-119 ref shape (fixture/competition/league/venue/pitch/both teams/both registered squads/events) PLUS perspective fields (`is_home`, `my_team_id`, `opponent_name`, `my_score`, `result`). Squads = LIVE active `player_registrations` (confirmed XI arrives 5.6). **Availability fields (`availability_counts`, `my_availability`) intentionally ABSENT — Cycle 5.5 adds them with same-commit mapper update (hard-rule #12).** **Consumers (hard-rule #14)**: 5.4 `FixtureDetailCard` (built); Phase 4 reception display; Phase 7 AI briefings. |
| `get_fixture_opposition_intel(p_token, p_fixture_id)` | `getFixtureOppositionIntel(playerToken, fixtureId)` | anon + authenticated | Migration 156 (Cycle 5.4). SECURITY DEFINER, search_path locked. Same visibility gate as detail. Returns `{ h2h:{all_time,this_season}, my_form, opponent_form, my_top_scorers, opponent_top_scorers, last_meeting }`. H2H/form from `fixtures` scores; top scorers from `match_events` (event_type='goal' — **no goals table**); walkover/forfeit → W/L only. **Consumers (hard-rule #14)**: 5.4 `OppositionIntel` (built); Phase 7 AI Gaffer narrative briefings. |

---

## PLAYER TOKEN RPCs (migration 011)

| SQL function | JS wrapper | Grant | Notes |
|---|---|---|---|
| `set_player_status` | `setPlayerStatus(token, status)` | anon | Sets status on players row. **Server-side sign-up-window gate (mig 241): refuses ANY status change — every value, not just 'in' — unless the active schedule is `game_is_live=true AND NOT is_cancelled` (raises `game_not_live`). The client hides the In/Out buttons when not live, but a stale client / direct call must not slip through.** Refuses 'in' if `admin_locked_in=true` (raises `admin_locked_in`). Refuses 'in' if squad_size cap met (raises `squad_full`). Updated in migrations 038, 241. |
| `set_player_paid` | `handleCashPayment(token)` → `setPlayerPaid(token)` | anon | **Mig 211:** PENDING CLAIM — flags self_paid only; owes UNCHANGED, ledger untouched. Debt is cleared by `admin_confirm_payment`, not here. (Was: cleared owes on self-declare.) |
| `set_player_injured` | `insertPlayerInjury(token, injured)` | anon | Writes player_injuries row |
| `set_my_nickname(p_token, p_nickname)` | `setMyNickname(token, nickname)` | anon + authenticated | **Migration 233 (session 77).** Player-self nickname edit for the My View pencil (modelled on `set_player_note`). Empty/blank clears the nickname; same-team case-insensitive clash check raises `nickname_taken`; >100 chars → `invalid_input`. Audit `player_nickname_updated_self`. Returns full player jsonb; fires `notify_team_change(...,'player_updated')`. **Distinct from admin `admin_update_player_name`** — that one needs an admin token and is used by SquadScreen + PlayerProfile admin-mode. The My View call site was wired to the admin RPC by mistake (signature drift in commit 7bd7ef2) and failed for every plain player until this. **Consumers:** apps/inorout PlayerView My View header only. |
| `set_player_contact(p_token, p_phone, p_channel)` | `setPlayerContact(token, phone, channel)` | anon + authenticated | **Migration 189 (Phase 9 finish).** Player-self contact-capture (modelled on `set_player_note`). Sets `players.phone` + `players.notification_channel` (push/email/sms/whatsapp; sms+whatsapp use `phone`, require it → `phone_required_for_channel`; email uses the linked auth email). Audit `player_contact_updated_self` (metadata stores `has_phone`, never the number). Returns `{ok, phone, notification_channel}`. **Consumers:** apps/inorout PlayerProfile NOTIFICATIONS section; the 48h/2h reminder crons' push→email→SMS fallback (Phase 9 finish, Part B). |
| `get_my_contact(p_token)` | `getMyContact(token)` | anon + authenticated | **Migration 189.** STABLE read for prefill: `{phone, notification_channel, has_linked_email}`. Avoids a `dbToPlayer` mapper ripple (hard-rule #12). |
| `add_guest_player` | `addGuestPlayer(hostToken, guestName)` | anon | Creates guest player row (`is_guest=true`, `guest_of=host`, `status='in'`) + team_players row. **Persistent guests (mig 216):** guests are no longer deleted on rollover — see `remove_guest_player` / `admin_go_live`. S2 will add a reactivate-dormant path. |
| `save_push_subscription` | `savePushSubscription(token, sub)` | anon | Upserts push_subscriptions |
| `get_my_payment_history` | `getMyPaymentHistory(token, limit?)` | anon | Player-side read of own payment_ledger. Mirrors `admin_get_player_ledger` shape; client uses `dbToLedger` for camelCase. Migration 039. |
| `get_my_injuries` | `getMyInjuries(token)` | anon | Player-side read of own player_injuries on the team the token belongs to. Migration 039. |
| `leave_squad` | `leaveSquad(token)` | anon | Soft remove from this team. Detaches team_players + push_subscriptions; player row + history preserved. Refuses with `debt_owed:<amount>` if owes > 0. Audit + notify_team_change. Migration 040. |
| `delete_my_account` | `deleteMyAccount(token)` (calls `/api/delete-account`) | anon | Anonymises players row (name → "Deleted player", token/user_id/nickname cleared, disabled+reason set) — preserves FKs from player_match / payment_ledger / player_injuries / potm_votes. Detaches all team_players. Deletes push_subscriptions + player_career. Revokes admin grants. Returns `auth_user_id`; edge function (`apps/inorout/api/delete-account.js`) calls `supabase.auth.admin.deleteUser` to finish. Refuses with `last_admin:<csv of team_ids>` if the user is the only non-revoked admin of any team. Audit per team. Migration 040. |

---

## ADMIN TOKEN RPCs (migrations 012–018)

| SQL function | JS wrapper | Notes |
|---|---|---|
| `admin_get_team_state` | `getTeamStateByAdminToken(adminToken)` | Bulk read — all squad/schedule/match data |
| `admin_add_player` | `addPlayerToTeam(adminToken, name, type, priority)` | Writes players + team_players rows, generates token |
| `admin_remove_player` | `deletePlayer(adminToken, playerId)` | Soft delete via disabled flag |
| `admin_update_player_name` | `adminUpdatePlayerName(adminToken, playerId, name)` | |
| `admin_set_vice_captain` | `toggleViceCaptain(adminToken, playerId, value)` | Writes team_players.is_vice_captain |
| `admin_set_player_group` | `setPlayerGroup(adminToken, playerId, groupNumber)` | Group Balancer; writes team_players.group_number (1–5 or NULL); audit_events |
| `admin_clear_all_groups` | `clearAllGroups(adminToken)` | Group Balancer; sets every group_number to NULL for the team; returns cleared_count |
| `admin_set_player_priority` | `setPlayerPriority(adminToken, playerId, priority)` | |
| `admin_set_player_status` | `adminSetPlayerStatus(adminToken, playerId, status)` | Sets status. Setting 'in' also flips `admin_locked_in=true`; setting out/maybe/reserve/none clears it. Refuses 'in' if cap met (raises `squad_full`). Audits before/after + locked_after. Updated in migration 038. |
| `admin_disable_player` | `disablePlayer(adminToken, playerId, disabled)` | |
| `admin_confirm_payment` | `handleMarkPaid(adminToken, playerId, matchId)` (payments.js) / `confirmPayment(adminToken, playerId, matchId)` (supabase.js) | Sets paid=true; ledger cross-path promotion. **Mig 211:** now also clears `owes=0` (confirmation is the money event). `admin_reset_payment` restores owes when undoing a confirmed payment. `set_guest_payment` got an audit row + a text→uuid `v_ledger_id` fix. Payment RPC wrappers (`setPlayerPaid`/`setGuestPayment`/`resetPayment`/`waiveDebt`) now live in supabase.js. |
| `admin_reset_payment` | `handleResetPayment(adminToken, playerId, matchId)` | Resets all payment flags + ledger |
| `admin_waive_debt` | `handleWaiveDebt(adminToken, playerId, note)` | Zeros owes; writes waiver ledger entry; notify |
| `admin_save_match_result` | `saveMatchResult(matchId, teamId, adminToken, match)` | Writes result + the full end-of-match cascade. **p_team_a/p_team_b/p_motm/p_bib_holder are player IDs** (stored on matches.* as IDs; display layers must resolve id-first, name-fallback). **Freshness = `matches.winner WAS NULL` before this save** (migs 205/206) — NOT a player_match-row-count check (the kickoff lineup-lock pre-creates player_match rows and defeated the old check, silently skipping the cascade → session-68 £0-outstanding/empty-history/dead-bib-tracker bug). Fresh block (once per match): w/l/d, owes+=price for unpaid non-guest attendees, INSERT payment_ledger game_fee/unpaid per same, reset attended+1/total+1/team=NULL/status='none'/**admin_locked_in=false**, goals, motm, bib_history cascade (+bib_count). **mig 241 changes to the fresh block:** (1) **CLOSES the game** — `schedule.game_is_live=false` so the played match stops accepting sign-ups (gates client buttons AND set_player_status); (2) resets **EVERY remaining squad status → 'none'** (reserves/maybes/no-shows), not just attendees — a blanket pass after the attendee reset, since the attendee-only `WHERE pm.attended=true` left reserves stuck; (3) the attendee reset now **preserves `paid=true`** for any player whose game_fee ledger for THIS match is already `paid` (was an unconditional `paid=false` wipe that diverged My View from the ledger) — `self_paid` still cleared; go-live resets status but NOT paid, and next week's save keys on next week's match_id so no stale paid carries forward. **Never set matches.winner before the first result save** or the cascade is skipped. **Consumers** of the stored IDs: StatsView, HistoryView share text, PlayerLeagueTable (hard-rule #14). See [[project_result_save_invariants]]. |
| `admin_save_teams` | `confirmTeams(adminToken, matchId, teamA, teamB, predictedWinner?, predictedConfidence?, balanceScore?)` | Sets matches.team_a/team_b on confirm + writes denormalised players.team (clears for all team_players, then sets 'A'/'B' on the confirmed ids) so PlayerView Live Board renders the team sheet. 3 trailing prediction params (Group Balancer, migration 031) write to matches.predicted_winner/confidence/balance_score. Migration 043 added players.team write; **migration 048 scoped the two SET statements via team_players join to close a cross-team write surface** (a legit admin for team X could previously pass team Y player_ids in p_team_a/p_team_b and flip their team value — foreign IDs now silently filtered). Migration 031 added prediction params. Old 5-arg signature dropped. |
| `admin_save_bib_holder` | `saveBibHolder(adminToken, matchId, playerId, name)` | 4-step atomic: match bib_holder, player bib_count++, bib_history upsert, had_bibs flag |
| `admin_cancel_match` | `adminCancelMatch(adminToken, reason)` | Atomic cancel — replaces 7-step cancelWeek() |
| `admin_reopen_week` | `reopenWeek(adminToken)` | Reopens a cancelled week: clears schedule.is_cancelled / cancel_reason, sets game_is_live=true, inserts a fresh matches row, points active_match_id at it. Previously cancelled match stays in history (cancelled=true). Migration 031 sibling. |
| `admin_go_live` | `goLive(adminToken)` | Manual "Open Next Week". Idempotent (reuses a live active_match_id). **Mig 204:** when a NEW match is created, resets the squad to a fresh board — status='none', admin_locked_in=false, team=NULL for all team_players — so last week's in/out + A/B don't carry over. **Mig 243:** the new-match reset now ALSO clears the per-game payment flags — `paid=false, self_paid=false, paid_by=NULL, paid_at=NULL` — so a freshly-opened game starts with a clean paid slate (previously last week's "Paid" showed in My View and in Payments → PAID UP for a game nobody had paid for yet). **`owes` is still left untouched** (running debt balance; `payment_ledger` keeps the permanent per-match record, so no history is lost). Supersedes the mig-204 "payment fields carry over" behaviour. **Mig 207:** also deletes all is_guest=true rows for the team on new-match creation (team_players rows first, then players via guest_of) so hosts see a fresh Plus One button each week. Gated to new-match path so double-taps never wipe a guest added for the live week. **Mig 208:** resolves the caller via `resolve_admin_caller` so a Vice Captain's player token works (was a bare admin_token lookup → VC got `invalid_admin_token`); audit actor_type now reflects `vice_captain`. The sibling `admin_reorder_reserves` got the identical mig-208 fix. **Mig 216 (PERSISTENT GUESTS S1):** the mig-207/209 guest-DELETE blocks are REMOVED — guests are no longer deleted on rollover. The mig-204 bulk status reset already includes guests (they have team_players rows), so they're left DORMANT (status='none', team=NULL, admin_locked_in=false) with their player_match history intact, hidden from the board but kept for S2's returning-guest picker. **Mig 230:** sets a transaction-local `inorout.bulk_reset` GUC (`set_config(...,true)`) immediately before the bulk status reset so the new `notify_spot_opened` spot-opened trigger skips the mass in→none transitions (anti-spam). Additive; signature/grants unchanged. |
| `admin_go_live_for_team` | _(none — cron-only; service-role `supabase.rpc()` in cron.js `autoOpenGameJob`)_ | Service-role sibling of `admin_go_live` (mig 126); same mig-204 squad reset. **Mig 216:** guest-DELETE removed (dormant-not-delete), matching `admin_go_live`. **Mig 230:** same `inorout.bulk_reset` GUC guard before the reset. **Mig 243:** same per-game payment-flag clear on new-match creation (`paid/self_paid/paid_by/paid_at` → reset; `owes` untouched), matching `admin_go_live`. |
| `admin_promote_guest` | `promoteGuest(adminToken, guestId)` | anon + authenticated | **Migration 218 (PERSISTENT GUESTS S3).** Admin "make permanent": flips `is_guest=false, guest_of=NULL` on the same row (token/status/stats/player_match history all preserved → player starts counting in reliability+POTM via existing `is_guest=false` filters). `resolve_admin_caller` (VC parity); guest must be `is_guest=true` on the team else `not_found`. Audit `guest_promoted` (route=admin), broadcast `player_updated`. Returns updated player jsonb. Consumer: SquadScreen kebab "Make permanent". |
| `reactivate_guest_player` | `reactivateGuestPlayer(hostToken, guestId)` | anon + authenticated | **Migration 217 (PERSISTENT GUESTS S2).** Host brings back a DORMANT team guest from the Plus One picker. Re-attaches the guest to the calling host (`guest_of`), sets `status='in'`, `team=NULL`, `admin_locked_in=false`, and resets per-week payment to a fresh-guest baseline (`paid=false`, `self_paid=false`, `paid_by/paid_at=NULL`, `owes=0`) — but **keeps accumulated stats + player_match history** (same row). Guest must be `is_guest=true` on the caller's team else `not_found`. Audit `guest_player_reactivated_self`, broadcasts `guest_player_added`. Returns the now-active guest jsonb (same shape as `add_guest_player`) → client swaps the dormant squad row. |
| `remove_guest_player` | `removeGuestPlayer(hostToken, guestId)` | anon + authenticated | Host removes their +1. **Mig 216 (PERSISTENT GUESTS S1):** DORMANT, not delete — `UPDATE players SET status='none', team=NULL, admin_locked_in=false` (keeps the players row, the team_players row, and player_match/payment_ledger history). Frees the host's Plus One button + keeps the guest available in S2's picker. Audit `guest_player_removed_self` (metadata `mode='dormant'`), broadcasts `guest_player_removed`. Reverses the mig-209 deleting body. |
| `admin_upsert_schedule` | `upsertSchedule(adminToken, schedule)` | Includes p_game_is_live (added session 27). Mig 207: game_date_time now uses `AT TIME ZONE 'Europe/London'` (was UTC-naive — caused 1hr BST offset on all kickoff-relative cron jobs). Grant corrected to authenticated-only (anon had EXECUTE via stale 13-param REVOKE that never matched the live 14-param signature). **Mig 210:** resolves the caller via `resolve_admin_caller` so a Vice Captain's player token works (was bare admin_token lookup → VC got invalid_admin_token); audit actor_type reflects vice_captain; anon re-granted (token check is the gate, matching all sibling admin RPCs). VC parity now complete across all casual admin_* RPCs. |
| `admin_upsert_settings` | `upsertSettings(adminToken, groupName, groupLabels?)` / `saveGroupLabels(adminToken, groupName, groupLabels)` | Now takes optional p_group_labels jsonb (migration 031). COALESCE means passing NULL preserves existing labels. Old 2-arg signature dropped. |
| `admin_close_potm_voting` | `closePOTMVoting(adminToken, matchId, winnerId, wasAdminDecided)` | Updates matches + player_match |
| `admin_reset_player_token` | `resetPlayerToken(adminToken, playerId)` | Generates new token |
| `set_player_nickname` | `setPlayerNickname(adminToken, playerId, nickname)` | Admin sets nickname |
| `clear_player_injury` | `clearPlayerInjury(adminToken, playerId)` | Sets cleared_at |

---

## DRAFT RPCs

| SQL function | JS wrapper | Notes |
|---|---|---|
| `admin_save_teams_draft` | `saveTeamsDraft(adminToken, matchId, a, b)` | Saves draft without confirming |

---

## AUTH RPCs

| SQL function | JS wrapper | Auth | Notes |
|---|---|---|---|
| `link_player_to_user` | `linkPlayerToUser(token)` | authenticated only | Links player token to auth.uid(); guards double-link |
| `claim_my_admin_teams` | `claimMyAdminTeams()` | authenticated only | **Migration 240. WRITE** (ephemeral-verified: claimed=1, player+team_players+team_admins+audit =1, idempotent on re-call, rolled back, leak 0). Adopts superadmin-created squad SHELLS (mig 239) into the signed-in user's account: for each unclaimed team whose `admin_email` = the user's **verified** `auth.email()`, creates the admin player (user_id) + team_players + team_admins so it shows in My Squads. "Unclaimed" = no active `team_admins`, so it can never hijack an owned squad; idempotent (skips already-claimed / already-member). Audits `admin_claimed_by_email`. Called fire-and-forget post-sign-in from `apps/inorout` App.jsx (isolated effect). **PWA-affecting (Hard Rule #13): needs a real-iPhone sign-in test before relied on.** |
| `player_join_team` | `playerJoinTeam(teamId, name)` | authenticated only | Handles new + returning players; upserts team_players. **Migration 044 fixed pre-Beta launch blocker:** new-player INSERT branch now generates a player token via `generate_url_safe_token('p_', 14)` (the same helper `create_team` uses). Pre-fix, first-time joiners landed with `player.token=NULL` → JoinSuccess.jsx fell back to `/` → stranded on landing page. |
| `player_get_teams` | `getPlayerTeams()` | authenticated only | Returns all squads for auth.uid(); anon revoked |
| `player_get_teams_by_token` | `getPlayerTeamsByToken(token)` | anon + authenticated | Token-resolved variant of `player_get_teams` for iOS PWA (storage-partitioned, unauthed at request time) — mig 072. Returns all of the token-owner's squads. **Mig 153 (Cycle 5.1)** added `is_competitive` (squad has an active registration in a league-type competition). Consumer: `MySquads.jsx` LEAGUE pill. |

---

## ONBOARDING RPCs

| SQL function | JS wrapper | Auth | Notes |
|---|---|---|---|
| `create_team` | `createTeam(teamData)` | authenticated | Atomic: team + players + schedule + settings + team_admins; full rollback on error |

---

## QUERY RPCs (reads)

| SQL function | JS wrapper | Notes |
|---|---|---|
| `get_team_state_by_player_token` | `getTeamStateByPlayerToken(token)` | Bulk read for player route; includes match_stats, win_rate, reliability, ledger, last_match_meta, player_form |
| `get_team_state_by_admin_token` | `getTeamStateByAdminToken(adminToken)` | Bulk read for admin/demoadmin routes |
| `submit_potm_vote` | `submitPOTMVote(matchId, teamId, voterToken, nomineeId)` | SECURITY DEFINER; UNIQUE violation returns {error:"already_voted"} |
| `get_potm_voting_state` | `getPOTMVotingState(matchId, teamId, voterToken)` | Returns eligible players + existing vote. **NB: still ships the full `votes` array (voter_id + nominee_id) to every eligible client — a latent who-voted-for-whom leak; `get_potm_tally_public` exists so the running tally never has to widen it.** |
| `get_potm_tally_public` | `getPOTMTallyPublic(token, matchId, teamId)` | **Migration 242. READ-only** (STABLE SECURITY DEFINER, anon + authenticated). Running POTM tally for players — **counts only** (`{voted, tally:[{nominee_id, votes}] winner-first, total_votes}`), **NEVER voter identities** (deliberately does not widen the `get_potm_voting_state` leak above). **Server-enforced "not before you vote" gate:** if the caller's token has no `potm_votes` row for this match → returns `{voted:false}` with no counts. Match-wide aggregation (both teams, to match the eligible-nominee set); `p_team_id` only validates the match belongs to the squad (`match_not_found`). Guests excluded (decision 2, mirrors mig 219 `get_potm_tally`). Read-only → no ephemeral-verify. **Consumer:** `POTMVotingModal.jsx` voted-state live leaderboard; re-fetched on the `team_live` broadcast (App.jsx:882) — no new channel. |
| `find_player_by_email` | `findPlayerByEmail(email)` | Returns [{token, player_id, player_name, team_id, team_name}] |
| `get_head_to_head_raw_by_admin_token` | called via `getHeadToHead(meId, themId, teamId, period, adminToken)` | Migration 041. Returns 3 jsonb arrays (all_time_matches, period_matches, player_match_rows for both players). JS function branches on adminToken — RPC for admin-token routes (unblocks anon /demoadmin), direct reads for authenticated player sessions. Server-side period cutoff mirrors `scoring.js#periodCutoff`. |
| `get_player_league_table_raw_by_admin_token` | called via `getPlayerLeagueTable(teamId, period, adminToken)` | Migration 042. Returns 5 jsonb arrays (period_matches, player_match_rows, all_time_attended summaries, all_team_match_dates, players). Same branch-on-adminToken pattern as 041. Used by StatsView (to populate form + reliability columns) and HeadToHead (Section 4 Overall Comparison bars). |

---

## SUPER-ADMIN RPCs (migrations 045, 046)

The `apps/superadmin` dashboard at `https://platform-superadmin-*.vercel.app`
calls these four RPCs. All gated by `is_platform_admin()` (a global, cross-team
authorisation helper that checks the caller's `auth.uid()` against the
`platform_admins` table — see SCHEMA.md). New entries to `platform_admins` are
inserted by hand via SQL only; there is intentionally no UI to grant this role.

| SQL function | JS wrapper | Notes |
|---|---|---|
| `is_platform_admin` | (internal helper, no JS wrapper) | SQL boolean. Used as a precondition in every superadmin_* RPC. Returns true iff `auth.uid()` exists in `platform_admins`. |
| `superadmin_whoami` | `superadminWhoami()` | Returns `{signed_in, user_id, email, is_platform_admin}`. App-level gate after Supabase Google OAuth — if `is_platform_admin=false`, the UI shows an "Access denied" card with the signed-in email. |
| `superadmin_list_teams` | `superadminListTeams()` | Returns jsonb array of every team with: team_id, name, admin_email, join_code, onboarding_complete, created_at, player_count, admin_count, last_match_date, outstanding_total (sum of players.owes>0), admin_emails[]. Powers the Teams tab. |
| `superadmin_team_detail` | `superadminTeamDetail(teamId)` | Returns jsonb `{team, schedule, squad[], matches[], payments, admins[], recent_events[]}`. Squad includes player tokens (for /p/TOKEN deep-link inspection) and `owes`. Matches limited to last 10. Events limited to last 20. Powers the Team Detail tab. |
| `superadmin_recent_activity` | `superadminRecentActivity({limit, sinceHours})` | Returns jsonb array of audit_events joined with team name + actor email. Default limit 100, default 24h window. Powers the Activity tab. |
| `superadmin_engagement(p_from date, p_to date)` | `superadminEngagement(from, to)` | **Migration 235.** STABLE, read-only. Granular engagement analytics for the **Engagement tab** — the explorable per-squad × per-feature-category counterpart to the lean ops EMAIL digest (mig 234). Buckets every in-window `audit_events` action into a category (opens/availability/squad_mgmt/team_selection/match_lifecycle/results/potm/payments/injuries/guests/profile) and returns `{range, squads_total, squads_active, total_events, opens{total,admin,player,distinct_users}, team_selection{saved,confirmed,ai,manual}, categories[{key,total,actions[]}], per_squad[{team_id,name,last_active,days_since,total,counts{}}]}`. AI vs manual team selection split via `match_teams_saved` metadata `balance_score` (Smart Teams populates it, manual leaves null); opens split admin/player via `app_boot` metadata `route_type`. **Real squads only** (`team_demo%` + `team_dc%` stripped, Hard Rule #15). `is_platform_admin()` gated (mirrors `superadmin_recent_activity`); anon revoked, authenticated granted. **BLIND SPOT** (documented): logs writes not views — "results checked / table viewed" needs app view-instrumentation (a planned follow-up that becomes another category here with no shape change). **Consumer**: `apps/superadmin` Engagement tab. |
| `superadmin_health(p_from date, p_to date)` | `superadminHealth(from, to)` | **Migration 236.** STABLE, read-only. Squad-health analytics for the **Health tab**, four blocks: `funnel` (activation — per squad created→week opened→players responded→teams picked→result recorded, all-time milestones + stall stage), `notification` (reach — roster vs reachable, where reachable = real delivery path: push subscription / phone / linked-account email; the `notification_channel` preference is **deliberately ignored** — it defaults to 'push' for everyone and overstates reach), `install` (windowed `app_boot` — PWA-installed vs browser, signed-in vs anonymous), `response` (windowed — roster vs who marked in/out). **Squad membership is `team_players`, NOT `players.team`** (which is the A/B matchday side — the same trap fixed in mig 234 same commit). Real squads only (`team_demo%`+`team_dc%` stripped). `is_platform_admin()` gated; anon revoked, authenticated granted. **Consumer**: `apps/superadmin` Health tab. |
| `superadmin_create_team(p_team_name, p_admin_email, p_day_of_week, p_kickoff, p_squad_size, p_venue?, p_price?)` | `superadminCreateTeam({name, adminEmail, dayOfWeek, kickoff, squadSize, venue?, price?})` | **Migration 239. WRITE** (ephemeral-verified: team=1 schedule=1 settings=1 members=0 admins=0 audit=1, rolled back, leak 0). Operator-led casual squad creation — the casual twin of `superadmin_create_venue`. Creates the squad SHELL only (team + schedule + settings + `admin_token`); **no players, no team_admins**. The `admin_token` IS the hand-off access (`/admin/<admin_token>`, same model as `create_team`'s token); the new organiser runs the squad via that link. Returns `{team_id, admin_token, join_code, name}`. `is_platform_admin()` gated (anon revoked); audits `team_created_by_admin` (actor_type `super_admin`). Mirrors `create_team`'s DST-safe schedule setup. **Consumer**: `apps/superadmin` Create Squad tab. |

**Foundation:** `platform_admins` table (migration 045) is the global authorisation
layer parallel to `team_admins` (per-team). The `audit_events.actor_type` CHECK
constraint already allows `'super_admin'` — write surfaces (Phase 3/4, not yet
shipped) will use that actor_type and `auth.uid()` as actor_user_id for traceability.

---

## ASK THE GAFFER RPCs (context builders — Phase 1)

Spec: `GAFFER.md`. Called only by `apps/inorout/api/gaffer.js` (Vercel edge function).
Each returns a jsonb context block consumed by the surface-matching system prompt.
Auth via `p_admin_token`; anon grant is fine because the token is the auth signal
(same pattern as other admin RPCs).

| SQL function | JS wrapper | Surface | Notes |
|---|---|---|---|
| `gaffer_get_context_team_summary` | (none — edge function calls directly) | team_summary | Squad/status counts, recent form, top scorer/reliable 30d, last POTM, schedule. Migration 034. |
| `gaffer_get_context_payment_summary` | (none — edge function calls directly) | payment_summary | Outstanding total + count, oldest debt, top 5 owers, last-week collected vs owed, always-paid list. Migration 035. |
| `gaffer_get_context_attendance_risk` | (none — edge function calls directly) | attendance_risk | Squad shortfall, hours to kickoff, declining regulars (4-week vs prior 4-week rate drop), not-responded list, cover pool depth, risk_level enum. Migration 036. |
| `gaffer_get_context_matchday_briefing` | (none — edge function calls directly) | matchday_briefing | Confirmed squad (with VC + group), predicted teams (Smart Teams), bib rotation, in-form players, last POTM. Migration 037. |

**Edge function wrappers** (NOT supabase.rpc — they POST to /api/gaffer):
| JS wrapper | Notes |
|---|---|
| `getGafferBriefing(adminToken, surface, opts?)` | All structured surfaces. Returns `{content, briefingId, cached, surface, model, tokensIn, tokensOut, costPence}` or `{error}`. |
| `askGafferQuestion(adminToken, question, opts?)` | Q&A surface — concatenates all four structured contexts and lets Claude answer freeform under grounding rules. |

---

## LEAGUE MODE — PHASE 3 RPCs (ref view, migration 119 onwards)

Spec: `LEAGUE_MODE_SCOPE.md` § Phase 3. Ref opens
`https://app/ref/<ref_token>` on their phone — token is on
`fixtures.ref_token` (auto-generated at fixture INSERT, mig 055).
Token grants access to exactly one fixture. Anon-callable so refs
don't need an account; also granted to authenticated for future
ref OAuth without re-grant.

| SQL function | JS wrapper | Notes |
|---|---|---|
| `get_fixture_state_by_ref_token` | `getFixtureStateByRefToken(refToken)` | Migration 119. Single-fixture read for the ref pre-match + resume path. Returns `{fixture, competition, league, venue, pitch, official, home_team, away_team, home_squad, away_squad, events, caller}`. Migration 120 added `fixture.actual_kickoff_at` for the live-timer source. **Mig 160 (Cycle 5.6) made squads lineup-aware**: if a `fixture_lineups` row exists for a team, `home_squad`/`away_squad` return only its starting+bench (each tagged `lineup_role` 'starting'/'bench', shirt from the lineup overriding `players.shirt_number`, ordered starting-then-bench); otherwise the full active `player_registrations` squad **exactly as before** plus `lineup_role:null` (additive only, hard-rule #12). Squad logic lives in internal helper `_fixture_squad_json` (granted to nobody). Events ordered by `(minute, created_at)` for offline-resume. Raises `invalid_ref_token`. **Consumers**: apps/ref PreMatch/LiveMatch/PostMatch; apps/inorout `FixtureDetailCard` (reads `home_squad`/`away_squad`, ignores `lineup_role`). |
| `ref_start_match` | `refStartMatch(refToken, clientEventId, localTimestamp?)` | Migration 120. `status: scheduled\|allocated → in_progress`. Records `actual_kickoff_at` and inserts a `period_change` 1H event. Broadcasts `match_started` to both teams. |
| `ref_record_goal` | `refRecordGoal(refToken, {playerId, minute, period, clientEventId, ownGoal?, localTimestamp?})` | Migration 120. Resolves scorer's team via `player_registrations`. `ownGoal=true` stores `event_type='own_goal'` with `team_id = scorer's own team` (counts for opposite in materialisation). Idempotent on `clientEventId` — replay is a no-op. |
| `ref_record_card` | `refRecordCard(refToken, {playerId, minute, period, colour, clientEventId, localTimestamp?})` | Migration 120. `colour ∈ {yellow,red}` → `event_type ∈ {yellow_card,red_card}`. Idempotent. |
| `ref_record_substitution` | `refRecordSubstitution(refToken, {onPlayerId, offPlayerId, minute, period, clientEventId, localTimestamp?})` | Migration 120. Both players must register to the same team in this competition. Idempotent. |
| `ref_set_period` | `refSetPeriod(refToken, period, clientEventId, localTimestamp?)` | Migration 120. `period ∈ {HT,2H,ET1,ET2,PEN}`. Inserts a `period_change` event. Idempotent. |
| `ref_undo_event` | `refUndoEvent(refToken, clientEventId)` | Migration 120. DELETEs the `match_events` row by `client_event_id`. Idempotent (missing row treated as no-op). Server requires `status='in_progress'`; the 30-sec undo window is client-enforced. |
| `ref_confirm_full_time` | `refConfirmFullTime(refToken)` | Migration 120. Materialises `home_score`/`away_score` from match_events (goals(home)+own_goals(away), mirror). Transitions `status → completed`. Broadcasts `match_result_saved`. Standings recompute on-read via `get_league_standings_for_player` — no separate cascade RPC. |
| `ref_set_clock` | `refSetClock(refToken, action, clientEventId, localTimestamp?)` | **Mig 264 (Ref V2).** `action ∈ {pause,resume}` — pauses/resumes the per-fixture clock. Records a `clock_pause`/`clock_resume` match_event for the idempotency key, then mutates `fixtures.clock_paused_at/_ms` only on a FRESH insert + state guard (can't pause-while-paused) → a re-drained [pause,resume] pair can't inflate paused time. Offline-safe (uses the client timestamp). Broadcasts `venue_live` only (no clock on the team tab). |
| `ref_record_note` | `refRecordNote(refToken, {text, playerId?, minute, period, clientEventId, localTimestamp?})` | **Mig 264 (Ref V2).** Free-text incident note → `match_events(event_type='note', note_text)`, optionally attributed to a player. Idempotent. Team+venue broadcast. |
| `ref_record_sin_bin` | `refRecordSinBin(refToken, {playerId, minute, period, durationMin, clientEventId, localTimestamp?})` | **Mig 264 (Ref V2).** Temporary dismissal → `match_events(event_type='sin_bin', duration)`. Player resolved via `player_registrations`. Idempotent. Team+venue broadcast. |
| `ref_set_added_time` | `refSetAddedTime(refToken, {period, minutes, clientEventId, localTimestamp?})` | **Mig 264 (Ref V2).** Absolute set of `fixtures.added_time[period] = minutes` (idempotent on replay). Renders "45 +N" on reception/venue. Venue broadcast. |

**Ref V2 (mig 264–265):** the **eleven** `ref_*` write RPCs (above) + the read RPC now also carry the
new design. `get_fixture_state_by_ref_token` (mig 265) gained resolved `match_format`
(league_config → competitions.config.match_format → fixtures.format_override, with `is_overridden`),
`fixture.clock_paused_at/_ms/added_time/format_override`, `note_text`/`duration` on events, and
**RESTORED `actual_kickoff_at`** (mig 160 dropped it — see BUGS.md session 87). **Consumers (hard-rule #14):**
`get_fixture_state_by_ref_token` → apps/ref (rebuilt, Ref V2); the new write RPCs → apps/ref;
all broadcast `venue_live` → apps/display + apps/venue (which refetch on any ping).

**All eleven `ref_*` write RPCs**: SECURITY DEFINER, search_path locked,
EXECUTE granted to `anon + authenticated`. Token-gated via private
helper `_ref_resolve_fixture` (anon/authenticated explicitly revoked —
Supabase auto-grants every public function so a plain `REVOKE FROM
PUBLIC` doesn't catch those roles). Every successful write inserts an
`audit_events` row (`actor_type='referee'`, `actor_identifier=ref_token`)
and fires THREE realtime broadcasts:
- `notify_team_change(home_team_id, reason)` →
  `team_live:<live_channel_key>` (subscriber: `apps/inorout` App.jsx)
- `notify_team_change(away_team_id, reason)` → same channel pattern
- `notify_venue_change(venue_id, reason)` →
  `venue_live:<live_channel_key>` (subscriber: `apps/venue` App.jsx
  via mig 121) — fires once per event regardless of teams. Helper:
  `_ref_venue_id_for_fixture(p_fixture)` (private; competition →
  season → league → venue lookup).

Both broadcast functions' whitelists were extended in the same
migration as the calling RPCs (avoiding the §6.3 drift bug):
- `notify_team_change` gained `match_started` +
  `match_event_recorded` (mig 120).
- `notify_venue_change` was introduced in mig 121 with whitelist
  `match_started`, `match_event_recorded`, `match_result_saved`.
  **Important**: mig 121 silently shrunk the whitelist from mig
  101's 26 reasons to 3 (Phase 2 reasons started logging WARNINGs).
  Mig 127 restored the full Phase 2 list + added `result_corrected`.
  See BUGS.md "notify_venue_change regressed in mig 121".

### Venue overrides (mig 127, session 51)

| SQL function | JS wrapper | Notes |
|---|---|---|
| `venue_update_fixture_result` | `venueUpdateFixtureResult(venueToken, {fixtureId, homeScore, awayScore, reason})` | Migration 127. The ONLY path for correcting a result after `ref_confirm_full_time`. Token-gated via `resolve_venue_caller`. Requires fixture in `status='completed'` + non-empty reason + non-negative scores. Audit-logged with previous + new scores + reason. Broadcasts `result_corrected` (team) + `result_corrected` (venue) + `fixture_result_corrected` (league). Verified end-to-end via Supabase MCP (8 assertions, no leak). **Consumers**: Phase 5+ venue dashboard "edit result" UI (planned, not yet built — RPC is callable via MCP/SQL today). |

## LEAGUE MODE — PHASE 4 RPCs (reception display, migrations 164–168)

Spec: `LEAGUE_MODE_SCOPE.md` § Phase 4. The reception big-screen runs at
`https://<display-app>/display/<display_token>` on a venue TV. **Venue-scoped**
on `venues.display_token` (a per-venue READ-ONLY public token, distinct from the
operator's `venue_admin_token`). Consumer app: `apps/display` (new standalone Vite
SPA, own Vercel project). The display subscribes to the existing
`venue_live:<venues.live_channel_key>` broadcast — every `ref_*` write already
publishes there, so live scores update with no polling.

| SQL function | JS wrapper | Grant | Notes |
|---|---|---|---|
| `get_display_state(p_display_token)` | `getDisplayState(displayToken)` | anon + authenticated | Migration 165, enriched 221 (standings `form`) + 244 (broadcast redesign). **Read-only** (STABLE, SECDEF, search_path locked). Resolves the venue from `display_token`; returns one jsonb: `venue` (incl. `live_channel_key` + `display_config`; **never `display_pin`**), `competitions[]` (all `status='active'` comps across the venue's leagues — each with `standings_confirmed` + `standings_live` + `top_scorers` (244: rows gain `apps` from completed-fixture lineup membership + `shirt_number`; no `position`/`mom`/`g_per_90` — display derives G/90)), `live_fixtures[]` (in-progress, live-computed scores + last 6 events), `upcoming_fixtures[]` (today, Europe/London; 244: rows gain `competition_type`, `round_name`, `official_id`, `official_name` — null official drives the "Needs ref" chip), `bookings[]` (244: today's confirmed casual pitch bookings backed by active `pitch_occupancy` — `{booking_id, kickoff_time, pitch_name, booked_name, source_kind:'booking'}`), `recent_results[]` (today), `goals_ticker[]` (today), `server_time`. Standings **lift the proven `get_league_standings_for_player` scoring byte-for-byte** (walkover/forfeit → 3/0; W/D/L from effective score; gated on `leagues.standings_visibility='public'`); the LIVE pass also folds in-progress `match_events` scores (goals(home)+own_goals(away), mirrors `ref_confirm_full_time`). Raises `invalid_display_token`. **Consumers (hard-rule #14)**: `apps/display` (reception broadcast redesign Part B); Phase 6 HQ (future); Phase 7 Gaffer briefings (future). Wrapper is a raw jsonb passthrough — no mapper. |
| `check_display_pin(p_display_token, p_pin)` | `checkDisplayPin(displayToken, pin)` | anon + authenticated | Migration 166. **Read-only** (STABLE, SECDEF, search_path locked). Returns `{pin_required, ok}` — keeps the PIN server-side (never in the state payload). `pin_required=false` when `venues.display_pin IS NULL`. The 3-strike / 30-min lockout is **client-side** (localStorage) in `apps/display`. Raises `invalid_display_token`. **Consumers**: `apps/display` PinGate. |
| `venue_update_display_config(p_venue_token, p_config, p_display_pin DEFAULT NULL)` | `venueUpdateDisplayConfig(venueToken, config, displayPin=null)` | anon + authenticated | Migration 167, capability-gated 239 (`manage_facility`), enriched 245 (sponsor + featured pin). **Write** (ephemeral-verify gated). Operator-only via `resolve_venue_caller`. Writes the WHOLE `p_config` into `venues.display_config` (the UI must always send the full object) — validated: `mode∈fixed/cycle/smart`, `interval_secs 10–60`, `zones` ∈ known keys; 245 adds `sponsor_image_url`/`sponsor_label`/`sponsor_title`/`sponsor_body`/`sponsor_url`/`featured_pin_story_tag`/`custom_message` string-or-null (`config_field_invalid`, DETAIL=key), `sponsor_ratio` number clamped server-side to 0..1 (`sponsor_ratio_invalid`), `featured_fixture_id` uuid-or-null that must belong to this venue's competitions (`featured_fixture_invalid` / `fixture_not_in_venue`), `featured_pin_expires_at` timestamptz-or-null (`featured_expiry_invalid`). Optional `display_pin` (NULL = leave, '' = clear, else 4–8 digits → `pin_invalid`). Audit `display_config_updated` + `notify_venue_change('venue_updated')` (live displays re-pull on save). **Consumers**: `apps/venue` DisplaySettings (Part C adds sponsor + pin UI); `apps/display` reads the keys back via `get_display_state.venue.display_config`. |

`venue_get_state` (mig 168) additively gained `display_token` + `display_config`
in its `venue` object (so the operator UI can show the link + prefill config);
`display_pin` was already returned. No consumer break.

### Cycle 3.4 offline queue — NOT shipped as an RPC

The parent Phase 3 plan flagged a `ref_replay_unsynced` batch RPC
for offline reconnect. Cycle 3.4 chose instead to drain the
IndexedDB queue client-side, calling the existing 7 ref_* RPCs one
at a time. Idempotency on `client_event_id` makes this safe; for
the ~30 events in a typical match, the round-trip cost is fine.
A batch RPC can be added later if real-world usage shows it's
needed.

### Phase 5 RPCs (planned, not yet built)

Tracked in `/Users/tarny/.claude/plans/continuing-phase-3-of-steady-falcon.md`.
Migration numbers will be assigned at cycle time.

- `get_competitive_context_for_squad(p_token)` — Cycle 5.1
- (none — reuses existing `get_league_standings_for_player`) — Cycle 5.2
- ~~`get_player_competition_fixtures(p_token, p_filter)` — Cycle 5.3~~ **SHIPPED (mig 155)** — see Phase 5 RPCs (shipped) above
- ~~`get_player_fixture_detail(p_token, p_fixture_id)` — Cycle 5.4~~ **SHIPPED (mig 156)** — see Phase 5 RPCs (shipped) above
- ~~`get_fixture_opposition_intel(p_token, p_fixture_id)` — Cycle 5.4~~ **SHIPPED (mig 156)** — see Phase 5 RPCs (shipped) above
- `player_set_fixture_availability(p_token, p_fixture_id, p_status)` — Cycle 5.5 (+ new `player_availability` table)
- ~~`team_admin_submit_lineup(p_admin_token, p_fixture_id, p_lineup)` — Cycle 5.6~~ **SHIPPED (mig 159)** + `get_team_next_fixture_lineup` (mig 159) + `get_fixture_state_by_ref_token` made lineup-aware (mig 160) — see Phase 5 RPCs (shipped) above
- ~~`team_admin_check_eligibility(p_admin_token, p_fixture_id, p_player_ids)` — Cycle 5.7~~ **SHIPPED (mig 162)** + `team_admin_submit_lineup` rewritten as the authoritative eligibility gate + `league_config.min_starting`/`max_subs` (mig 161). **Phase 5 complete.** — see Phase 5 RPCs (shipped) above

---

## LEAGUE MODE — PHASE 6 RPCs (HQ dashboard, mig 171)

HQ is authenticated-only (Google OAuth, **no token** — scope 6A). The caller is resolved
from `auth.uid()` against `company_admins`; a `platform_admin` (mig 045) is a super_admin
override over any company. Role scoping: super_admin = all company venues; regional_admin =
own region only (`venues.region`, mig 169); analyst = read-only.

| RPC (SQL) | JS wrapper | Grant | Notes |
|---|---|---|---|
| `resolve_company_caller(p_company_id)` | _(internal — used by the hq_* RPCs)_ | authenticated | Mig 171. SECDEF, search_path. `TABLE(company_id, actor_type, role, region)`. auth.uid()→company_admins; platform_admin override → super_admin/no-region. Returns empty (no row) when unauthorized; callers raise `not_authorized`. |
| `company_admin_whoami()` | `companyAdminWhoami()` | authenticated | Mig 171. The HQ app's gate (mirrors `superadmin_whoami`). `{signed_in,user_id,email,is_platform_admin,companies:[{company_id,name,role,region}]}`. Platform admins with no company_admins row see all active companies. **Consumers:** apps/hq App.jsx. |
| `hq_get_company_state(p_company_id)` | `hqGetCompanyState(companyId)` | authenticated | Mig 171; scored health mig 179; **revenue axis mig 182**. SECDEF. `{company, venues:[{health 🟢🟡🔴, health_score /100, health_reason, collection_rate, health_axes{operations,utilisation,fixture_completion,revenue} + tonight/incident/pitch/ref counts}], summary, caller}`. regional_admin scoped to region. Revenue axis = all-time collection-rate % (NULL when owed=0, dropped from the score). **Consumers:** apps/hq VenueHealthGrid (dot + score badge + reason line, wired mig 182) + AlertsActions. `summary` shape also feeds Phase 6.3 analytics Overview + the deferred Phase 9 HQ digest (hard-rule #14). |
| `hq_get_venue_detail(p_company_id, p_venue_id)` | `hqGetVenueDetail(companyId, venueId)` | authenticated | Mig 171. SECDEF. Single-venue drill-down: leagues, open_incidents, fixtures tonight/this-week/recent, pending_registrations, refs. Validates venue∈company + region. **Consumers:** apps/hq VenueDetail. |
| `hq_resolve_incident(p_company_id, p_incident_id, p_resolution_note)` | `hqResolveIncident(companyId, incidentId, note)` | authenticated | Mig 171. **WRITE** (ephemeral-verify gated). analyst rejected (`read_only_role`); region enforced. Sets `resolved_at/by/note`; audits `incident_resolved` (actor_type `company_admin`, team_id=venue_id); `notify_venue_change('incident_resolved')`. **Consumers:** apps/hq VenueDetail resolve button. |

Also mig 171: `audit_events.actor_type` CHECK += `'company_admin'`; `notify_venue_change`
whitelist += `'incident_resolved'`.

**Cycle 6.3 — composable analytics (mig 172–173):**

| RPC (SQL) | JS wrapper | Grant | Notes |
|---|---|---|---|
| `hq_get_analytics(p_company_id, p_date_from DEFAULT NULL, p_date_to DEFAULT NULL)` | `hqGetAnalytics(companyId, from?, to?)` | authenticated | Mig 173; **`revenue` card mig 182**. SECDEF. One read returning all 7 card datasets (`overview`, `venue_comparison`, `top_scorers` [match_events goals], `discipline` [cards], `incidents`, `billing`, `revenue` [owed/collected/outstanding/collection_rate + `by_venue`, from venue_charges/venue_payments, mirrors `venue_get_charges`]) + the caller's saved layout (`config`) + `caller`/`range` meta. Role/region scoped like hq_get_company_state; optional date filter on fixtures + match_events + charge `created_at`. **Classes block mig 345 (Classes+Room-Hire Phase 8):** additive `classes` dataset — `{sessions, avg_fill_pct, class_revenue_pence, room_hire_revenue_pence, busiest_session{class,when,fill_pct}, by_type[{type,sessions,fill_pct,revenue_pence}], instructors[{instructor,sessions,hours}]}`. Fill = held seats (booking `status IN ('confirmed','no_show')` OR `checked_in_at` set) / capacity over non-cancelled sessions. **Double-count guard:** `class_revenue_pence` = Σ(`class`)+Σ(`class_package`) charges (sums CHARGES not sessions, so a pass-funded booking carries no charge → counted once); `by_type.revenue_pence` is `class`-source only (package excluded). Existing `revenue` block (sums all source_types) untouched — classes is a drill-down. **Consumers:** apps/hq AnalyticsView (Classes card, mig 345); the card registry is the Phase 7 AI composition vocabulary. |
| `hq_get_utilisation(p_company_id, p_date_from DEFAULT NULL, p_date_to DEFAULT NULL)` | `hqGetUtilisation(companyId, from?, to?)` | authenticated | Mig 178; **`spaces` block mig 345**. SECDEF, STABLE, search_path locked, `resolve_company_caller`-gated/region-scoped. Pitch-availability utilisation (30-min buckets from `playing_areas.booking_windows` × `pitch_occupancy` fixtures+bookings). **mig 345 adds a COMPANY-level `spaces` block** — `{class_hours, class_sessions, room_hire_hours, room_hires, activity_hours}` from `venue_class_sessions` (scheduled+completed) + `venue_room_hires` (confirmed), clipped to range by start. NOT folded into pitch `used_hours` (venue_spaces have no availability windows — would corrupt the pitch denominator); per-venue activity lives in the analytics `classes` block + `hq_get_class_insights`. **Consumers:** apps/hq UtilisationPanel + AnalyticsView Utilisation card. |
| `hq_get_class_insights(p_company_id)` | `hqGetClassInsights(companyId)` (mig 345) | authenticated (REVOKE anon) | **READ, gated `resolve_company_caller`** (auth.uid→company_admins; regional_admins region-scoped; super_admin/platform_admin all). SECDEF, STABLE, search_path locked, single overload. Classes+Room-Hire **Phase 8** waitlist intelligence over a trailing 90d + upcoming 30d window: `{caller, window, flags[{type,venue,avg_fill_pct,sessions,total_waiting,recommendation}] (class types avg ≥90% full over ≥2 sessions), instructor_utilisation[{instructor,venue,sessions,hours}], revenue_by_type[{type,venue,revenue_pence,charges}] (class-source charges only)}`. Fill = held seats / capacity (same definition as `hq_get_analytics.classes`). EV 15/15 (aggregation + double-count guard) + leak 0, rpc-security-sweep PASS. **Consumers (Hard Rule #14):** apps/hq AnalyticsView Classes card "Waitlist pressure" strip; **Phase-7 Gaffer/AI context source** — any return-shape change must keep the Gaffer briefing builder aligned. |
| `hq_get_analytics_for_company(p_company_id, p_date_from DEFAULT NULL, p_date_to DEFAULT NULL)` | _(none — cron-only; called via service-role `supabase.rpc()` in cron.js)_ | **service-role only** (anon + authenticated + PUBLIC REVOKED) | Mig 190 (Phase 9 HQ weekly digest). SECDEF read. **Service-role sibling of `hq_get_analytics`** for the JWT-less cron: identical analytics jsonb (same 7 sections) but **no `resolve_company_caller` gate, no region scoping (company-wide), no `config`/`caller`/`range` meta** — returns the bare analytics object. Precedent: mig 126 `admin_go_live_for_team`. **Consumer:** `apps/inorout/api/cron.js` `weeklyDigestJob` (hard-rule #14). Any return-shape change must stay aligned with the `_mailer.js` `hqWeeklyDigest` ctx builder. |
| `_hq_health_score(p_ops, p_util, p_completion, p_revenue DEFAULT NULL)` | _(internal — used by hq_get_company_state)_ | n/a (IMMUTABLE, not secdef) | Mig 179 (3 axes); **mig 182 added 4th `p_revenue` axis** (param-count change → old 3-arg DROPped). Weights ops .40 / util .30 / comp .30 / revenue .30; missing axes dropped + renormalised. Returns `{score, weakest}`. |
| `hq_set_dashboard_config(p_company_id, p_config)` | `hqSetDashboardConfig(companyId, config)` | authenticated | Mig 173. **WRITE** (ephemeral-verify gated). Validates shape, filters `cards` to the known 6 keys (order preserved), writes the caller's own `company_admins.dashboard_config`. Personal UI pref (not company data) → no audit. Returns `{ok, persisted, config}` (persisted=false for a platform_admin with no membership row). **Consumers:** apps/hq AnalyticsView edit mode. |

mig 172: `company_admins.dashboard_config jsonb NULL` (per-admin saved layout; NULL = default preset).

**Cycle 6.4 — live activity feed (mig 174):**

| RPC (SQL) | JS wrapper | Grant | Notes |
|---|---|---|---|
| `hq_get_activity(p_company_id)` | `hqGetActivity(companyId)` | authenticated | Mig 174. SECDEF, read-only. `{live:[tonight's fixtures + scores/status], upcoming:[soonest when none today], goals:[recent match_events goals], channels:[per-venue live_channel_key]}`. Role/region scoped. **Consumers:** apps/hq ActivityFeed (centre column) — subscribes to each `venue_live:<key>` channel + 30s poll. |

**Cycle 6.5 — HQ preview token (mig 175):**

| RPC (SQL) | JS wrapper | Grant | Notes |
|---|---|---|---|
| `hq_generate_preview_token(p_company_id)` | `hqGeneratePreviewToken(companyId)` | authenticated | Mig 175. **WRITE** (ephemeral-verify gated). **super_admin only** (regional_admin/analyst → `forbidden_role`). Inserts a 7-day `hq_preview_tokens` row; audits `hq_preview_token_generated`. Returns `{ok, token, expires_at}`. **Consumers:** apps/hq Share-preview button. |
| `get_hq_preview_state(p_token)` | `getHqPreviewState(token)` | **anon** + authenticated | Mig 175. SECDEF read (token is the secret). Validates + expiry (`expired_or_invalid`), stamps `accessed_at` on first open, returns a watermarked read-only company snapshot (company + venue health grid + summary; no drill-down/incidents/tokens). Whole-company (not role-scoped). **Consumers:** apps/hq PreviewView (`/hq/preview/TOKEN`). |

---

## QR ONBOARDING RPCs (migration 248 — slice 1 routing layer)

Generic `invite_links` routing layer. Stable code → mutable destination;
the QR encodes only `/q/<code>`. Plan: `QR_ONBOARDING_SCOPE.md`; design:
DECISIONS.md "QR ONBOARDING ARCHITECTURE".

| RPC | JS wrapper | Grant | Notes |
|---|---|---|---|
| `resolve_invite_link(p_code)` | `resolveInviteLink(code)` | anon + authenticated | **Read-only** (STABLE, SECURITY DEFINER, search_path locked). Resolves a scanned code → `{ok, status, code, action, entity_type, entity_id, destination}`. `status ∈ ok\|inactive\|expired\|exhausted\|not_found`. `destination` is minimal per action (team→`{team_id,team_name}`; venue→`{venue_id,venue_name,logo_url,colours}`; fixture→`{fixture_id}`) — richer data comes from each action's own RPC. NULL destination (target entity gone) → status `not_found`. **Consumers (hard-rule #14)**: `apps/inorout` `InviteResolve.jsx` (`/q/<code>`); slice 3 venue landing; slice 6 check-in. |
| `redeem_invite_link(p_code)` | `redeemInviteLink(code)` | anon + authenticated | **Write** (VOLATILE, SECURITY DEFINER, search_path locked). Atomic `use_count++` under `FOR UPDATE`, re-checks active/expiry/max_uses in-txn (race-safe); refuses invalid without incrementing. INSERTs `audit_events` (`action='invite_link_redeemed'`, `actor_type='system'`, scope `team_id`=entity_id for team/venue; Hard Rule #9). Slice 1 handles team + venue; the **fixture branch raises `checkin_redeem_not_built`** until slice 6 adds it. Call AFTER the action succeeds (post-join), NOT on scan. EV-verified (resolve/redeem/status-paths/audit-trace/fixture-deferred, 6 assertions, leak 0); rpc-security-sweep PASS (secdef, search_path, 1 overload, anon+auth granted, PUBLIC revoked). **Consumers (hard-rule #14)**: slice 2 post-join hook (shipped); slice 3 venue-landing registration (shipped); slice 6 check-in. |
| `venue_ensure_invite_link(p_credential, p_entity_type, p_entity_id, p_action)` | `venueEnsureInviteLink(credential, entityType, entityId, action)` | anon + authenticated | **Write** (VOLATILE, SECURITY DEFINER, search_path locked) — mig 251, slice 4. The "code provider" the venue QR view + reception display read: get-or-creates the ONE canonical active `invite_links` code for an (entity_type, entity_id, action) so a QR can render — idempotent (returns existing or `created:true`). Venue-authed via `resolve_venue_caller`; ownership-checked server-side (`team` must be in one of the venue's competitions → `team_not_in_venue`; `venue` must equal the caller's venue → `not_your_venue`; action↔entity pairing enforced → `action_entity_mismatch`). Supports `team`(join_team) + `venue`(venue_landing); fixture → `unsupported_entity_type` (slice 6). INSERTs `audit_events` (`invite_link_created`) only on create (Hard Rule #9). EV-verified (idempotency + venue create + audit×2 + 5 error paths, leak 0); rpc-security-sweep PASS. **Consumers (hard-rule #14)**: `apps/venue` `InvitesView.jsx`; slice 4b reception display; slice 7 management. |
| `get_display_landing_code(p_display_token)` | `getDisplayLandingCode(displayToken)` | anon + authenticated | **Read-only** (STABLE, SECURITY DEFINER, search_path locked) — mig 252, slice 4b. Resolves the venue from `display_token` and returns its canonical `venue_landing` code + full `/q/<code>` url (`{code, url}`, both NULL if none). The reception display fetches this ONCE on mount to render the "scan to join" QR panel — kept OFF the hot `get_display_state` broadcast payload (rarely changes). Read-only: the display never creates a code (the venue provisions it via `venue_ensure_invite_link`). rpc-security-sweep PASS. **Consumers (hard-rule #14)**: `apps/display` `QRPanel.jsx`. |
| `get_venue_landing(p_venue_id)` | `getVenueLanding(venueId)` | anon + authenticated | **Read-only** (STABLE, SECURITY DEFINER, search_path locked) — mig 249, slice 3. Public "what's on at this venue" for the `/q/<venue_code>` landing. Returns `{ok, status, venue{id,name,logo_url,colours}, competitions:[{competition_id, name, status, type, format, league_code, league_name, teams:[{team_id,name,colours}]}]}`. Only `setup`/`active` competitions under the venue's `active` leagues; teams = `status='active'` (approved) ONLY — never pending or private/casual teams. `league_code` feeds the register-your-team form (`join_register_team`). Venue not found → `{ok:false, status:'not_found'}`. rpc-security-sweep PASS. **Consumers (hard-rule #14)**: `apps/inorout` `VenueLanding.jsx`. |
| `venue_owns_entity(p_venue_id, p_entity_type, p_entity_id)` | _(internal — no JS wrapper)_ | postgres/service_role only | **Internal helper** (STABLE, SECURITY DEFINER, search_path locked) — mig 254, slice 7. Boolean ownership predicate: does `entity` (`venue`/`team`/`fixture`) belong to `venue_id`? team/fixture roll up `competition_teams|fixtures → competitions → seasons → leagues.venue_id`. Called by the four management RPCs below; **explicitly revoked from anon/authenticated** (Supabase default privileges re-grant otherwise) — never client-callable. |
| `venue_list_invite_links(p_credential)` | `venueListInviteLinks(credential)` | anon + authenticated | **Read-only** (STABLE, SECURITY DEFINER, search_path locked) — mig 254, slice 7. Every code the venue owns (its `venue_landing`, its teams' `join_team`, its fixtures' `match_checkin`) via `resolve_venue_caller` + `venue_owns_entity`. Returns `{ok, links:[{code, entity_type, entity_id, action, label, active, use_count, max_uses, expires_at, created_at, target_name}]}` ordered active-first then newest; `target_name` server-derived (venue/team name, or "Home v Away" for fixtures). EV-verified (list-count + cross-venue isolation). **Consumers (hard-rule #14)**: `apps/venue` `InvitesView.jsx`. |
| `venue_create_invite_link(p_credential, p_entity_type, p_entity_id, p_action, p_label)` | `venueCreateInviteLink(credential, entityType, entityId, action, label)` | anon + authenticated | **Write** (VOLATILE, SECURITY DEFINER, search_path locked) — mig 254, slice 7. Mints a **NEW** code unconditionally (vs `venue_ensure_invite_link`'s get-or-create), so staff can run multiple labelled codes per destination. Pairing enforced (`action_entity_mismatch`) + ownership via `venue_owns_entity` (`not_your_entity`). INSERTs `audit_events` (`invite_link_created`, `source:'manage'`; Hard Rule #9). EV-verified + rpc-security-sweep PASS. **Consumers**: `apps/venue` `InviteLinkForm.jsx`. |
| `venue_set_invite_link_active(p_credential, p_code, p_active)` | `venueSetInviteLinkActive(credential, code, active)` | anon + authenticated | **Write** (VOLATILE, SECURITY DEFINER, search_path locked) — mig 254, slice 7. Toggle a code on/off. Ownership re-derived from the code's **stored** entity (never a client-passed venue) → `not_your_entity`; unknown code → `invite_link_not_found`. INSERTs `audit_events` (`invite_link_active_set`; Hard Rule #9). EV-verified + rpc-security-sweep PASS. **Consumers**: `apps/venue` `InvitesView.jsx`. |
| `venue_repoint_invite_link(p_credential, p_code, p_entity_type, p_entity_id, p_action)` | `venueRepointInviteLink(credential, code, entityType, entityId, action)` | anon + authenticated | **Write** (VOLATILE, SECURITY DEFINER, search_path locked) — mig 254, slice 7. Re-points a code to a new destination — **fully flexible**: the new (entity_type, action) may differ from the old, so a code can move across types (team→venue→fixture). **Double ownership check**: caller must own BOTH the existing code's entity AND the new target (`not_your_entity`); new pairing enforced (`action_entity_mismatch`); unknown code → `invite_link_not_found`. INSERTs `audit_events` (`invite_link_repointed`, metadata `{from,to}`; Hard Rule #9). EV-verified (same-type + cross-type repoint + both foreign-ownership paths) + rpc-security-sweep PASS. **Consumers**: `apps/venue` `InviteLinkForm.jsx`. |

---

## MIGRATION FILE MAP

| Migration | Contents |
|---|---|
| 254 | `venue_owns_entity` (internal helper) + `venue_create_invite_link` + `venue_set_invite_link_active` + `venue_repoint_invite_link` (writes) + `venue_list_invite_links` (read) — QR slice 7 link management. All via `resolve_venue_caller` + the shared `venue_owns_entity` ownership predicate; re-point is fully flexible (cross-type). Writes INSERT `audit_events`. Helper revoked from anon/authenticated (internal-only); four RPCs anon+authenticated, PUBLIC revoked. EV-verified (15 assertions incl. cross-venue isolation + 4 foreign-ownership rejections, leak 0) + rpc-security-sweep PASS. See QR ONBOARDING RPCs above + `QR_ONBOARDING_SCOPE.md`. |
| 252 | `get_display_landing_code` (read) — QR slice 4b. Resolves venue from display_token → its canonical venue_landing code + /q/ url, for the reception display's "scan to join" QR panel (off the hot get_display_state path). STABLE SECURITY DEFINER, search_path locked, anon+authenticated, PUBLIC revoked. See QR ONBOARDING RPCs above. |
| 251 | `venue_ensure_invite_link` (write) — QR slice 4 "code provider" (get-or-create canonical QR code per entity+action, venue-authed, ownership-checked). VOLATILE SECURITY DEFINER, search_path locked, anon+authenticated, PUBLIC revoked. EV-verified + rpc-security-sweep PASS. See QR ONBOARDING RPCs above + `QR_ONBOARDING_SCOPE.md`. |
| 250 | `venue_get_state` REPLACE — QR slice 3b approval-card enrichment. `v_pending` CTE gains `captain_email` (teams.admin_email) + `competition_name` (JOIN competitions); `to_jsonb(p)` auto-includes them in `pending_registrations` (the ONLY change vs mig-227 body — pulled live body first). Lets the venue judge a self-serve registration (was team-name only). Read-only → no EV mandate, but EV-verified the enrichment surfaces + leak 0; security intact (secdef, search_path, overload 1, anon+auth). Consumer: apps/venue `Operations.jsx` approval card. |
| 249 | `get_venue_landing` (read) — QR Onboarding slice 3 venue landing page ("what's on at this venue" + register-your-team). STABLE SECURITY DEFINER, search_path locked, anon+authenticated, PUBLIC revoked. Public/open competitions + approved teams only. rpc-security-sweep PASS. See QR ONBOARDING RPCs above + `QR_ONBOARDING_SCOPE.md`. |
| 248 | `invite_links` table + `resolve_invite_link` (read) + `redeem_invite_link` (write) — QR Onboarding slice 1 routing layer. RLS enabled, no client policies; both SECURITY DEFINER, search_path locked, anon+authenticated, PUBLIC revoked. EV-verified + rpc-security-sweep PASS + casual-regression PASS (additive route only). See QR ONBOARDING RPCs above + `QR_ONBOARDING_SCOPE.md`. |
| 006 | RLS enable on all 19 tables |
| 007 | RLS team-scoped table policies |
| 008 | RLS financial/audit table policies |
| 010 | `get_team_state_by_player_token` + `get_team_state_by_admin_token` bulk RPCs |
| 011 | Player token RPCs: set_player_status, set_player_paid, set_player_injured, add_guest_player, save_push_subscription |
| 012 | Admin token RPCs: all admin write operations |
| 013 | Admin match/schedule RPCs |
| 014 | Admin payment RPCs |
| 015 | Onboarding: create_team |
| 016 | POTM: submit_potm_vote, get_potm_voting_state |
| 017+ | Incremental additions |
| 022 | link_player_to_user (authenticated only) |
| 026 | is_vice_captain migrated to team_players; players_public view updated |
| 027 | team_switches jsonb on matches |
| 028 | player_join_team (authenticated only) |
| 029 | price_per_player numeric(10,2) |
| 030 | drop create_team int variant |
| 031 | Group Balancer: team_players.group_number, settings.group_labels, matches.predicted_winner / predicted_confidence / balance_score; new RPCs admin_set_player_group + admin_clear_all_groups; admin_save_teams + admin_upsert_settings extended (old signatures dropped) |
| 032 | admin_reopen_week — fixes the cancel-then-relive toggle gap (admin_upsert_schedule writes neither is_cancelled nor active_match_id). Single RPC owns the clear-cancelled + fresh-matches-row + active_match_id update + audit_events insert. |
| 033 | ai_briefings table — Gaffer AI agent audit log; jsonb context_snapshot per row makes every LLM claim traceable. RLS: admins read own team admin rows, players read own player rows, writes via service role only. |
| 034 | gaffer_get_context_team_summary — Phase 1 surface RPC. |
| 035 | gaffer_get_context_payment_summary — Phase 1 surface RPC. |
| 036 | gaffer_get_context_attendance_risk — Phase 1 surface RPC. |
| 037 | gaffer_get_context_matchday_briefing — Phase 1 surface RPC. |
| 038 | players.admin_locked_in column + REPLACES admin_set_player_status (lock + cap), set_player_status (lock + cap), get_team_state_by_admin_token (includes admin_locked_in in squad rows). |
| 039 | `get_my_payment_history(p_token, p_limit)` + `get_my_injuries(p_token)` — Player-token-authed reads for the new player-facing PlayerProfile screen. Both SECURITY DEFINER; derive (player_id, team_id) from players.token via team_players join. GRANT to anon+authenticated. |
| 040 | `leave_squad(p_token)` + `delete_my_account(p_token)` — Player-token-authed destructive RPCs (self-leave + self-delete). leave_squad detaches team_players + push_subscriptions only; refuses with debt_owed. delete_my_account anonymises players row + detaches all teams; refuses with last_admin guard; returns auth_user_id for edge function follow-up. |
| 041 | `get_head_to_head_raw_by_admin_token(p_admin_token, p_me_id, p_them_id, p_period)` — SECURITY DEFINER raw-data RPC for H2H. Returns all-time matches, period-filtered matches, player_match rows for both players. Fixes anon /demoadmin direct-read RLS block. Anon grant (admin_token is the auth signal). |
| 042 | `get_player_league_table_raw_by_admin_token(p_admin_token, p_period)` — SECURITY DEFINER raw-data RPC for PlayerLeagueTable. Returns period matches, player_match rows, all-time attended summaries, all match dates (reliability denominator), player details. Same pattern as 041 — fixes anon /demoadmin StatsView (form + reliability columns) and H2H Section 4 (comparison bars). |
| 043 | admin_save_teams REPLACE — now also writes `players.team` ('A'/'B'/NULL scoped to team) when p_confirm=true so PlayerView Live Board renders the per-player team sheet. Previously only matches.team_a/team_b was written, leaving p.team stale. |
| 044 | `player_join_team` REPLACE — generates a player token on the new-player INSERT branch (was missing → first-time joiners landed with NULL token → JoinSuccess.jsx fell back to `/`). Pre-Beta launch blocker fixed before the invite link went out. |
| 045 | `platform_admins` table (global cross-team authorisation, separate from per-team team_admins) + `is_platform_admin()` helper + `superadmin_whoami()` RPC. Seeded with developer's auth uid. Foundation for the new apps/superadmin dashboard. |
| 046 | Superadmin read RPCs: `superadmin_list_teams()`, `superadmin_team_detail(p_team_id)`, `superadmin_recent_activity(p_limit, p_since)`. All gated by is_platform_admin(), all SECURITY DEFINER + STABLE, all return jsonb. Power the Activity / Teams / Team Detail tabs of the superadmin dashboard. |
| 047 | `delete_my_account(p_token)` REPLACE — purges FK refs to auth.users from team_admins, platform_admins.granted_by, user_profiles so auth.admin.deleteUser() succeeds. Pre-fix: SQL succeeded but auth row remained, blocking re-sign-in with the same email. Session 37. |
| 048 | `admin_save_teams` REPLACE — adds team_players scope to the two `UPDATE players SET team='A'/'B'` statements (the CLEAR was already scoped). Closes the cross-team write surface flagged in the pre-Beta audit. Foreign player_ids silently update 0 rows. Verified with adversarial + happy-path tests against live DB inside rolled-back transactions. |
| 049 | `notify_team_change` REPLACE — adds `player_account_deleted` to v_known_reasons whitelist (migration 047 passed this reason; warning was log-only, broadcast worked). Bonus diagnostic comment block in the file documenting the apex→www cron URL gotcha. |
| 155 | `get_player_competition_fixtures(p_token, p_filter)` — League Mode Phase 5 Cycle 5.3. Read-only player-facing competition fixtures (upcoming/past/all). See Phase 5 RPCs (shipped) table above. |
| 156 | `get_player_fixture_detail(p_token, p_fixture_id)` + `get_fixture_opposition_intel(p_token, p_fixture_id)` — League Mode Phase 5 Cycle 5.4. Read-only player-facing fixture detail + opposition intel, gated to the player's own competitions. See Phase 5 RPCs (shipped) table above. |
| 157 | `reset_team_status_on_fixture_played()` trigger fn + `trg_reset_status_on_fixture_played` AFTER UPDATE ON `fixtures` — League Mode Phase 5 Cycle 5.5. NOT an RPC. On `scheduled → completed/walkover/forfeit/void`, resets both teams' `players.status` to 'none' (+ `notify_team_change(...,'schedule_updated')`) so competitive availability (reusing the casual in/out board) starts fresh each game. SECURITY DEFINER, search_path locked. No new availability table/RPC — `set_player_status` (mig 011) is reused. |
| 158 | `join_register_team` REPLACE — League Mode session 55. A casual `existing_team_id` is rejected (`casual_team_cannot_register`); no in-place casual→competitive promotion. Closes the global-status dual-context must-fix structurally (a league team is always a separate squad). Also revoked stale anon EXECUTE grant. See Phase 2 registration table above. |
| 159 | `fixture_lineups` table + `team_admin_submit_lineup` + `get_team_next_fixture_lineup` — League Mode Phase 5 Cycle 5.6 Stage A. Teamsheet submission (submit registers picked players) + admin next-fixture read. See Phase 5 RPCs (shipped) table above. |
| 160 | `get_fixture_state_by_ref_token` REPLACE (lineup-aware) + internal helper `_fixture_squad_json` — League Mode Phase 5 Cycle 5.6 Stage B. Ref pre-match shows the submitted XI+bench when present (additive `lineup_role`), else the full registered squad unchanged. Backward compatible. See Phase 5 RPCs (shipped) table above. |
| 161 | `league_config.min_starting` + `max_subs` (nullable int, NULL = unbounded; CHECK >0 / >=0) — League Mode Phase 5 Cycle 5.7 Stage A part 1. Per-league matchday squad-size bounds for teamsheet enforcement. Additive (no backfill); flows through `get_league_config` via `to_jsonb(*)`. |
| 162 | `team_admin_check_eligibility` (read) + `team_admin_submit_lineup` DROP/REPLACE (authoritative eligibility gate: squad-size, double-reg block + audit, suspended override) + `get_team_next_fixture_lineup` REPLACE (VC dual-lookup) — League Mode Phase 5 Cycle 5.7 Stage A part 2. **Phase 5 complete.** See Phase 5 RPCs (shipped) table above. |
| 164 | `venues.display_token` (UNIQUE, auto-gen default) + `venues.display_config` jsonb + 8 companion read indexes — League Mode **Phase 4** reception display schema. Additive. |
| 165 | `get_display_state(p_display_token)` — venue-scoped read for the reception big-screen (confirmed + live standings, top scorers, live fixtures, upcoming/recent, goals ticker). See Phase 4 RPCs above. |
| 166 | `check_display_pin(p_display_token, p_pin)` — read-only PIN check (PIN stays server-side; client owns lockout). |
| 167 | `venue_update_display_config(p_venue_token, p_config, p_display_pin)` — operator write for display panel config + PIN. ephemeral-verify PASS. |
| 168 | `venue_get_state` REPLACE — venue object additively gains `display_token` + `display_config` for the apps/venue settings editor. |
| 169 | `venues.region text NULL` — regional_admin scoping for the HQ dashboard (Phase 6.1). Additive. |
| 170 | Demo company seed `company_demo` (Demo Sports Group) — links demo_venue (North) + venue_demo_south (South), tarny super_admin, 2 open incidents. Namespaced + reversible (`170_down`). |
| 171 | Phase 6 HQ RPCs — `resolve_company_caller`, `company_admin_whoami`, `hq_get_company_state`, `hq_get_venue_detail`, `hq_resolve_incident` (write). + `audit_events.actor_type`+='company_admin' + `notify_venue_change` whitelist+='incident_resolved'. rpc-security-sweep + ephemeral-verify PASS. |
| 172 | `company_admins.dashboard_config jsonb NULL` — per-admin HQ dashboard layout (Phase 6.3). |
| 173 | Phase 6.3 composable analytics — `hq_get_analytics` (read, 6 card datasets + caller layout) + `hq_set_dashboard_config` (write). rpc-security-sweep + ephemeral-verify PASS. |
| 174 | Phase 6.4 live activity feed — `hq_get_activity` (read; tonight/upcoming fixtures + goals ticker + venue channel keys). rpc-security-sweep PASS. |
| 175 | Phase 6.5 HQ preview token — `hq_generate_preview_token` (write, super_admin) + `get_hq_preview_state` (anon read, watermarked snapshot + accessed_at stamp). rpc-security-sweep + ephemeral-verify PASS. |
| 163 | `notification_log` + `channel`/`entity_id`/`recipient` (nullable) + partial email-dedup index — League Mode **Phase 9 Cycle 9.1**. Lets transactional email (Resend) be logged/deduped alongside web-push. No RPC change — the email layer (`api/_mailer.js` + `onboardingEmailJob` in `api/cron.js`) is a read-only consumer of existing audit actions (`team_registration_submitted`/`team_approved`/`team_rejected`/`fixture_ref_assigned`). |
| 204 | (session 68) `admin_go_live` + `admin_go_live_for_team` REPLACE — on **new-match creation** reset the squad board: status='none', admin_locked_in=false, team=NULL for all team_players. Payment fields carry over. Fixes week-rollover "teams stay locked, can't say in/out" (carried status='in' made the squad read full → set_player_status squad_full). |
| 205 | (session 68) `admin_save_match_result` REPLACE — freshness now keyed on `matches.winner` (NULL until first finalisation; the kickoff lineup-lock pre-creates player_match and defeated the old row-count check, silently skipping the whole cascade). Adds payment_ledger game_fee/unpaid charge per unpaid non-guest attendee + bib_history cascade. Fixes £0-outstanding, empty payment history, dead admin Bib tracker. (Applied 205→205b; 205b qualified latent ambiguous p.attended/p.goals exposed once the block ran.) |
| 206 | (session 68) `admin_save_match_result` REPLACE — also clears `admin_locked_in` in the fresh-save reset, closing the cross-week stuck-lock gap on the idempotent go-live reuse path. |
| 207 | (session 70) `admin_go_live` + `admin_go_live_for_team` REPLACE — on **new-match creation**, delete all is_guest=true rows for the team (team_players first, then players) before the status reset. Fixes stale guest row blocking the Plus One button each week. Gated to new-match path only. |
| 219 | (session 72, PERSISTENT GUESTS S5) `submit_potm_vote` + `get_potm_tally` + `admin_close_potm_voting` REPLACE — guests are not POTM-eligible until promoted (decision 2). submit rejects a guest nominee (`nominee_not_eligible`); tally drops guest nominees; close refuses a guest winner (`winner_not_eligible`). Keyed on live `players.is_guest` so a promoted guest is eligible automatically. `get_potm_voting_state` already excluded guests from the nominee list (`pm.is_guest=false`) — unchanged. Also (JS) `deeperIntel.reliabilityRanking` now filters guests. Bodies preserved byte-for-byte + guards; search_path/grants unchanged. |
| 218 | (session 72, PERSISTENT GUESTS S3) NEW `admin_promote_guest(p_admin_token, p_guest_id)` (admin "make permanent" — flip is_guest=false, keep history; resolve_admin_caller, anon+authenticated) + `link_player_to_user` REPLACE — adds a GATED promote-on-link branch (if the linked row is_guest=true, also flip is_guest=false/guest_of=NULL): a guest who signs in via their own token link is promoted+linked (token = identity, no name-matching); regulars untouched. Both routes = same row, history carries over. Additive (no row mutation on apply). Consumers: SquadScreen "Make permanent" + guest copy-link; App.jsx auto-link on sign-in. |
| 217 | (session 72, PERSISTENT GUESTS S2) NEW `reactivate_guest_player(p_token, p_guest_id)` — host brings back a dormant team guest (re-attach `guest_of`, `status='in'`, fresh payment baseline, keep stats+history). SECDEF, search_path pinned, anon+authenticated. Additive (no row mutation on apply). Consumer: PlayerView Plus One "Bringing someone back?" picker (`reactivateGuestPlayer` wrapper). |
| 216 | (session 72, PERSISTENT GUESTS S1) `admin_go_live` + `admin_go_live_for_team` + `remove_guest_player` REPLACE — **reverse the guest-DELETE portions of migs 207/209.** Guests are no longer deleted on rollover or on host-remove; they go DORMANT (status='none', team=NULL, admin_locked_in=false) with player_match history kept. The go-live RPCs simply drop their guest-delete + child-cleanup blocks (the mig-204 bulk reset already makes guests dormant); `remove_guest_player`'s DELETE chain → a single dormant UPDATE. PURE function-redefinition, no row mutation. JS hides dormant guests via `isDormantGuest(p)` and keys PlayerView's Plus One on an ACTIVE guest. |
| 243 | (session 82) `admin_go_live` + `admin_go_live_for_team` REPLACE — on **new-match creation** the squad reset now also clears the per-game payment flags (`paid=false, self_paid=false, paid_by=NULL, paid_at=NULL`); `owes` deliberately left untouched. Fixes user-reported "My View still shows ✓ Paid after a new game opens" (and the admin Payments PAID-UP section showing last week's payers). Ephemeral-verified both entry points incl. owes-preservation; leak-check clean. |
| 230 | (session 76) NEW `notify_spot_opened()` trigger fn + `players_spot_opened_notify` AFTER UPDATE OF status, disabled ON `players` — **NOT a client RPC.** Reliable server-side "a spot's opened — claim it" reserve notification. Fires on any spot-freeing transition (`'in'→not 'in'`, or `disabled` flips true while `'in'`), recomputes in-count vs `schedule.squad_size`, and on a genuine opening `net.http_post`s to `/api/notify` **direct mode** (no auth; canonical **www** URL per mig 049) for the **next reserve only** (lowest `reserve_priority_order`). notify.js does all gating (trigger config/quiet-hours/injured/log). SECURITY DEFINER, search_path pinned, exception-swallowing (never breaks the player write — mig-225 pattern), anon/authenticated EXECUTE revoked. Replaces the fragile client-only `spotOpened` push (removed from PlayerView). Anti-spam: `admin_go_live`/`_for_team` set `inorout.bulk_reset` GUC before the weekly mass reset so the trigger skips it. No auto-promotion (tap-to-claim). |
| 241 | (session 80) `admin_save_match_result` + `set_player_status` REPLACE — post-game lifecycle hardening. **admin_save_match_result** fresh block now (1) closes the game (`schedule.game_is_live=false`), (2) resets EVERY squad status → 'none' (reserves included, blanket pass — attendee-only reset left them stuck), (3) preserves `paid=true` for players whose game_fee ledger for that match is already `paid` (was an unconditional wipe diverging My View from the ledger). **set_player_status** gains a server-side sign-up-window gate: raises `game_not_live` unless the active schedule is `game_is_live=true AND NOT is_cancelled` — the client gate was the only protection before. Both SECURITY DEFINER, grants intact (anon+authenticated). Ephemeral-verified 8/8 + leak-check 0; rpc-security-sweep PASS. Live mitigation for Footy Tuesdays (game closed, statuses reset) applied same session before the migration generalised it. See [[project_result_save_invariants]]. |
| 242 | (session 81) `get_potm_tally_public` NEW — running POTM tally for players, **revealed only after you vote**. Counts only (`{voted, tally:[{nominee_id, votes}], total_votes}`), never voter identities — does NOT widen the latent `get_potm_voting_state` voter_id+nominee_id leak. Server gate: no `potm_votes` row for the caller → `{voted:false}`, no counts. Match-wide; guests excluded (mirrors mig 219). STABLE SECURITY DEFINER, search_path set, anon+authenticated granted, single overload. Read-only → no ephemeral-verify (Hard Rule #15 scope is writes); verified live on a real squad (voter=6 votes winner-first, non-voter=`{voted:false}`, wrong-team=`match_not_found`). rpc-security-sweep PASS. Consumer: `POTMVotingModal.jsx` voted-state leaderboard, live-refreshed via the `team_live` broadcast. |
| 244 | (session 83, Reception Display A1) `get_display_state` REPLACE — broadcast-redesign enrichment, additive only. `upcoming_fixtures[]` rows gain `competition_type`/`round_name`/`official_id`/`official_name` (LEFT JOIN `match_officials`); NEW top-level `bookings[]` (today's confirmed casual `pitch_bookings` backed by active `pitch_occupancy`, `superseded_at IS NULL`); `top_scorers[]` rows gain `apps` (player id in completed fixtures' `fixture_lineups.starting/bench` jsonb arrays — 0 until lineups used) + `shirt_number`. No `position` key (players has no source column). Featured-pin keys pass through `display_config` untouched (persisted by mig 245). Base body = mig 221, applied byte-identical to source (md5-verified). Read-only → no ephemeral-verify; live-verified on demo_venue (2 booking rows, apps/shirt on scorers) + officials join sampled. rpc-security-sweep PASS (31 pre-existing PUBLIC-grant fns noted as baseline, none touched here). Consumers: `apps/display` broadcast redesign (Part B); Phase 6 HQ + Phase 7 Gaffer (future). |
| 247 | (session 83, Reception Display follow-up) `get_display_state` REPLACE — `live_fixtures[]` rows gain `round_name`/`official_id`/`official_name` (LEFT JOIN `match_officials`), feeding the hero bar "R6 · Main Pitch · Ref Cooper" line (mig 244 only enriched upcoming). Applied via a guarded in-place rewrite of the live body (mig-239 pattern: unique-anchor replace + already-applied no-op); full source in the 247 file, live body md5-verified ≡ file. Additive; Hero.jsx already read the keys optimistically — no JS change. |
| 245 | (session 83, Reception Display A2) `venue_update_display_config` REPLACE — sponsor creative + featured-match pin keys. Base body = LIVE function (167 + the mig-239 `manage_facility` guard that the 167 file lacks — guard was injected live by 239's pg_get_functiondef rewrite). Adds validation: sponsor/story-tag text fields string-or-null (`config_field_invalid`), `sponsor_ratio` clamped 0..1 (`sponsor_ratio_invalid`), `featured_fixture_id` must be a uuid in THIS venue's competitions (`featured_fixture_invalid`/`fixture_not_in_venue`), `featured_pin_expires_at` timestamptz (`featured_expiry_invalid`). PIN/audit/notify unchanged; signature unchanged → grants preserved. Applied byte-identical to source (md5). Ephemeral-verify PASS (11 assertions: happy-path persist, clamp 1.7→1/−0.3→0, 6 error paths, null unpin + PIN set, 4/4 audit rows; `_e2e_` leak-check 0). |
| 246 | (session 83, Reception Display A3) Storage bucket `venue-media` — public read, 5 MB, image mimes only; write policies (insert/update/delete) for `authenticated` venue staff scoped to a `<venue_id>/...` object path via active `venue_admins` membership. No RPC; the venue app uploads via the JS storage client and persists the public URL through `venue_update_display_config.sponsor_image_url`. Legacy shared-token venues (no authenticated staff) cannot upload until they get logins — known limitation. Verified live (bucket + 3 policies). |

**Note:** Migrations 013–016 headers say "DO NOT EXECUTE" — stale from Phase B design phase.
All were deployed in Phase C via Supabase SQL editor.

---

## SUPABASE SCHEMA CACHE

PostgREST caches function signatures. After any RPC change, cache may serve stale version.

Symptoms: 404 on a function that exists, wrong parameter order error.

Fix: `SELECT pg_notify('pgrst', 'reload schema');` in Supabase SQL editor.

---

## PITCH BOOKING RPCs (migrations 133+)

Stage 3 read RPCs (booking-owned). Write RPCs (`book_pitch_*`,
`venue_confirm_booking`, etc.) land in Stage 4; JS wrappers land with the
casual/venue UI (Stages 5/6).

| SQL function | JS wrapper | Grant | Notes |
|---|---|---|---|
| `search_bookable_venues(p_query)` | `searchBookableVenues(q)` | anon, authenticated | Typeahead over `venues` WHERE `bookings_enabled`+`active`, name/slug/city ILIKE, LIMIT 20. Returns `[{venue_id,name,slug,city,cancellation_policy}]` (policy added mig 149). PII-free. *Consumers: `apps/inorout` ScheduleScreen booking modal.* |
| `get_team_bookings(p_team_id)` | `getTeamBookings(teamId)` | **authenticated** | Casual read of the team's own bookings (recent + future) with venue/pitch/policy detail. Auth via `auth.uid()` → `team_admins`. Powers the in-app bookings list, live Requested→Confirmed badge, recent-venues discovery, cancel. mig 148. *Consumer: `apps/inorout` ScheduleScreen (Stage 5).* |
| `get_pitch_free_slots(p_venue_id, p_date, p_playing_area_id?, p_slot_length?)` | `getPitchFreeSlots(...)` (Stage 5) | anon, authenticated | Expands `playing_areas.booking_windows` for `p_date`'s weekday → back-to-back slots per offered length → subtracts active `pitch_occupancy`. Graceful default 08:00–22:00/60 when a pitch has no windows. Returns `[{playing_area_id,pitch_name,slot_start,slot_end,slot_minutes}]`. PII-free. *Consumers: `apps/inorout` booking modal (Stage 5).* |
| `get_pitch_occupancy(p_venue_token, p_from, p_to)` | `getPitchOccupancy(...)` (Stage 6) | anon, authenticated (token is the secret) | Venue calendar grid: one row per active occupancy row joined to fixture/booking/maintenance detail over `[p_from, p_to]`. **Returns PII** (team names, walk-in names) → venue-operator only via `resolve_venue_caller`. detail carries `kind`/`status`/`series_id` and (mig 233) **`owed`** (any non-refunded charge for the source has an outstanding balance — drives green=paid / amber=owed) + **`is_first`** (booker's first-ever booking → NEW badge; bookings only). owed math via `_venue_source_owed(source_type, source_id)`. *Consumers: `apps/venue` ScheduleGrid (colour=payment, type word, NEW badge; hard-rule #14).* |

Stage 4 write RPCs (booking-owned). Each: SECURITY DEFINER, holds/frees a
`pitch_occupancy` row through the partial EXCLUDE (taken slot → `slot_unavailable`),
audits (Phase 2 shape), broadcasts `notify_venue_change` + `notify_team_change`.
JS wrappers land with the consuming UI (Stages 5/6).

| SQL function | JS wrapper | Grant | Notes |
|---|---|---|---|
| `book_pitch_adhoc(p_team_id, p_playing_area_id, p_booking_date, p_kickoff_time, p_slot_minutes?)` | `bookPitchAdhoc(...)` (Stage 5) | **authenticated** | Casual one-off request. Auth via `auth.uid()` → `team_admins` (must administer `p_team_id`). Status `requested`, occupancy priority 3. Fires `booking_requested`. *Consumer: `apps/inorout` booking modal (Stage 5).* |
| `book_pitch_series(p_team_id, p_playing_area_id, p_kickoff_time, p_start_date, p_weeks, p_slot_minutes?)` | `bookPitchSeries(...)` (Stage 5) | **authenticated** | Casual block request. Creates `booking_series` + N weekly `pitch_bookings` (priority 2) atomically (any clash → `slot_unavailable`). `day_of_week` derived from `p_start_date`. Fires `booking_requested`. *Consumer: `apps/inorout` (Stage 5).* |
| `venue_create_booking(p_venue_token, p_playing_area_id, p_booking_date, p_kickoff_time, p_slot_minutes?, p_team_id?, p_booked_by_name?, p_contact_email?, p_contact_phone?)` | `venueCreateBooking(...)` (Stage 6) | anon, authenticated | Single booking → status `confirmed` directly. Team (`team_id`) OR walk-in/new (`team_id` NULL + `booked_by_name`). **Contact email + phone now REQUIRED** (mig 232 — validated via `_validate_booking_contact`; stored on `pitch_bookings.contact_email/contact_phone`). Old 7-arg signature DROPPED in mig 232. Fires `booking_confirmed`. *Consumer: `apps/venue` New-booking modal.* |
| `venue_create_booking_series(p_venue_token, p_playing_area_id, p_kickoff_time, p_start_date, p_weeks, p_team_id, p_slot_minutes?, p_contact_email?, p_contact_phone?)` | `venueCreateBookingSeries(...)` | anon, authenticated | **WRITE** (mig 232, ephemeral-verify 15/15). Venue-side weekly block: N `pitch_bookings` (kind `block`, status `confirmed`, priority-2 occupancy) under one `booking_series`, mirroring `book_pitch_series` but venue-token auth + confirmed. **Team-only** (`series_team_required` if no team); contact required; whole block rolls back on any week clash (`slot_unavailable`). Fires `booking_confirmed` (venue+team). *Consumer: `apps/venue` New-booking modal (Block).* |
| `venue_confirm_booking(p_venue_token, p_booking_id)` | `venueConfirmBooking(...)` (Stage 6; +member discount mig 277) | anon, authenticated | `requested` → `confirmed` (refuses if not pending — e.g. superseded by a fixture). Fires `booking_confirmed`. **mig 277:** applies the booker's active membership tier `discount_pct` to the charge. Member resolved by `pitch_bookings.customer_id` if set, else auto-matched by `contact_email` to an ACTIVE venue member (link then persisted). Only an active membership w/ numeric pct discounts; 100% → no charge. Returns now include `member_discount_pct`, `charge_fee_pence`; audit gains `base_fee_pence`/`member_discount_pct`/`member_customer_id`. EV 9/9 + leak 0. *Consumer: `apps/venue` requests inbox.* |
| `venue_confirm_booking_series(p_venue_token, p_series_id)` | `venueConfirmBookingSeries(...)` (mig 236; +member discount mig 277) | anon, authenticated | Confirms every still-`requested` booking in a series + mints a charge each. **mig 277:** same per-booking member discount as `venue_confirm_booking`. *Consumer: `apps/venue` requests inbox.* |
| `_booking_member_discount(p_venue_id, p_customer_id, p_email)` | — (internal helper, mig 277) | **service_role ONLY** | Returns `(customer_id, pct)` — resolves a booking's member (explicit link, else active-member email match) + clamped tier `discount_pct`. Called only inside the SECDEF confirm RPCs. |
| `venue_decline_booking(p_venue_token, p_booking_id)` | `venueDeclineBooking(...)` (Stage 6) | anon, authenticated | `requested` → `declined`, frees the slot. Fires `booking_declined`. *Consumer: `apps/venue` inbox (Stage 6).* |
| `cancel_booking(p_booking_id, p_venue_token?, p_reason?, p_note?, p_decision?, p_within_policy?)` | `cancelBooking(id, token?, opts?)` (Stage 5/6; mig 222) | anon, authenticated | Dual auth: venue token OR team admin (`auth.uid()`). Walk-ins venue-only. `requested`/`confirmed` → `cancelled`, frees slot. **mig 222:** `p_decision` `full`/`partial`/`none` acts on the booking's `venue_charges` (full→refunded, partial→due halved, none→untouched, mirrors fixture-void); reason/note/decision/refund_pence/charged_pence recorded in the `booking_cancelled` audit row; `p_within_policy` recorded only. Old 2-arg signature dropped. Returns `{ok, status, refund_pence, charged_pence}`. *Consumers: both apps; venue CancelBookingModal.* |
| `venue_list_cancellations(p_venue_token, p_limit?)` | `venueListCancellations(token, limit?)` (mig 222) | anon, authenticated | Venue read. `booking_cancelled` audit rows for this venue, newest first, joined to live pitch/team detail (reason/note/decision/refund). Rows cancelled pre-mig-222 have null detail. *Consumers: venue CancellationsLog + CSV export.* |
| `venue_list_customers(p_venue_token)` | `venueListCustomers(token)` (mig 223) | anon, authenticated | Venue read, **venue-domain only**. Bookers at this venue (teams grouped by `team_id`, walk-ins by `booked_by_name`) aggregated from `pitch_bookings` + `venue_charges` (+ `venue_payments` for collected): bookings_count, total_paid/outstanding pence, recency-based `nudge_status` (new/healthy/lapsing/dormant). No casual `players`/`team_admins`/ins read — respects the venue↔casual RLS wall. *Consumers: venue CustomersView.* |
| `venue_request_nudge(p_venue_token, p_booker_key, p_template?)` | `venueRequestNudge(token, bookerKey, template?)` (mig 224) | anon, authenticated | Fire-and-forget write. Records a `venue_nudge_requested` audit_event for a **team** booker at this venue (authorizes via a booking match). Returns `{ok, recipients}` (count only — no contact exposed) or `{ok:false, reason:'no_contact'}` (walk-ins / no admin email). **Delivery is server-side:** `cron.js onboardingEmailJob` resolves the team admin email (`teamAdminEmails`) and sends the `venue_nudge` mailer template — the venue never sees the address. Email live; SMS/WhatsApp no-op till Twilio keys. *Consumers: venue CustomersView NudgeModal; sender = apps/inorout/api/cron.js.* |
| `venue_get_booking_ins(p_venue_token)` | `venueGetBookingIns(token)` (mig 225) | anon, authenticated | Venue read, **cross-domain by opt-in** (counts only). Map of `booking_id → {team_id, in_count, target}` for upcoming team bookings (next 14d): `in_count` = team players IN & not disabled; `target` = the team's active `schedule.squad_size`. No player identities cross to the venue. *Consumers: venue ScheduleGrid/DayAgenda/BookingDetailModal.* |
| `venue_create_customer(p_venue_token, p_first_name, p_last_name?, p_email?, p_phone?, p_dob?, p_household_id?, p_consent_marketing?)` | `venueCreateCustomer(token, {firstName,...})` (mig 270) | anon, authenticated | **WRITE, gated `manage_memberships`** (Membership Phase 2, EV 10/10). Creates a `venue_customers` person. De-dups on `(venue_id, lower(email))` → raises `customer_exists` (existing id in error DETAIL); `first_name_required` on blank. Sets `consent_at` when consent given. Audit `venue_customer_created` (flags only, no PII). *Forward consumers (Hard Rule #14): Phase 3 enrolment, Phase 4 venue Customers/Memberships UI, Phase 5 member pass.* |
| `venue_update_customer(p_venue_token, p_customer_id, p_first_name?, p_last_name?, p_email?, p_phone?, p_dob?, p_household_id?, p_consent_marketing?, p_notes?)` | `venueUpdateCustomer(token, customerId, {...})` (mig 270) | anon, authenticated | **WRITE, gated `manage_memberships`**. Partial update (NULL arg = field unchanged). Re-checks email de-dup on change (`customer_exists`); `customer_not_found` if missing/erased. Audit `venue_customer_updated`. *Forward consumers: Phase 4 UI.* |
| `venue_erase_customer(p_venue_token, p_customer_id)` | `venueEraseCustomer(token, customerId)` (mig 270) | anon, authenticated | **WRITE, gated `manage_memberships`** — GDPR right-to-erasure. Scrubs all PII, KEEPS the row (`status='erased'`, `first_name='[erased]'`) so membership/charge history stays referentially intact. `customer_not_found` if already erased. Audit `venue_customer_erased`. *Forward consumers: Phase 4 UI.* |
| `venue_list_customers_people(p_venue_token, p_include_erased?)` | `venueListCustomersPeople(token, includeErased?)` (mig 270) | anon, authenticated | **READ** (any venue member). The **people** directory — DISTINCT from `venue_list_customers` (booking-derived). Returns `{ok, customers[]}` (excludes erased unless `p_include_erased`). *Forward consumers: Phase 4 venue CustomersView, Phase 6 churn intelligence.* |
| `venue_create_membership_tier(p_venue_token, p_name, p_benefits?, p_prices?, p_audience?, p_pricing_model?, p_season_start?, p_season_end?)` | `venueCreateMembershipTier(token, name, benefits, prices, {audience,pricingModel,seasonStart,seasonEnd})` (mig 271; **sig extended mig 291 — old 4-arg DROPPED**) | anon, authenticated | **WRITE, gated `manage_memberships`**. Creates a tier + its `venue_tier_prices` rows (period ∈ monthly/quarterly/annual/season; `price_type` ∈ standard/family/sibling). `p_audience` ∈ all/adult/junior/child; `p_pricing_model` ∈ recurring/season. Audit `venue_tier_created`. *Consumers: apps/venue MembershipsView TierModal.* |
| `venue_update_membership_tier(p_venue_token, p_tier_id, p_name?, p_benefits?, p_active?, p_prices?, p_audience?, p_pricing_model?, p_season_start?, p_season_end?)` | `venueUpdateMembershipTier(token, tierId, {...})` (mig 271; **sig extended mig 291 — old 6-arg DROPPED**) | anon, authenticated | **WRITE, gated**. Partial update (NULL = unchanged); `p_prices` upserts by `(tier_id, period, price_type)`. `tier_not_found`. Audit `venue_tier_updated`. *Consumer: apps/venue MembershipsView TierModal.* |
| `venue_enrol_membership(p_venue_token, p_customer_id, p_tier_id, p_period)` | `venueEnrolMembership(token, customerId, tierId, period)` (mig 271) | anon, authenticated | **WRITE, gated**. Enrols a person, snapshots `amount_pence` from the tier price, sets `renews_at = today + period`, mints the first `membership` charge. Raises `already_member` (one live per person), `price_not_set`, `customer_not_found`, `tier_not_found`, `invalid_period`. Audit `venue_membership_enrolled`. |
| `venue_freeze_membership(p_venue_token, p_membership_id, p_until)` | `venueFreezeMembership(token, membershipId, until)` (mig 271) | anon, authenticated | **WRITE, gated**. Pauses (no charge while frozen) + pushes `renews_at` out by the freeze length so the frozen window is never billed. Auto-reactivated by the renewal cron when `frozen_until` passes. `membership_not_active`, `invalid_freeze_until`. Audit `venue_membership_frozen`. |
| `venue_cancel_membership(p_venue_token, p_membership_id, p_immediate?)` | `venueCancelMembership(token, membershipId, immediate?)` (mig 271) | anon, authenticated | **WRITE, gated**. `immediate`=true → `cancelled` now; false → `ending` (access to period end; renewal cron flips to `cancelled` at `renews_at`, no further charge). `membership_not_cancellable`. Audit `venue_membership_cancelled`. |
| `venue_list_members(p_venue_token)` | `venueListMembers(token)` (mig 271; +`pass_token` mig 272; **V2+club-scoped mig 308**) | anon, authenticated | **READ** (any member). Roster: membership + customer/profile + tier. LEFT JOINs both `venue_customers` and `member_profiles`; COALESCE names/email. WHERE clause includes V2 club-scoped members (EXISTS club_venues). Returns `customer_id` + `member_profile_id` on each row. Excludes cancelled. *Consumers: Phase 4 MembershipsView; Phase 6 churn intelligence.* |
| `get_member_pass(p_token)` | `getMemberPass(passToken)` (mig 272; +`offers` mig 273; **V2 fix + valid_venues + member_profile_id mig 308; +`payment_state` mig 331**) | anon, authenticated | **PUBLIC READ** keyed by the secret `pass_token`. Returns `{ok, member_profile_id, first_name, last_name, tier_name, benefits, period, amount_pence, status, payment_state, renews_at, frozen_until, venue_name/logo/colours, check_in_code, offers[], valid_venues[]}`. `payment_state` = current/past_due/suspended (Stripe payment health). *Consumer: apps/inorout `MemberPass.jsx` at `/m/<token>`.* |
| `member_check_in(p_display_token, p_pass_token)` | `memberCheckIn(displayToken, passToken)` (mig 274; **multi-venue + V2 names mig 308**) | anon, authenticated | **WRITE (reception check-in).** The display scans the member's pass QR. Multi-venue: if `venue_id ≠ display_venue`, checks club_venues EXISTS (club member scanning at a sibling venue). V2 fix: LEFT JOINs venue_customers + member_profiles; COALESCE names. INSERT now writes `member_profile_id` alongside nullable `customer_id`. De-dup within 4h. Returns `{ok, first_name, last_name, tier_name, status, frozen, visit_count, already_checked_in}` or `{ok:false, reason}`. *Consumer: apps/display `CheckInOverlay.jsx`.* |
| `venue_list_club_venues(p_venue_token, p_club_id)` | `venueListClubVenues(token, clubId)` (mig 308) | anon, authenticated | **READ.** Caller must be in the club. Returns `{ok, venues:[{venue_id, venue_name, city, is_self, recent_checkins(30d)}]}`. *Consumer: apps/venue MembershipsView ClubTab VenuesSection.* |
| `venue_add_club_venue(p_venue_token, p_club_id, p_target_venue_id)` | `venueAddClubVenue(token, clubId, targetVenueId)` (mig 308) | anon, authenticated | **WRITE.** Trusted-network auth: caller's venue must already be in the club. Target venue must exist. Idempotent: `already_existed=true`. Audits `club_venue_added`. EV 14/14 PASS. *Consumer: apps/venue MembershipsView ClubTab AddVenueModal.* |
| `venue_remove_club_venue(p_venue_token, p_club_id, p_target_venue_id)` | `venueRemoveClubVenue(token, clubId, targetVenueId)` (mig 308) | anon, authenticated | **WRITE.** Guards: caller in club; `last_venue_cannot_be_removed`; `venue_has_active_members` (blocks if active members were issued at target). Audits `club_venue_removed`. *Consumer: apps/venue MembershipsView ClubTab VenuesSection.* |
| `venue_search(p_venue_token, p_query, p_club_id?)` | `venueSearch(token, query, clubId?)` (mig 308) | anon, authenticated | **READ.** ILIKE on `venues.name` + `venues.city`. Returns `{ok, venues:[{venue_id, venue_name, city}]}` LIMIT 10. When `p_club_id` supplied, already-linked venues excluded. Min query length = 2. *Consumer: apps/venue MembershipsView ClubTab AddVenueModal search.* |
| `member_self_signup(p_code, p_first_name, p_last_name?, p_email?, p_phone?, p_consent_marketing?)` | `memberSelfSignup(code, {firstName,lastName,email,phone,consentMarketing})` (mig 275) | anon, authenticated | **PUBLIC WRITE.** A prospective member on the venue's `/q/<code>` (`venue_landing`) landing self-registers → a `venue_customers` row in new `pending` status. Resolves the venue from the invite code (must be entity `venue` + action `venue_landing` + active/in-window). Idempotent on `(venue,email)` → existing non-erased person returned as `already_registered` w/ its status (never duplicated). Bumps invite `use_count`; audits `venue_customer_self_signup` (actor_type `system`); notifies `customer_self_signup`. Returns `{ok, already_registered, status}` or `{ok:false, reason}` (`invalid_code`/`inactive`/`first_name_required`). EV 13/13 + leak 0. *Consumer: apps/inorout `VenueLanding.jsx`.* |
| `venue_approve_customer(p_venue_token, p_customer_id, p_approve?)` | `venueApproveCustomer(token, customerId, approve=true)` (mig 275) | anon, authenticated | **WRITE, gated `manage_memberships`.** Acts ONLY on a `pending` person: approve → `active`, reject → `archived` (else `{ok:false, reason:'not_pending'}`). Audits `venue_customer_approved`/`_rejected`; notifies `customer_approved`. Returns `{ok, status}`. *Consumer: apps/venue `MembershipsView.jsx` (Members → Membership requests, tier-less/legacy path).* |
| `get_venue_signup_tiers(p_code)` | `getVenueSignupTiers(code)` (mig 280; **extended mig 296; +`stripe_connected` mig 331; status bug fixed mig 332**) | anon, authenticated | **PUBLIC READ** keyed by the `/q` venue code. mig 331 adds `stripe_connected: boolean` to the `club` object; mig 332 fixes the check from `status='active'` to `status='connected'` (the valid CHECK value). Consumer: `MembershipSignup` wizard forks to Stripe Checkout when true + paid tier. |
| `member_get_self()` | `memberGetSelf()` (mig 287; +`active_clubs`+`managed_teams` mig 301; **+`venues[]` per club mig 308**) | **authenticated** (anon REVOKED) | **READ.** Returns `{found, id, first_name, last_name, email, phone, dob, active_clubs:[{club_id, club_name, venue_id, venue_name, tier_name, status, renews_at, pass_token, venues:[{venue_id,venue_name}]}], managed_teams:[{team_id,team_name,club_id,role}]}` or `{found:false}`. mig 308 adds `venues` array to each `active_clubs` row — the full club footprint (all club_venues entries). *Consumer: `apps/inorout` profile screen + `MemberPass.jsx` owner-check.* |
| `member_self_create_profile(p_first_name, p_last_name?, p_email?, p_dob?, p_phone?)` | `memberSelfCreateProfile({firstName,lastName,email,dob,phone})` (mig 296; **actor_type fixed mig 297**) | **authenticated** (anon REVOKED) | **WRITE.** Self-creates a `member_profiles` row for the authenticated user. Raises: `not_authenticated`, `first_name_required`, `profile_exists`. Audits `member_profile_self_created` (actor_type `player`). EV 4/4 + leak 0 (s99). *Consumer: `MembershipSignup` wizard, `apps/inorout VenueLanding.jsx`.* |
| `member_enrol_membership(p_invite_code, p_tier_id, p_period, p_for_profile_id?)` | `memberEnrolMembership(inviteCode, tierId, period, forProfileId?)` (mig 296; **actor_type fixed mig 297**) | **authenticated** (anon REVOKED) | **WRITE.** Self-enrols for free tiers / venues without Stripe. Stripe path uses `stripe_complete_member_enrolment` instead (mig 331). EV 8/8 + leak 0 (s99). *Consumer: `MembershipSignup` StepEnrol (free/non-Stripe path).* |
| `stripe_complete_member_enrolment(p_invite_code, p_subscription_id, p_stripe_customer_id, p_stripe_price_id, p_tier_id, p_period, p_member_profile_id, p_amount_pence?)` | — (mig 331; **actor_type fixed mig 335**) | **service_role ONLY** (anon + authenticated REVOKED) | **WRITE.** Called by `api/stripe-webhook.js` after `checkout.session.completed`. Idempotent on `stripe_subscription_id` (UNIQUE partial index). Season one-time payments pass NULL subscription_id. Validates invite code + period + tier ownership. Inserts `venue_memberships` with `stripe_*` fields + `payment_state='current'`. Audits `stripe_member_enrolled` actor_type=`'player'` (mig 335 fixed mig 331's `'member'` which violated check constraint). Returns `{ok, membership_id, pass_token}` or `{ok, already_enrolled:true}` or `{ok:false, reason}`. EV 7/7 PASS + leak=0 (s135). *Consumer: `api/stripe-webhook.js` checkout arm.* |
| `member_get_venue_membership_pass(p_invite_code)` | `memberGetVenueMembershipPass(inviteCode)` (mig 336) | **authenticated** (anon REVOKED) | **READ.** Returns `{found, pass_token, membership_id, status}` for the caller's active/ending membership at the venue identified by the invite code, or `{found:false}` if not enrolled. Used by `MembershipSignup` on mount to recover `passToken` after external Stripe redirect (state wiped on redirect), and to detect already-enrolled members so they jump straight to the done state. *Consumer: `MembershipSignup.jsx` mount effect.* |
| `member_self_signup(…, p_tier_id?)` | `memberSelfSignup(code, {…, tierId})` (mig 275; +tier mig 280, **6-arg DROPPED**) | anon, authenticated | **PUBLIC WRITE.** Now tier-aware: chosen tier is validated (must be `self_signup`). **FREE tier** (`benefits.is_free`) → auto-approved: active person + active £0 membership (pass issued, never renew-bills), returns `{free:true, status:'active', pass_token}`. **PAID/tier-less** → `pending` person tagged `requested_tier_id`, returns `{free:false, status:'pending'}`. Idempotent on email. EV 8/8. |
| **mig 282 — 360Player registration fields** widen `venue_create_customer` (8→28 args, **8-arg DROPPED**), `venue_update_customer` (10→30 args, **10-arg DROPPED**), `member_self_signup` (7→28 args, +`p_dob` & **7-arg DROPPED**) | wrappers take a single `reg`/object (`venueCreateCustomer(token, reg)`, `venueUpdateCustomer(token, id, reg)`, `memberSelfSignup(code, reg)`); shared `registrationParams()` mapper in supabase.js | anon, authenticated | All three accept the full set: `p_gender`, `p_address_line1/2/city/postcode`, `p_emergency_name/relationship/phone`, `p_medical_conditions/allergies/medications/gp_details`, `p_guardian_name/relationship/phone/email`, `p_consent_data_processing/terms/photo/medical` (each `_at` stamped on grant). **Gates** (RAISE on create, `{ok:false,reason}` on self-signup): `consent_required` (data-processing+terms), `guardian_required` (dob<18y → name+phone), `medical_consent_required` (any medical field → consent_medical). `venue_erase_customer` scrub + `venue_list_customers_people` SELECT extended to every new column. EV 9/9 + leak 0, rpc-security 5/5 PASS. *Consumers: apps/inorout `VenueLanding.jsx` (MemberSignupForm), apps/venue `MembershipsView.jsx` (EnrolModal + ProfileModal).* |
| `venue_approve_and_enrol(p_venue_token, p_customer_id, p_tier_id, p_period)` | `venueApproveAndEnrol(token, customerId, tierId, period)` (mig 280) | anon, authenticated | **WRITE, gated `manage_memberships`.** One tap: activate the pending person + clear the request + enrol them (membership + first charge for paid; £0/no-charge for free). Audits `venue_membership_enrolled` (`via:approve_and_enrol`); notifies `customer_approved`. *Consumer: apps/venue `ApproveEnrolModal`.* |
| `get_membership_reminders_due()` | direct `supabase.rpc` in cron.js (mig 276) | **service_role ONLY** (REVOKE anon/authenticated explicitly — returns member PII) | **READ.** Returns `{ok, reminders:[{kind, entity_key, email, first_name, venue_name, tier_name, amount_pence, period, date_label, pass_token}]}` due now across 4 kinds: `welcome` (started ≤1d), `renewal_due` (active, renews ≤+7d), `freeze_ending` (paused, thaws ≤+3d), `payment_due` (unpaid/partial membership charge at/over due_date). Members with no email skipped; charges joined via the `<mid>:<date>` source_id. `entity_key` is the cron's dedupe key. EV 5/5 + leak 0. *Consumer: `apps/inorout/api/cron.js membershipRemindersJob` (10:00 UK) → Resend, deduped via `notification_log`.* |
| `hq_get_membership_rollup(p_company_id)` | `hqGetMembershipRollup(companyId)` (mig 278) | authenticated (REVOKE anon) | **READ, gated `resolve_company_caller`** (auth.uid→company_admins; regional_admins scoped to their region; platform_admin sees all). Company-wide membership health: `{ok, venues:[{venue_id, venue_name, region, active, paused, ending, due_soon, mrr_pence, cancelled_30d, pending_requests}], total:{…, venues}}`. Same metrics as `venue_membership_summary` (mig 273) summed across the company's venues. EV: aggregation 4/4 + gate-rejects-unauth, leak 0. *Consumer: apps/hq `AnalyticsView.jsx` Memberships card.* |
| **Phase 10 — Club Attendance admin RPCs (mig 298, session 101). Auth: resolve_venue_caller + manage_memberships cap. All SECDEF/search_path/1-overload/anon+authenticated.** | | | |
| `club_create_cohort(p_venue_token, p_club_id, p_name, p_description?, p_min_age?, p_max_age?)` | `clubCreateCohort(token, clubId, {name, description?, minAge?, maxAge?})` | anon, authenticated | **WRITE.** Inserts `club_cohorts`. Raises: `invalid_venue_token`, `insufficient_role`, `name_required`, `club_not_found`. Audit `club_cohort_created`. EV 13/13 + leak 0 s101. |
| `club_list_cohorts(p_venue_token, p_club_id, p_include_inactive?)` | `clubListCohorts(token, clubId, includeInactive?)` | anon, authenticated | **READ.** Returns `[{cohort_id, name, description, min_age, max_age, active, created_at}]` for club. Raises: `club_not_found`. |
| `club_update_cohort(p_venue_token, p_cohort_id, p_name?, p_description?, p_min_age?, p_max_age?, p_active?)` | `clubUpdateCohort(token, cohortId, {name?, description?, minAge?, maxAge?, active?})` | anon, authenticated | **WRITE.** Partial update via cohort→club→club_venues ownership join. Raises: `cohort_not_found`. Audit `club_cohort_updated`. |
| `club_create_session(p_venue_token, p_club_id, p_title, p_scheduled_at, p_cohort_id?, p_location?, p_notes?, p_capacity?)` | `clubCreateSession(token, clubId, {title, scheduledAt, cohortId?, location?, notes?, capacity?})` | anon, authenticated | **WRITE.** Inserts `club_sessions` (status=`scheduled`). Raises: `title_required`, `scheduled_at_required`, `club_not_found`, `cohort_not_found`. Audit `club_session_created`. |
| `club_update_session(p_venue_token, p_session_id, p_title?, p_scheduled_at?, p_location?, p_notes?, p_capacity?)` | `clubUpdateSession(token, sessionId, {title?, scheduledAt?, location?, notes?, capacity?})` | anon, authenticated | **WRITE.** Partial update; only updates `status='scheduled'` sessions (cancelled → `session_not_found`). Ownership via session→club→club_venues. Audit `club_session_updated`. |
| `club_cancel_session(p_venue_token, p_session_id, p_reason?)` | `clubCancelSession(token, sessionId, reason?)` | anon, authenticated | **WRITE.** Sets `status='cancelled'`, `cancelled_reason`. Only cancels `status='scheduled'` sessions. Raises: `session_not_found`. Audit `club_session_cancelled`. |
| `club_list_sessions(p_venue_token, p_club_id, p_cohort_id?, p_from?, p_to?)` | `clubListSessions(token, clubId, {cohortId?, from?, to?})` | anon, authenticated | **READ.** Returns `[{session_id, club_id, cohort_id, cohort_name, title, scheduled_at, location, notes, capacity, status, cancelled_reason, rsvp_in, rsvp_out, rsvp_maybe, attendance_marked}]`. Raises: `club_not_found`. |
| `club_get_session_rsvps(p_venue_token, p_session_id)` | `clubGetSessionRsvps(token, sessionId)` | anon, authenticated | **READ.** Returns `{session_id, rsvps:[{rsvp_id, member_profile_id, first_name, status, note, updated_at}], attendance:[{member_profile_id, first_name, status, marked_at}]}`. First names only (operator decision — same-club privacy). Raises: `session_not_found`. |
| `club_mark_attendance(p_venue_token, p_session_id, p_attendances jsonb)` | `clubMarkAttendance(token, sessionId, attendances)` | anon, authenticated | **WRITE.** Bulk upsert to `club_session_attendance`. `p_attendances=[{member_profile_id, status}]`; `status ∈ attended/absent/late`. Idempotent re-mark. Only marks `status='scheduled'` sessions. Raises: `invalid_venue_token`, `insufficient_role`, `session_not_found`, `invalid_status`. Returns `{ok, session_id, marked}`. Audit `club_attendance_marked`. EV 13/13 + leak 0 s101. *Consumer: Slice 3 venue UI (not yet built).* |
| **Phase 10 — Club Attendance member RPCs (mig 299, session 102). Auth: auth.uid() → member_profiles.auth_user_id. Membership check: venue_memberships WHERE club_id + member_profile_id + status IN ('active','ending'). All SECDEF/search_path/1-overload/authenticated-only (anon revoked).** | | | |
| `member_list_upcoming_sessions(p_club_id, p_cohort_id?)` | `memberListUpcomingSessions(clubId, cohortId?)` | authenticated | **READ.** Lists `club_sessions` for a club where caller has an active membership: `status='scheduled'`, `scheduled_at > now()`, optional cohort filter. LEFT JOIN `club_session_rsvps` for caller's `own_rsvp_status`. Returns `[]` if none. Raises: `not_authenticated`, `membership_required`. |
| `member_rsvp_session(p_session_id, p_status, p_for_profile_id?, p_note?)` | `memberRsvpSession(sessionId, status, {forProfileId?, note?})` | authenticated | **WRITE.** Upserts `club_session_rsvps` (ON CONFLICT uq_session_rsvp_member). `p_status ∈ in/out/maybe`. If `p_for_profile_id` set, checks `member_guardians` — caller must be guardian. Only `status='scheduled'` sessions accepted. Raises: `not_authenticated`, `invalid_status`, `session_not_found`, `not_guardian`, `membership_required`. Returns `{ok, rsvp_id, status}`. Audit `club_session_rsvp_set` (`team_id='_system'`, `actor_type='player'`). EV 11/11 + leak 0 s102. |
| `member_get_session_rsvp_board(p_session_id)` | `memberGetSessionRsvpBoard(sessionId)` | authenticated | **READ.** Returns RSVP board grouped by status: `{session_id, in:[{first_name}], out:[{first_name}], maybe:[{first_name}], pending:[{first_name}]}`. First names only (operator decision). Caller must have active membership in session's club. Raises: `not_authenticated`, `session_not_found`, `membership_required`. EV 11/11 + leak 0 s102. |
| **Phase 10 — Club Attendance manager RPCs (mig 303, session 109). Auth: auth.uid() → member_profiles → club_team_managers.is_active=true for the target team. No venue token. All SECDEF/search_path/1-overload/authenticated-only (anon+PUBLIC revoked). Audit: `team_id='_system'`, `actor_type='player'`, `actor_identifier=first_name+last_name`.** | | | |
| `club_manager_create_session(p_team_id, p_title, p_scheduled_at, p_session_type, p_location?, p_notes?, p_capacity?, p_meet_time?, p_opponent_name?, p_home_away?, p_opponent_venue_name?, p_opponent_address?)` | `clubManagerCreateSession(teamId, {title, scheduledAt, sessionType, location?, notes?, capacity?, meetTime?, opponentName?, homeAway?, opponentVenueName?, opponentAddress?})` | authenticated | **WRITE.** Creates a single `club_sessions` row with `status='scheduled'`; derives `club_id`/`cohort_id` from `club_teams`. Raises: `not_authenticated`, `profile_not_found`, `not_a_manager`, `team_not_found`. Returns `{ok, session_id}`. Audit `manager_session_created`. EV 7/7 + leak 0 s109. |
| `club_manager_create_session_series(p_team_id, p_title, p_day_of_week, p_start_time, p_from_date, p_to_date, p_session_type, p_location?, p_notes?, p_capacity?)` | `clubManagerCreateSessionSeries(teamId, {title, dayOfWeek, startTime, fromDate, toDate, sessionType, location?, notes?, capacity?})` | authenticated | **WRITE.** Inserts `club_session_series` row + pre-generates all weekly `club_sessions` from `from_date` to `to_date` by DOW stepping (same pattern as `club_create_session_series` mig 302). `p_day_of_week` = EXTRACT(DOW): 0=Sun…6=Sat. Returns `{ok, series_id, sessions_created}`. Audit `manager_series_created`. EV 7/7 + leak 0 s109. |
| `club_manager_cancel_session(p_session_id, p_reason?)` | `clubManagerCancelSession(sessionId, reason?)` | authenticated | **WRITE.** Sets `status='cancelled'` on one session. Guards: `not_team_session` (team_id IS NULL — whole-cohort sessions untouchable), `not_a_manager`, `session_not_scheduled`, `session_in_past`. Returns `{ok, session_id}`. Audit `manager_session_cancelled`. EV 7/7 + leak 0 s109. |
| `club_manager_get_team_members(p_team_id, p_session_id?)` | `clubManagerGetTeamMembers(teamId, sessionId?)` | authenticated | **READ.** Returns `[{profile_id, first_name, last_name, is_session_guest}]` for active team members (`club_team_members.is_active=true`). When `p_session_id` provided, folds `is_session_guest` bool via EXISTS on `club_session_guests`. Returns `[]` if none. Raises: `not_authenticated`, `profile_not_found`, `not_a_manager`. No audit. EV 7/7 + leak 0 s109. |
| `club_manager_add_session_guest(p_session_id, p_guest_profile_id)` | `clubManagerAddSessionGuest(sessionId, guestProfileId)` | authenticated | **WRITE.** INSERT INTO `club_session_guests` ON CONFLICT DO NOTHING (idempotent). Guards: `not_team_session`, `not_a_manager`, `session_not_scheduled`, `guest_not_found`. Returns `{ok, session_id, guest_profile_id}`. Audit `manager_guest_added`. EV 7/7 + leak 0 s109. |
| `club_manager_remove_session_guest(p_session_id, p_guest_profile_id)` | `clubManagerRemoveSessionGuest(sessionId, guestProfileId)` | authenticated | **WRITE.** DELETE FROM `club_session_guests`. Same guards as add. Returns `{ok, session_id, guest_profile_id}`. Audit `manager_guest_removed`. EV 7/7 + leak 0 s109. |
| `club_manager_mark_attendance(p_session_id, p_attendances jsonb)` | `clubManagerMarkAttendance(sessionId, attendances)` | authenticated | **WRITE (mig 304, session 110).** Bulk upsert into `club_session_attendance` (status: attended/absent/late). Guards: `not_authenticated`, `profile_not_found`, `not_a_manager`. `p_attendances = [{member_profile_id, status}]`. ON CONFLICT DO UPDATE idempotent. Audit `team_id='_system'`, `actor_type='player'`. EV 8/8 + leak=0 s110. *Consumer: `apps/inorout` SessionsScreen attendance panel.* |
| **Phase 11 — Club Comms (mig 307, session 112). One-way broadcast announcements from venue dashboard to club members via email. Table: `club_announcements`. Auth: venue token (write) / auth.uid() (member read) / service_role (cron). All SECDEF/search_path. Security sweep 3/3 PASS. EV 7 assertions + leak=0.** | | | |
| `club_send_announcement(p_token, p_club_id, p_title, p_body, p_audience, p_cohort_id?, p_team_id?)` | `clubSendAnnouncement(venueToken, clubId, title, body, audience, cohortId?, teamId?)` | anon, authenticated | **WRITE (mig 307).** Guards: `manage_memberships` cap, club in venue, audience in (club/cohort/team), cohort/team_id required per audience, cohort/team must belong to club. Inserts `club_announcements` row with `status='queued'`. Audit `club_announcement_queued`. EV 7/7 + leak=0 s112. *Consumer: `apps/venue` MembershipsView AnnouncementsTab.* |
| `get_pending_club_broadcasts()` | *(service_role cron only — no JS wrapper)* | **service_role ONLY** | **READ (mig 307).** Returns `{ok, broadcasts:[{announcement_id, club_id, title, body, audience, club_name, venue_name, created_at, recipients:[{member_profile_id, first_name, email}]}]}`. 3-way UNION recipient resolution (audience=club via venue_memberships, cohort via cohort-scoped vm, team via club_team_members). Used by `clubBroadcastJob` in cron.js. |
| `member_list_club_announcements(p_club_id)` | `memberListClubAnnouncements(clubId)` | authenticated (anon revoked) | **READ (mig 307).** Auth: `auth.uid()→member_profiles.auth_user_id`; active membership check. Returns `{ok, announcements:[{id,title,body,audience,created_at}]}` — only `status='sent'`; cohort/team scoped to caller's membership/team. LIMIT 20 DESC. *Consumer: `apps/inorout` SessionsScreen AnnouncementsSection.* |
| **Phase 12 — Club Staff + DBS (migs 305–306, session 111). Auth mix: venue-token RPCs use `resolve_venue_caller`; manager RPCs use `auth.uid()→member_profiles→club_team_managers`. All SECDEF/search_path. Security sweep 7/7 PASS (incl. caught + fixed expire_staff_dbs auto-grant; see rpc-security-sweep notes s111).** | | | |
| `club_manager_get_team_members(p_team_id, p_session_id?)` | `clubManagerGetTeamMembers(teamId, sessionId?)` | authenticated | **READ (UPDATED mig 306).** Now includes `has_medical_notes` bool (`medical_conditions OR allergies OR medications OR gp_details IS NOT NULL`). Used by SessionsScreen medical-alert panel. *Consumers: `apps/inorout` SessionsScreen (original + new medical-alerts path).* |
| `club_manager_get_member_detail(p_member_profile_id)` | `clubManagerGetMemberDetail(memberProfileId)` | authenticated | **READ (mig 306).** Two-tier auth: (1) role='manager' on ANY active club_team in the member's club → full CPSU + guardian access; (2) coach/asst_manager who shares any team with target → same. Returns `{profile_id, first_name, last_name, dob, ec1_*, ec2_*, medical_conditions, allergies, medications, gp_details, send_notes, dietary_notes, consent_emergency_treatment, consent_administer_medication, guardian_first_name, guardian_last_name, guardian_phone}`. Raises `not_authenticated`, `profile_not_found`, `member_not_found`, `not_authorised`. No audit. *Consumer: `apps/inorout` SessionsScreen medical-alert expandable cards.* |
| `venue_assign_team_manager(p_token, p_team_id, p_member_profile_id, p_role)` | `venueAssignTeamManager(token, teamId, memberProfileId, role)` | anon, authenticated | **WRITE (mig 305).** Inserts/upserts `club_team_managers`. Guards: manage_memberships cap, team in venue's club, member has active/ending membership in that club, role in (manager/assistant_manager/coach). ON CONFLICT DO UPDATE role+is_active+assigned_at. Audit `staff_assigned`. EV 10/10 + leak=0 s111. *Consumer: `apps/venue` MembershipsView StaffTab.* |
| `venue_remove_team_manager(p_token, p_team_id, p_member_profile_id)` | `venueRemoveTeamManager(token, teamId, memberProfileId)` | anon, authenticated | **WRITE (mig 305).** Soft-deletes `club_team_managers` (SET is_active=false). Guards: manage_memberships cap, team in venue. Audit `staff_removed`. EV 10/10 + leak=0 s111. *Consumer: `apps/venue` MembershipsView StaffTab.* |
| `venue_list_club_staff(p_token, p_club_id)` | `venueListClubStaff(token, clubId)` | anon, authenticated | **READ (mig 305).** Returns all `club_team_managers` rows for a club's teams, LEFT JOIN `club_staff_dbs`. No cap check (read). Raises `invalid_venue_token`, `club_not_found`. Returns `[{team_id, team_name, cohort_id, manager_id, member_profile_id, first_name, last_name, role, is_active, dbs_id, dbs_status, dbs_check_type, dbs_expiry_date}]`. *Consumer: `apps/venue` MembershipsView StaffTab.* |
| `venue_upsert_staff_dbs(p_token, p_member_profile_id, p_club_id, p_check_type, p_status, p_certificate_number?, p_issued_date?, p_expiry_date?, p_notes?)` | `venueUpsertStaffDbs(token, memberProfileId, clubId, {checkType, status, certificateNumber?, issuedDate?, expiryDate?, notes?})` | anon, authenticated | **WRITE (mig 305).** Upserts `club_staff_dbs` (UNIQUE member_profile_id+club_id). Guards: manage_memberships, club in venue, member exists, check_type/status value validation. Audit `staff_dbs_recorded`. EV 10/10 + leak=0 s111. *Consumer: `apps/venue` MembershipsView StaffTab DBS modal.* |
| **Phase 9 — Club Merchandise (mig 309, session 114). Tables: `club_merchandise` + `club_purchases`. 8 RPCs. EV 14/14 PASS + leak=0. Stripe scaffold dormant — purchases enter `pending_payment`; status moves to `pending` when Stripe activates (same Connect account as memberships). Bug caught by EV: `venue_list_merchandise` had nested aggregate (fixed with inner subquery in mig 309b).** | | | |
| `venue_upsert_merchandise(p_venue_token, p_club_id, p_name, p_category, p_price_pence, p_id?, p_description?, p_stock_qty?, p_active?)` | `venueUpsertMerchandise(venueToken, clubId, {name, category, pricePence, id?, description?, stockQty?, active?})` | anon, authenticated | **WRITE (mig 309).** Guards: manage_memberships, club in venue. Upserts `club_merchandise` (INSERT when `p_id IS NULL`, UPDATE otherwise). category CHECK: kit\|accessories\|equipment\|other. Audit `merchandise_upserted`. Returns `{ok, item_id, is_new}`. *Consumer: `apps/venue` MembershipsView Shop tab CataloguePanel.* |
| `venue_list_merchandise(p_venue_token, p_club_id)` | `venueListMerchandise(venueToken, clubId)` | anon, authenticated | **READ (mig 309).** Returns `{items:[{id, name, description, category, price_pence, stock_qty, active, created_at, purchases_count, pending_count}]}` (counts via inner subquery — nested aggregate fix mig 309b). *Consumer: `apps/venue` MembershipsView Shop tab CataloguePanel.* |
| `venue_list_purchases(p_venue_token, p_club_id, p_status?)` | `venueListPurchases(venueToken, clubId, status?)` | anon, authenticated | **READ (mig 309).** Returns `{purchases:[{id, item_id, item_name, item_category, member_name, member_profile_id, quantity, unit_price_pence, total_pence, status, notes, created_at, stripe_payment_intent_id}]}`. Filter by status optional. *Consumer: `apps/venue` MembershipsView Shop tab OrdersPanel.* |
| `venue_fulfil_purchase(p_venue_token, p_purchase_id, p_notes?)` | `venueFulfilPurchase(venueToken, purchaseId, notes?)` | anon, authenticated | **WRITE (mig 309).** Guards: manage_memberships, purchase in venue, status must be pending_payment\|pending (else `invalid_transition`). Sets status='fulfilled'. Audit `merchandise_fulfilled`. Returns `{ok:true}`. *Consumer: `apps/venue` MembershipsView Shop tab OrdersPanel Fulfil button.* |
| `venue_cancel_purchase(p_venue_token, p_purchase_id, p_reason?)` | `venueCancelPurchase(venueToken, purchaseId, reason?)` | anon, authenticated | **WRITE (mig 309).** Guards: manage_memberships, purchase in venue, not already fulfilled (`already_fulfilled`), not already cancelled (`already_cancelled`). Sets status='cancelled'. Audit `merchandise_cancelled`. Returns `{ok:true}`. *Consumer: `apps/venue` MembershipsView Shop tab OrdersPanel Cancel button.* |
| `member_get_merchandise(p_club_id)` | `memberGetMerchandise(clubId)` | **authenticated** (anon REVOKED) | **READ (mig 309).** Auth: auth.uid()→member_profiles, active membership check for club. Returns `{items:[{id, name, description, category, price_pence, stock_qty}]}` — only active items with stock_qty > 0 (or NULL=unlimited). *Consumer: `apps/inorout` SessionsScreen ShopSection.* |
| `member_purchase_merchandise(p_item_id, p_qty?, p_notes?)` | `memberPurchaseMerchandise(itemId, qty?, notes?)` | **authenticated** (anon REVOKED) | **WRITE (mig 309).** Auth: auth.uid()→member_profiles, active membership in item's club (else `not_a_member`). Guards: item active (else `item_not_found`), stock (else `insufficient_stock`), qty ≥ 1 (else `invalid_quantity`). Decrements stock_qty if not NULL. Inserts `club_purchases` with status=`pending_payment`. Audit `merchandise_purchased`. Returns `{ok, purchase_id, item_name, total_pence}`. *Consumer: `apps/inorout` SessionsScreen ShopItemRow.* |
| `member_list_my_purchases(p_club_id?)` | `memberListMyPurchases(clubId?)` | **authenticated** (anon REVOKED) | **READ (mig 309).** Auth: auth.uid()→member_profiles. Returns `{purchases:[{id, item_id, item_name, item_category, club_id, quantity, unit_price_pence, total_pence, status, created_at}]}` — cancelled excluded; optionally scoped to one club. *Consumer: `apps/inorout` MemberProfile "My orders" section.* |
| **Phase 7 Stripe scaffolding (mig 279 — DORMANT, no money moves; see DECISIONS money-flow gate)** | | | |
| `record_stripe_event(stripe_event_id, entity_type, entity_id, event_type, amount_pence, payload)` | webhook (no JS wrapper yet) | **service_role ONLY** | Persist-then-process + idempotent (UNIQUE `billing_events.stripe_event_id`). Returns `{inserted, event_id, status}` — `inserted=false` on replay so the handler skips. EV 5/5. |
| `mark_stripe_event_processed(stripe_event_id, status?, note?)` | webhook | **service_role ONLY** | Closes the billing_event lifecycle (`processed`/`failed`/`ignored`). |
| `apply_membership_subscription_status(subscription_id, stripe_status)` | webhook + reconciliation cron | **service_role ONLY** | THE STATE MACHINE. Maps Stripe sub status → `venue_memberships.payment_state` (`current`/`past_due`/`suspended`); `canceled` also sets `status='cancelled'`. Audits `membership_payment_state`. `{ok:false, no_membership}` if unmatched. |
| `set_venue_connect_state(venue_id, account_id, status, charges_enabled, details_submitted)` | webhook (`account.updated`) / onboarding callback | **service_role ONLY** | Upserts into `venue_integrations` (provider='stripe'). Maps Stripe account status (none/onboarding/active/restricted) → integration status (pending/connected). Stores charges_enabled + details_submitted in config jsonb. (mig 329 rewrite) |
| `venue_get_billing_status(p_venue_token)` | `venueGetBillingStatus(token)` | anon, authenticated (gated `manage_memberships`) | **READ.** Returns `{ok, stripe:{connected, status, account_id, config, connected_at, disconnected_at}, gocardless:{connected, status, account_id, connected_at, disconnected_at}, members:{total, on_stripe, current, past_due, suspended}}`. Reads from `venue_integrations`. Powers IntegrationsView. (mig 329 rewrite) |
| `venue_stripe_disconnect(p_venue_token)` | `venueStripeDisconnect(token)` | anon, authenticated (gated `manage_memberships`) | **WRITE.** Sets `venue_integrations` status='disconnected', disconnected_at=now() for provider='stripe'. Idempotent (already-disconnected returns `{ok:true, already_disconnected:true}`). Raises `invalid_venue_token` / `insufficient_role` / `not_connected`. Audits `stripe_disconnected`. EV 5/5 PASS, leak-clean. (mig 330) |
| `venue_create_partner(p_venue_token, p_name, p_contact?)` | `venueCreatePartner(token, name, contact?)` (mig 273) | anon, authenticated | **WRITE, gated `manage_memberships`** (Phase 6, EV 7/7). Local partner (e.g. the pub). Audit `venue_partner_created`. |
| `venue_create_offer(p_venue_token, p_partner_id, p_title, p_description?, p_code?, p_tier_ids?)` | `venueCreateOffer(token, partnerId, title, {...})` (mig 273) | anon, authenticated | **WRITE, gated**. Offer on a partner; `p_tier_ids` NULL/empty = all members else tier-scoped; `p_code` NULL = show-your-pass. `partner_not_found`, `title_required`. Audit `venue_offer_created`. |
| `venue_set_offer_active(p_venue_token, p_offer_id, p_active)` | `venueSetOfferActive(token, offerId, active)` (mig 273) | anon, authenticated | **WRITE, gated**. Toggle an offer on/off. `offer_not_found`. |
| `venue_list_partners(p_venue_token)` | `venueListPartners(token)` (mig 273) | anon, authenticated | **READ**. Partners + their offers + redemption counts. *Consumer: MembershipsView Perks tab.* |
| `redeem_member_offer(p_pass_token, p_offer_id)` | `redeemMemberOffer(passToken, offerId)` (mig 273) | anon, authenticated | **WRITE, public** (keyed by the secret pass token). Logs a `partner_redemptions` row + returns `{ok, title, code}`; `{ok:false, reason}` (invalid_pass/invalid_offer/not_eligible). *Consumer: MemberPass.jsx perk reveal.* |
| `venue_membership_summary(p_venue_token)` | `venueMembershipSummary(token)` (mig 273) | anon, authenticated | **READ**. `{active, paused, ending, due_soon, mrr_pence (cadence-normalised), cancelled_30d}`. *Consumer: MembershipsView Members stat strip; Phase 6 HQ rollup forward.* |
| `venue_list_membership_tiers(p_venue_token, p_include_inactive?)` | `venueListMembershipTiers(token, includeInactive?)` (mig 271; enriched mig 291) | anon, authenticated | **READ**. Returns `{ok, tiers:[…]}`. Each tier now includes `audience`, `pricing_model`, `season_start`, `season_end`; prices include `price_type`. *Consumer: apps/venue MembershipsView PlansTab.* |
| `venue_list_clubs(p_venue_token)` | `venueListClubs(token)` (mig 286; **fixed token-auth + safeguarding_config mig 291**) | anon, authenticated | **READ**. Clubs linked to this venue via `club_venues`. Returns bare jsonb array: `[{id, name, short_name, contact_email, id_mandate, safeguarding_config, cohorts_count}]`. Old mig-286 version used `p_venue_id + auth.uid()` — broken at runtime; mig 291 rewrites to use `resolve_venue_caller`. *Consumer: apps/venue MembershipsView ClubTab.* |
| `venue_update_club_settings(p_venue_token, p_club_id, p_id_mandate?, p_safeguarding_config?)` | `venueUpdateClubSettings(token, clubId, {idMandate, safeguardingConfig})` (mig 291, **NEW**) | anon, authenticated | **WRITE, gated `manage_memberships`**. Updates `clubs.id_mandate` and/or `clubs.safeguarding_config` (CPSU field toggles). Validates club belongs to the caller's venue via `club_venues`. Audit `venue_club_settings_updated`. *Consumer: apps/venue MembershipsView ClubTab.* |
| `venue_create_fee_plan(p_venue_token, p_name, p_amount_pence, p_period, p_sport?)` | `venueCreateFeePlan(token, name, amountPence, period, sport?)` (mig 271) | anon, authenticated | **WRITE, gated**. Team/booker recurring fee plan (period also allows `weekly`). Audit `venue_fee_plan_created`. |
| `venue_enrol_fee(p_venue_token, p_plan_id, p_member_key, p_team_id?)` | `venueEnrolFee(token, planId, memberKey, teamId?)` (mig 271) | anon, authenticated | **WRITE, gated**. Enrols a booker (`member_key` = team id or booked-by name; `p_team_id` set when a team → `venue_charges.team_id`). Mints first `fee` charge. `plan_not_found`, `team_not_found`. Audit `venue_fee_enrolled`. |
| `venue_cancel_fee(p_venue_token, p_subscription_id)` | `venueCancelFee(token, subscriptionId)` (mig 271) | anon, authenticated | **WRITE, gated**. `subscription_not_active`. Audit `venue_fee_cancelled`. |
| `venue_list_fee_plans(p_venue_token)` | `venueListFeePlans(token)` (mig 271) | anon, authenticated | **READ**. Fee plans + their active subscriptions. |
| `run_membership_renewals()` | *(none — cron calls `supabase.rpc` directly)* (mig 271) | **service_role only** | **WRITE, cron engine.** One pass: reactivate lapsed freezes, flip `ending`→`cancelled` at term, mint the next `venue_charges` for due memberships + fee subscriptions, advance `renews_at`/`next_charge_at`. Idempotent (charge `source_id` encodes the period). Driven by `apps/inorout/api/cron.js membershipRenewalsJob` (09:00 UK). **Phase 7 will extend this engine for Stripe reconciliation.** |
| `venue_get_customer(p_venue_token, p_booker_key)` | `venueGetCustomer(token, bookerKey)` (mig 226) | anon, authenticated | Venue read, venue-domain. One booker's bookings at this venue (newest first) with per-booking charge (`paid` from `venue_payments`, `amount_due_pence`) and live `in_count`/`target` on UPCOMING team sessions (counts only). booker_key = `team:<id>` / `walkin:<lower(name)>`. *Consumers: venue CustomerDetailModal.* |
| `notify_booking_ins_for_team(p_team_id)` + trigger `players_ins_notify` (`trg_notify_booking_ins`) | — (internal) | — | mig 225. AFTER UPDATE OF status ON players → pings each venue where the team has an upcoming booking with a content-free `booking_ins_changed` broadcast (drives the venue's live ins refetch). Exception-swallowing — never breaks the player toggle. |
| `cancel_booking_series(p_series_id, p_venue_token?)` | `cancelBookingSeries(...)` (Stage 5/6) | anon, authenticated | Dual auth (same as above). Cancels the series + its live weekly bookings, frees their slots. Fires `booking_cancelled`. No per-booking refund decision yet (follow-up). *Consumers: both apps.* |
| `venue_update_booking_settings(p_venue_token, p_updates)` | `venueUpdateBookingSettings(...)` (Stage 6) | anon, authenticated | Venue settings write (mig 150; `default_prime_time_windows` mig 177; **`payment_link` mig 183**). Keys: `bookings_enabled` (bool), `cancellation_policy` (text, blank→NULL), `default_prime_time_windows` (jsonb), `payment_link` (text, validated `^https?://`, blank→NULL). Auth via `resolve_venue_caller`. Audit + `notify_venue_change('venue_updated')`. *Consumer: `apps/venue` BookingSettings + PaymentsView (pay-link editor).* |
| `venue_add_fixture_charge(p_venue_token, p_fixture_id, p_team_id, p_amount_pence DEFAULT NULL)` | `venueAddFixtureCharge(token, fixtureId, teamId, amountPence?)` | anon, authenticated | **WRITE** (mig 183, ephemeral-verify gated). Manual per-team fixture charge. Amount = arg or `league_config.fixture_fee_pence`. Validates fixture∈venue + team∈fixture; rejects `charge_exists` (active dup), reactivates a `refunded` charge (clears status→unpaid then `_recompute_charge_status`). Audit `charge_created`; `notify_venue_change('charge_updated')`. *Consumer: `apps/venue` PaymentsView Add-charge.* |
| `venue_void_charge(p_venue_token, p_charge_id)` | `venueVoidCharge(token, chargeId)` | anon, authenticated | **WRITE** (mig 183, ephemeral-verify gated). Sets charge `status='refunded'` (drops from owed/collected), payments kept; idempotent (`already:true`). Audit `charge_voided`; `notify_venue_change('charge_updated')`. *Consumer: `apps/venue` PaymentsView Void.* |
| `venue_log_incident(p_venue_token, p_description, p_severity, p_fixture_id DEFAULT NULL)` | `venueLogIncident(token, description, severity, fixtureId?)` | anon, authenticated | **WRITE** (mig 231, ephemeral-verify 12/12). Venue admin reports an incident. Validates `description_required`, `severity ∈ info/warning/critical`, optional `p_fixture_id` must be in-venue (`fixture_not_in_venue`). Inserts `incidents` (`reported_by=auth.uid()`, NULL for token callers — `reported_by` made nullable mig 231). Audit `incident_flagged`; `notify_venue_change('incident_flagged')`. *Consumer: `apps/venue` Operations ReportIncidentButton.* |
| `venue_resolve_incident(p_venue_token, p_incident_id, p_resolution_note DEFAULT NULL)` | `venueResolveIncident(token, incidentId, note?)` | anon, authenticated | **WRITE** (mig 231, ephemeral-verify 12/12). Venue admin closes one of their own open incidents (scoped `id+venue_id+resolved_at IS NULL`; cross-venue/double-resolve → `incident_not_found_or_resolved`). Sets `resolved_at/resolved_by/resolution_note`. Audit `incident_resolved`; `notify_venue_change('incident_resolved')`. Venue-side sibling of `hq_resolve_incident` (mig 171). *Consumer: `apps/venue` Operations IncidentActions.* |
| `venue_list_equipment(p_venue_token)` | `venueListEquipment(token)` | anon, authenticated | **READ** (mig 256, Cycle 1 of EQUIPMENT_HIRE_PLAN.md). STABLE SECDEF, `resolve_venue_caller`. The venue's kit catalogue + per-item `hires_count`/`out_now` (all-zero in Cycle 1 — shaped now for the Cycle 2 hire flow per Hard Rule #12/#14) + summary (`item_count`, `active_count`, `total_units`, `asset_value_pence`). Venue-domain. *Consumer: `apps/venue` EquipmentView.* |
| `venue_upsert_equipment(p_venue_token, p_name, p_category, p_quantity, p_id?, p_default_fee_pence?, p_deposit_pence?, p_hire_unit?, p_purchase_price_pence?, p_acquired_on?, p_condition?, p_active?)` | `venueUpsertEquipment(token, {name, category, quantity, id?, defaultFeePence?, depositPence?, hireUnit?, purchasePricePence?, acquiredOn?, condition?, active?})` | anon, authenticated | **WRITE** (mig 256, EV 10/10 + leak 0, rpc-security-sweep PASS). Create (`p_id` NULL) or edit a catalogue item; ownership-checked on edit (`equipment_not_in_venue`). Validates category/hire_unit/condition enums + non-negative amounts + name. Audit `equipment_created`/`equipment_updated`. No `notify_venue_change` (single-operator; view self-refetches). Sport-agnostic — venue's own catalogue is the sport adapter. *Consumer: `apps/venue` EquipmentView EquipmentModal.* **Future consumers (Hard Rule #14):** Cycle 2 hire flow + availability; Phase HQ equipment-ROI/utilisation. |
| `get_equipment_availability(p_venue_token, p_from, p_to, p_category?)` | `getEquipmentAvailability(token, from, to, category?)` | anon, authenticated | **READ** (mig 257, Cycle 2). STABLE SECDEF. **Quantity-aware** free units per active item across `[from,to)` — `free = quantity − peak-concurrent committed qty` (via internal `_equipment_peak_committed`). The new logic vs pitch booking's binary free/busy. *Consumer: `apps/venue` EquipmentView HiresPanel availability.* |
| `venue_create_equipment_hire(p_venue_token, p_equipment_id, p_qty, p_start_at, p_end_at, p_team_id?, p_booked_by_name?, p_due_back_at?, p_booking_id?, p_fixture_id?, p_contact_email?, p_contact_phone?, p_amount_pence?)` | `venueCreateEquipmentHire(token, {equipmentId, qty, startAt, endAt, teamId?, bookedByName?, …})` | anon, authenticated | **WRITE** (mig 257, EV 13/13 + leak 0, security-sweep PASS). Pre-confirmed hire (mirrors `venue_create_booking`) + **row-locked quantity guard** (`SELECT … FOR UPDATE` on the catalogue row serialises concurrent hires). Auto-charges the fee via the shared ledger (`source_type='equipment'`; skip if zero). On insufficient stock **RETURNS `{ok:false, reason:'insufficient_quantity', free, wanted}` AND logs an `equipment_demand_misses` row** — a soft-fail not a RAISE, so the turn-away signal persists (a RAISE would roll it back). Audit `equipment_hired` / `equipment_demand_miss`. Booker = `team_id` OR `booked_by_name` (`booker_required`). *Consumer: `apps/venue` EquipmentView HireModal.* **Future:** Cycle 4 self-serve QR hire. |
| `venue_cancel_equipment_hire(p_venue_token, p_hire_id)` | `venueCancelEquipmentHire(token, hireId)` | anon, authenticated | **WRITE** (mig 257, EV-covered). Cancels a hire (`status='cancelled'`, frees the stock) + voids its equipment charge (`refunded`, payments kept). Idempotent (`already:true` for already-terminal). Audit `equipment_hire_cancelled`. *Consumer: `apps/venue` EquipmentView Hires list.* |
| `venue_list_equipment_hires(p_venue_token, p_status?, p_limit?)` | `venueListEquipmentHires(token, {status?, limit?})` | anon, authenticated | **READ** (mig 257; extended mig 259). STABLE SECDEF. Hires for this venue (newest first) joined to equipment name + booker + charge state (`amount_due_pence`, `paid_pence`, `charge_status`). **Mig 259 adds** `deposit_pence`/`deposit_status`, `handed_out_at`, `returned_at`/`returned_condition`, derived `is_overdue`, and a board `summary` (`out_now`/`overdue`/`due_today`). Optional status filter. *Consumer: `apps/venue` EquipmentView Hires list + board.* |
| `venue_mark_equipment_out(p_venue_token, p_hire_id)` | `venueMarkEquipmentOut(token, hireId)` | anon, authenticated | **WRITE** (mig 259, Cycle 3, EV-covered). `confirmed → out` (+ `handed_out_at`). Idempotent (`already:true` if already out); `invalid_transition` from any other state. Audit `equipment_handed_out`. *Consumer: `apps/venue` EquipmentView Hires "Hand out".* |
| `venue_mark_equipment_returned(p_venue_token, p_hire_id, p_condition?, p_forfeit_deposit?)` | `venueMarkEquipmentReturned(token, hireId, {condition?, forfeitDeposit?})` | anon, authenticated | **WRITE** (mig 259, Cycle 3, EV 7/7 + leak 0). `confirmed/out → returned` (+ `returned_at`). Resolves a held deposit → `released` (default) or `forfeited` (`p_forfeit_deposit`). Optional `p_condition` is validated + **written back to the catalogue item** (`equipment.condition`). `invalid_transition`/`invalid_condition` guards. Audit `equipment_returned`. *Consumer: `apps/venue` EquipmentView ReturnModal.* |
| `venue_equipment_insights(p_venue_token, p_from?, p_to?)` | `venueEquipmentInsights(token, {from?, to?})` | anon, authenticated | **READ** (mig 260, Cycle 5 — the data-product tail. READ-ONLY → ephemeral-verify N/A; rpc-security-sweep PASS; live `BEGIN…ROLLBACK` revenue-join probe ALL PASS + leak 0). STABLE SECDEF, `resolve_venue_caller`. Three intelligence blocks over the venue's equipment: **`roi[]`** per asset **lifetime** (`purchase_price_pence` vs `collected_pence` = net non-voided `venue_payments` on the hire's `source_type='equipment'` charge, + `billed_pence`, `payback_pct`, `payback_status` recouped/partial/not_started/unknown, `idle`); **`usage[]`** per asset over `[from,to]` (default trailing 90d — `hires_count`/`units_hired`/`unit_hours`/`busiest_day`/`share_pct`, no fabricated denominator); **`procurement[]`** from `equipment_demand_misses` grouped by category (`miss_count`/`units_wanted`/`last_miss_at` vs currently-`owned_qty`). "Hired" = confirmed/out/returned/overdue. *Consumer: `apps/venue` EquipmentView InsightsPanel.* **Future consumer (Hard Rule #14):** venue-Gaffer "what should I buy next?" — returns a single jsonb to pass verbatim as `<context>`; any return-shape change must re-check that surface. |
| `_equipment_peak_committed(p_equipment_id, p_start, p_end)` | _(internal — no JS wrapper)_ | postgres/service_role only | **Internal** (STABLE, search_path pinned, **revoked from anon/authenticated**) — mig 257. Peak concurrent committed (`confirmed`/`out`) qty in `[start,end)`: the availability primitive. Called by `get_equipment_availability` + `venue_create_equipment_hire`. |
| `venue_create_space(p_venue_token, p_name, p_capacity, p_space_type, p_description?, p_is_enquiry_only?, p_enquiry_contact_name?, p_enquiry_contact_email?)` | `venueCreateSpace(token, {name, capacity, spaceType, description?, isEnquiryOnly?, enquiryContactName?, enquiryContactEmail?})` | anon, authenticated | **WRITE** (mig 338, Classes+Room-Hire Phase 1; EV 14/14 + leak 0, security-sweep PASS). Creates a `venue_spaces` row (the bookable facility). `space_type ∈ studio/room/hall/outdoor`; validates type/name/non-negative capacity. `resolve_venue_caller` → `venue_id`. Audit `venue_space_created` (`entity_type='venue_space'`). *Consumer: `apps/venue` SpacesView.* |
| `venue_update_space(p_venue_token, p_space_id, p_updates)` | `venueUpdateSpace(token, spaceId, updates)` | anon, authenticated | **WRITE** (mig 338; EV 14/14 + leak 0). Partial jsonb patch (presence-keyed; only supplied keys change). Ownership-checked → `space_not_found`. Validates `space_type`/`capacity` when present. Audit `venue_space_updated`. *Consumer: `apps/venue` SpacesView.* |
| `venue_list_spaces(p_venue_token)` | `venueListSpaces(token)` | anon, authenticated | **READ** (mig 338). STABLE SECDEF. jsonb array of the caller's spaces + `upcoming_session_count`/`upcoming_hire_count`. Counts are emitted via dynamic SQL guarded by `to_regclass` — **0 until Phases 2/5 add `venue_class_sessions`/`venue_room_hires`, then self-upgrade with no body change.** *Consumer: `apps/venue` SpacesView.* |
| `_space_is_available(p_space_id, p_starts_at, p_ends_at)` | _(internal — no JS wrapper)_ | postgres/service_role only | **Internal** (STABLE, search_path pinned, **revoked from anon/authenticated**) — mig 338. The shared double-booking guard: false if any non-cancelled class session OR room hire overlaps `[start,end)` on the space. References to `venue_class_sessions`/`venue_room_hires` are `to_regclass`-guarded so it creates & runs in Phase 1 (nothing to conflict with). **Future consumers (Hard Rule #14): Phase 2 `venue_schedule_class_session` + Phase 5 `member_request_room_hire`/`public_enquire_room_hire` call this before booking — built here so neither phase recreates it.** |
| `venue_create_class_type(p_venue_token, p_name, p_space_id, p_duration_minutes, p_default_capacity, p_category, p_cancellation_cutoff_hours?, p_first_session_free?, p_description?)` | `venueCreateClassType(token, {name, spaceId, durationMinutes, defaultCapacity, category, cancellationCutoffHours?, firstSessionFree?, description?})` | anon, authenticated | **WRITE** (mig 339, Classes+Room-Hire Phase 2; EV 19/19 + leak 0, security-sweep PASS). Creates a `venue_class_types` row (catalogue template). `category ∈ fitness/yoga/dance/martial_arts/other`. Space ownership-checked → `space_not_found`. Audit `venue_class_type_created`. *Consumer: `apps/venue` ClassesView.* |
| `venue_update_class_type(p_venue_token, p_class_type_id, p_updates)` | `venueUpdateClassType(token, classTypeId, updates)` | anon, authenticated | **WRITE** (mig 339). Partial jsonb patch (presence-keyed). Ownership-checked → `class_type_not_found`. Validates category/duration/capacity/space when present. Audit `venue_class_type_updated`. *Consumer: `apps/venue` ClassesView.* |
| `venue_list_class_types(p_venue_token)` | `venueListClassTypes(token)` | anon, authenticated | **READ** (mig 339, STABLE SECDEF). jsonb array of the caller's types + `space_name` + `upcoming_session_count`. *Consumer: `apps/venue` ClassesView.* |
| `venue_schedule_class_session(p_venue_token, p_class_type_id, p_instructor_id, p_starts_at, p_price_pence, p_payment_mode)` | `venueScheduleClassSession(token, {classTypeId, instructorId, startsAt, pricePence, paymentMode})` | anon, authenticated | **WRITE** (mig 339). One-off session. `ends_at = starts_at + duration`; capacity/space inherited from the type. `payment_mode ∈ prepay/door/both`. Instructor must be an `active` `venue_admins` row in the venue → `instructor_not_found`. **Calls `_space_is_available` → rejects `space_unavailable` on overlap.** Audit `venue_class_session_scheduled`. *Consumer: `apps/venue` ClassesView.* |
| `venue_create_class_series(p_venue_token, p_class_type_id, p_instructor_id, p_day_of_week, p_start_time, p_series_start, p_price_pence, p_payment_mode, p_series_end?)` | `venueCreateClassSeries(token, {classTypeId, instructorId, dayOfWeek, startTime, seriesStart, seriesEnd?, pricePence, paymentMode})` | anon, authenticated | **WRITE** (mig 339). Pre-generates one session per matching weekday (`day_of_week` 0=Sun…6=Sat) from `series_start` to `series_end` (or a bounded 180-day horizon when open-ended). Each slot is `_space_is_available`-checked; conflicts are **skipped, not failed**. Returns `{ok, series_id, sessions_created, sessions_skipped}`. Audit `venue_class_series_created`. Pattern mirrors `club_create_session_series` (mig 302). *Consumer: `apps/venue` ClassesView.* |
| `venue_cancel_class_session(p_venue_token, p_session_id, p_reason?)` | `venueCancelClassSession(token, sessionId, reason?)` | anon, authenticated | **WRITE** (mig 339). Venue-side cancel — no cutoff. Flips session→`cancelled`. **Forward-guarded (`to_regclass venue_class_bookings`): VOIDS+REFUNDS every class `venue_charges` row for the session's bookings (`status='refunded'`), cancels those bookings, queues a `class_cancelled` `notification_log` per member.** Idempotent (`already`). Cascade is a no-op until Phase 3 lands bookings. Audit `venue_class_session_cancelled`. *Consumer: `apps/venue` ClassesView.* |
| `venue_cancel_class_series(p_venue_token, p_series_id, p_reason?)` | `venueCancelClassSeries(token, seriesId, reason?)` | anon, authenticated | **WRITE** (mig 339). Bulk-cancels future scheduled sessions of a series + same refund/notify cascade; series→`is_active=false`. Returns `{ok, sessions_cancelled, refunded}`. Audit `venue_class_series_cancelled`. *Consumer: `apps/venue` ClassesView.* |
| `venue_reassign_class_instructor(p_venue_token, p_session_id, p_new_instructor_id)` | `venueReassignClassInstructor(token, sessionId, newInstructorId)` | anon, authenticated | **WRITE** (mig 339). Reassigns a `scheduled` session's instructor (new instructor must be `active` in the venue). Forward-guarded `class_instructor_changed` notify. Audit `venue_class_instructor_reassigned`. *Consumer: `apps/venue` ClassesView.* |
| `venue_list_class_sessions(p_venue_token, p_from?, p_to?)` | `venueListClassSessions(token, {from?, to?})` | anon, authenticated | **READ** (mig 339, STABLE SECDEF). jsonb array of sessions in the window + class/space/instructor names + `booked_count`/`waitlist_count` (dynamic `to_regclass`-guarded → 0 until Phase 3). *Consumer: `apps/venue` ClassesView.* |
| `venue_get_class_session_detail(p_venue_token, p_session_id)` | `venueGetClassSessionDetail(token, sessionId)` | anon, authenticated | **READ** (mig 339, STABLE SECDEF). Session core + `attendees[]` (forward-guarded → empty until Phase 3). *Consumer: `apps/venue` ClassesView.* |
| `venue_mark_class_completed(p_venue_token, p_session_id)` | `venueMarkClassCompleted(token, sessionId)` | anon, authenticated | **WRITE** (mig 339). Session→`completed` + `completed_at`. **Forward-guarded no-show capture: every still-`confirmed` booking with no `checked_in_at` flips to `no_show` and `member_profiles.no_show_count` is bumped.** Both the bookings table + `no_show_count` land Phase 3; `checked_in_at` lands Phase 6 — all probed at runtime (`to_regclass` + `information_schema`), so **no later edit needed. Consumer contract (Hard Rule #14): Phase 6 check-in MUST stamp `venue_class_bookings.checked_in_at`, else attendees are wrongly no-showed.** Idempotent (`already`); errors `session_cancelled`. Audit `venue_class_session_completed`. *Consumer: `apps/venue` ClassesView.* |
| **Charge contract (Hard Rule #14):** Phase 3 `member_book_class_session` MUST write its `venue_charges` row as `source_type='class'`, `source_id = <venue_class_bookings.id>::text` — the mig-339 refund cascades key on exactly that shape. **SATISFIED in mig 340** via `_apply_class_booking_charge`. |
| `venue_class_checkin(p_venue_token, p_session_id, p_pass_token)` | `venueClassCheckin(token, sessionId, passToken)` | anon, authenticated (token-resolved) | **WRITE** (mig 343, Classes+Room-Hire Phase 6; EV 12/12 + leak 0, security-sweep PASS). QR check-in. Gate: the session's assigned instructor (caller's `venue_admins.id` via `auth.uid()` = `session.instructor_id`) **OR** a venue manager (`_venue_has_cap(role,grant,deny,'manage_facility')` — owner/manager auto-true) **OR** platform admin; else `not_instructor`. Resolves the member from the scanned pass: normalises a `/m/<token>` URL or bare token (reuses mig-274 strip), looks up `venue_memberships.pass_token` → `member_profile_id` + `venue_id`, then the `(session_id, member_profile_id)` booking. **Stamps `venue_class_bookings.checked_in_at` (satisfies the mig-339 Phase-6 consumer contract)** and promotes any non-cancelled booking (`waitlist`/`offered`/`confirmed`) → `confirmed`, clearing `waitlist_position`/`offer_expires_at`. **Attendance is booking-row-only — does NOT write `venue_member_checkins` (reception arrival is a separate concern) and does NOT touch `venue_charges`** (door payment is physical; prepay charged at booking — mirrors reception check-in). Graceful `{ok:false,reason}` per-scan: `no_token`/`pass_not_found`/`wrong_venue`/`not_booked`/`booking_cancelled`; `{ok:true,already_checked_in}` on re-scan. RAISE (operator/authz): `invalid_venue_token`/`session_not_found`/`session_cancelled`/`session_completed`/`not_instructor`. Audit `venue_class_checkin` (Hard Rule #9), metadata incl. `promoted`. *Consumer: `apps/venue` ClassCheckinScanner (full-screen BarcodeDetector, ports apps/display CheckInOverlay).* |
| `member_list_class_sessions(p_venue_id, p_from?, p_to?)` | `memberListClassSessions(venueId, {from,to})` | **anon, authenticated** | Read (mig 340). Public "What's on" timetable — scheduled future sessions for a venue (+ spots_left/booked/waitlist counts, instructor, space). Anon-callable (the public club-site surface); an authenticated caller with a `member_profile` also gets per-session `my_status`/`my_booking_id`/`my_waitlist_position`. Default window now→+28d. *Consumers: `apps/inorout` VenueLanding (ClassesTimetable).* |
| `member_book_class_session(p_session_id)` | `memberBookClassSession(sessionId)` | **authenticated** | **WRITE** (mig 340, EV-gated). `auth.uid()`→`member_profiles`. Gates: active membership (`venue_memberships` status∈active/ending), no-show suspension (`venues.no_show_suspension_threshold` vs `member_profiles.no_show_count`→`{ok:false,reason:'suspended'}`), prepay-dormancy (payment_mode='prepay' + no stripe/connected `venue_integrations`→`{ok:false,reason:'payment_method_unavailable'}`). Confirmed if capacity free else waitlist (`waitlist_position`). Confirmed bookings call `_apply_class_booking_charge`: tier `discount_pct` reduces / `included_sessions` or `first_session_free` (first booking at venue) or £0 waives → else `venue_charges` row `source_type='class'`,`source_id=booking_id::text`,`status='unpaid'`. Re-books a prior cancelled row (UNIQUE(session,member)). `already_booked` if active. Audit `member_class_booked` (`actor_type='player'`). *Consumers: VenueLanding (ClassesTimetable).* |
| `member_cancel_class_booking(p_booking_id)` | `memberCancelClassBooking(bookingId)` | **authenticated** | **WRITE** (mig 340; **rewired mig 341**, EV-gated). Ownership-checked (`booking_not_found`). Enforces `cancellation_cutoff_hours` on confirmed bookings (`cutoff_passed`); waitlist/offered cancel anytime. Refunds any class `venue_charges` (`status='refunded'`). **Phase 4: a freed seat (cancelling a `confirmed` OR a live `offered` booking) now OFFERS the next waitlister via `_offer_next_waitlist_spot` — no longer straight-promotes.** Queues `class_booking_cancelled`. Return shape: `{ok, booking_id, refunded, offered}` (was `promoted` — Hard Rule #7, no JS/SQL consumer read it). Audit `member_class_cancelled`. *Consumers: MemberPass (Upcoming classes).* |
| `member_claim_waitlist_spot(p_session_id)` | `memberClaimWaitlistSpot(sessionId)` | **authenticated** | **WRITE** (mig 341, EV-gated). The notify-and-claim tap. `auth.uid()`→`member_profiles`; row-locks the caller's booking (`FOR UPDATE`); requires a live `offered` row (`status='offered'` AND `offer_expires_at > now()`) on a still-bookable session, else graceful `{ok:false, reason:'spot_taken'}`. Defensive capacity re-check (excludes the caller's held seat). Promotes `offered`→`confirmed`, clears `offer_expires_at`, applies `_apply_class_booking_charge`. Audit `member_class_claimed`. *Consumers: MemberPass (Claim spot).* |
| `member_list_my_class_bookings(p_venue_id?)` | `memberListMyClassBookings(venueId?)` | **authenticated** | Read (mig 340; **mig 341** adds `offer_expires_at` + `'offered'` counts as upcoming). The caller's bookings (optional venue scope) with session/class/space/venue + payment_status + `is_upcoming` flag (splits Upcoming vs History client-side). *Consumers: MemberPass (Upcoming + claim countdown), MemberProfile (My class history).* |
| `_apply_class_booking_charge(p_booking_id)` | *(internal — no wrapper)* | **service_role/postgres only** | Internal SECURITY DEFINER helper (mig 340). Single source of truth for waive-vs-charge on a `confirmed` booking; called by `member_book_class_session`, `member_claim_waitlist_spot`, + (legacy) the `member_cancel_class_booking` path. REVOKEd from anon/authenticated. |
| `_offer_next_waitlist_spot(p_session_id)` | *(internal — no wrapper)* | **service_role/postgres only** | Internal SECURITY DEFINER helper (mig 341). Single source for "freed seat → offer the front waitlister": if `confirmed + live offers < capacity` on a future scheduled session, flips the front `waitlist` row → `offered` with `offer_expires_at = now() + venues.class_claim_window_minutes` and queues a `class_spot_offered` notification. Called by `member_cancel_class_booking` + `expire_class_waitlist_offers`. REVOKEd from anon/authenticated. |
| `expire_class_waitlist_offers()` | *(none — cron only)* | **service_role** | Cron tick (mig 341). Expired `offered` bookings (`offer_expires_at <= now()`) roll to the BACK of their session's waitlist, then each affected session re-offers its front waitlister via `_offer_next_waitlist_spot`. Returns `{expired, reoffered}`. *Consumer: `apps/inorout/api/cron.js classWaitlistExpiryJob`.* |
| **Class packages & trials — Classes+Room-Hire Phase 7 (mig 344). All SECURITY DEFINER, search_path pinned, single overload. EV 6/6 + leak 0, security-sweep PASS.** | | | |
| `venue_create_class_package(p_venue_token, p_name, p_session_count, p_price_pence, p_valid_days?)` | `venueCreateClassPackage(token, {name,sessionCount,pricePence,validDays})` | anon, authenticated (token-resolved) | **WRITE** (mig 344). Creates a `venue_class_packages` row (a prepaid pass: N sessions for a price, optional `valid_days` expiry). Validates `name_required`/`bad_session_count`/`bad_price`/`bad_valid_days`. Audit `venue_class_package_created`. *Consumer: `apps/venue` ClassesView (Packages sub-tab).* |
| `venue_list_class_packages(p_venue_token)` | `venueListClassPackages(token)` | anon, authenticated (token-resolved) | **READ** (mig 344). STABLE. The venue's packages, each with a nested `balances[]` array of active (non-empty, unexpired) member balances (`member_name`/`member_email`/`sessions_remaining`/`expires_at`) for the per-member-balances UI. *Consumer: ClassesView (Packages sub-tab).* |
| `member_list_class_packages(p_venue_id)` | `memberListClassPackages(venueId)` | **anon, authenticated** | **READ** (mig 344). STABLE. Public menu of a venue's active packages (`id`/`name`/`session_count`/`price_pence`/`valid_days`) — readable without login, mirrors the public timetable. Drives the "Buy a class pass" sheet. *Consumer: `apps/inorout` ClassesTimetable.* |
| `member_purchase_class_package(p_package_id)` | `memberPurchaseClassPackage(packageId)` | **authenticated** | **WRITE** (mig 344, EV-gated). `auth.uid()`→`member_profiles`. Requires active membership at the package's venue → else `{ok:false,reason:'membership_required'}`. **NOT Stripe-gated** (mirrors memberships/merchandise): creates a `venue_member_package_balances` row (`sessions_remaining=session_count`, `expires_at` from `valid_days`) granting credits immediately + raises an `unpaid` `venue_charges` row `source_type='class_package'`,`source_id=balance_id::text` (if price>0; collected door/reception now, Stripe once connected). Returns `{ok,balance_id,sessions_remaining,expires_at,charge_pence}`. Audit `member_class_package_purchased` (`actor_type='player'`). *Consumer: ClassesTimetable (Buy a class pass).* |
| `member_get_package_balance(p_venue_id?)` | `memberGetPackageBalance(venueId?)` | **authenticated** | **READ** (mig 344). STABLE. `auth.uid()`→`member_profiles`; returns the caller's active (non-empty, unexpired) balances (`balance_id`/`venue_name`/`package_name`/`sessions_remaining`/`expires_at`). `p_venue_id=NULL` → all venues (member pass); set → one venue (timetable credit pill). *Consumers: MemberPass (Class passes), ClassesTimetable.* |
| **Class-package precedence (mig 344):** `_apply_class_booking_charge` re-issued with a **waiver > package > charge** order — a waived session (tier `included_sessions`/`first_session_free`/£0) never burns a paid credit; else a valid balance (soonest-expiring, `FOR UPDATE`) is decremented and the booking marked `payment_status='paid'`,`package_balance_id=<bal>` with NO charge; else the normal `class` charge. `member_cancel_class_booking` re-issued: restores the credit (`+1`) on cancel within cutoff (`credit_restored` in return + audit). `venue_cancel_class_session`/`_series` re-issued: restore burned credits for every cancelled booking (a venue cancel must not strand member money). Return shapes otherwise unchanged (Hard Rule #7). |
| **Room hire — Classes+Room-Hire Phase 5 (mig 342). All SECURITY DEFINER, search_path pinned, single overload. EV 13/13 + leak 0.** | | | |
| `member_request_room_hire(p_space_id, p_starts_at, p_ends_at, p_purpose, p_attendee_count?, p_equipment_ids?)` | `memberRequestRoomHire(spaceId, {startsAt,endsAt,purpose,attendeeCount,equipmentIds})` | **authenticated** (anon REVOKED) | **WRITE** (mig 342). `auth.uid()`→`member_profiles`. Self-serve hire request. Calls `_space_is_available` → `{ok:false,reason:'space_unavailable'}` on overlap (with class sessions OR other live hires); per-member open-request throttle → `'too_many_requests'`. Creates `venue_room_hires` `booker_type='member'` status `requested`; links optional equipment add-ons as `requested` `equipment_bookings` (only venue's own kit, no charge). Queues `room_hire_requested` ack. Audit `room_hire_requested` (`actor_type='player'`). *Consumer: `apps/inorout` VenueLanding (HireSpace).* |
| `public_enquire_room_hire(p_space_id, p_name, p_email, p_phone, p_starts_at, p_ends_at, p_purpose, p_attendee_count?)` | `publicEnquireRoomHire(spaceId, {name,email,phone,startsAt,endsAt,purpose,attendeeCount})` | **anon, authenticated** | **WRITE — FIRST anon write in this epic** (mig 342). Restricted to `is_enquiry_only` spaces (`not_enquiry_only` reject); no charge; validates + length-caps name(120)/email(160)/phone(40)/purpose(500) (`name_required`/`bad_email`/`purpose_required`/`input_too_long`); per-email+space 10-min throttle → `'too_many_requests'`. Creates `booker_type='non_member'` `requested`. Queues `room_hire_requested` ack. Audit `room_hire_enquired` **`actor_type='system'`, `actor_user_id=NULL`** (anon-safe — the whitelist has no `anon`). Returns only `{ok, hire_id}` (no venue internals). *Consumer: VenueLanding (HireSpace contact form).* |
| `venue_list_room_hires(p_venue_token, p_status?)` | `venueListRoomHires(token, status?)` | anon, authenticated (token-resolved) | **READ** (mig 342). STABLE. Caller's hires (optional status filter), requests first then by `starts_at`; each row + space name/type, booker, latest `charge_status`, and `equipment[]` add-ons (joined `equipment_bookings`+`equipment`). *Consumer: `apps/venue` RoomHiresView.* |
| `venue_confirm_room_hire(p_venue_token, p_hire_id, p_price_pence, p_deposit_pence?)` | `venueConfirmRoomHire(token, hireId, pricePence, depositPence?)` | anon, authenticated (token-resolved) | **WRITE** (mig 342). `requested`→`confirmed`; prices it; raises a `venue_charges` row `source_type='room_hire'`,`source_id=hire_id::text`,`status='unpaid'` (if price>0; idempotent vs non-refunded); confirms linked equipment add-ons; notifies booker (`room_hire_confirmed`). `not_confirmable`/`bad_price`/`hire_not_found`/`invalid_venue_token`. Audit `room_hire_confirmed`. *Consumer: RoomHiresView.* |
| `venue_cancel_room_hire(p_venue_token, p_hire_id, p_reason?)` | `venueCancelRoomHire(token, hireId, reason?)` | anon, authenticated (token-resolved) | **WRITE** (mig 342). status→`cancelled`; refunds the `room_hire` charge (`status='refunded'`); cancels linked equipment add-ons; a `held` deposit → `returned`; notifies booker (`room_hire_cancelled`). Idempotent (`{ok,already}`). Returns `{ok, refunded, deposit_status}`. Audit `room_hire_cancelled`. *Consumer: RoomHiresView.* |
| `venue_record_hire_deposit(p_venue_token, p_hire_id, p_deposit_status)` | `venueRecordHireDeposit(token, hireId, depositStatus)` | anon, authenticated (token-resolved) | **WRITE** (mig 342). Transitions `deposit_status` ∈ none/held/returned/forfeited (`bad_deposit_status` otherwise). Row-tracked — no separate ledger row (mirrors equipment-hire deposits). Audit `room_hire_deposit_recorded`. *Consumer: RoomHiresView.* |
| `member_list_hireable_spaces(p_venue_id)` | `memberListHireableSpaces(venueId)` | **anon, authenticated** | **READ** (mig 342). Active spaces for a venue + `is_enquiry_only` + enquiry contact — powers the public "Hire a space" cards. *Consumer: VenueLanding (HireSpace).* |
| `member_list_my_room_hires(p_venue_id?)` | `memberListMyRoomHires(venueId?)` | **authenticated** (anon REVOKED) | **READ** (mig 342). `auth.uid()`→`member_profiles`; the caller's hires (optional venue scope) + `is_upcoming` flag. Returns `[]` when no profile. *Consumer: `apps/inorout` MemberPass (Room hires).* |
| `create_renewal_holds()` | *(none — cron only)* (Stage 7) | **service_role** | Reserves the next block for any series ending ≤21d (mirror original length; holds `pitch_bookings.status='hold'` + active occupancy priority 2; origin→`ending`). Skips if the slot's taken. Audit `system`/`booking_renewal_held`; notify both channels. mig 152. *Consumer: `apps/inorout/api/cron.js renewalHoldsJob`.* |
| `confirm_renewal(p_series_id)` | `confirmRenewal(seriesId)` (Stage 7) | **authenticated** | Team "keep my slot": `auth.uid()`→`team_admins`, flips held weeks `hold`→`requested` (venue re-approves via the inbox), origin→`cancelled`. Rejects `renewal_lapsed`/`not_team_admin`. Fires `booking_requested`. mig 152. *Consumer: `apps/inorout` ScheduleScreen.* |
| `expire_renewal_holds()` | *(none — cron only)* (Stage 7) | **service_role** | Releases holds past `hold_expires_at` (occupancy off, `hold`→`expired`, series→`cancelled`). Audit `system`/`booking_renewal_expired`; notify both. mig 152. *Consumer: `cron.js renewalHoldsJob`.* |
| `get_team_admin_player_ids(p_team_id)` | *(none — cron only)* (Stage 7) | **service_role** | Returns the team's active-admin player ids (`team_admins`→`players.user_id`) for push targeting. mig 152. *Consumer: `cron.js` push helpers.* |
| `venue_list_staff(p_venue_token)` | `venueListStaff(token)` | anon, authenticated | Read. Returns `{ ok, staff: [...] }` from `venue_staff` for the caller's venue (token→`resolve_venue_caller`). mig 195. *Consumer: `apps/venue` StaffView (Venue Staff section).* |
| `venue_add_staff(p_venue_token, p_staff)` | `venueAddStaff(token, staff)` | anon, authenticated | **WRITE** (mig 195, ephemeral-verify gated). Inserts a `venue_staff` row (role ∈ reception/manager/admin/groundstaff/coach/other). Validates name + role. Audit `staff_added`; `notify_venue_change('staff_added')`. *Consumer: `apps/venue` StaffMemberForm.* |
| `venue_update_staff(p_venue_token, p_staff_id, p_updates)` | `venueUpdateStaff(token, id, updates)` | anon, authenticated | **WRITE** (mig 195, ephemeral-verify gated). Patches a venue-owned `venue_staff` row (ownership-checked → `staff_not_found`). Audit `staff_updated`; `notify_venue_change('staff_updated')`. *Consumer: `apps/venue` StaffMemberForm.* |
| `venue_get_team_roster(p_venue_token, p_team_id)` | `venueGetTeamRoster(token, teamId)` | anon, authenticated | Read. Returns `{ ok, team, players[], competitions[] }` for a team that plays in one of the caller's competitions (ownership via competition_teams→competitions→seasons→leagues→venue_id → `team_not_in_venue`). Excludes token/user_id/phone. mig 196. *Consumer: `apps/venue` TeamDetail.* |
| `venue_get_standings(p_venue_token, p_competition_id)` | `venueGetStandings(token, compId)` | anon, authenticated | Read. League table for a round-robin competition, computed from completed/walkover/forfeit fixtures (walkover/forfeit = 3-0). Ranked pts→gd→gf→name. Ownership via competition→season→league→venue_id → `competition_not_in_venue`. mig 197. *Consumer: `apps/venue` LeagueTable.* |
| `venue_list_players(p_venue_token)` | `venueListPlayers(token)` | anon, authenticated | Read. Every player across all teams in the caller's competitions (one row per player·team), with team name/colour + stats. Excludes token/user_id/phone. mig 198. *Consumer: `apps/venue` PlayersView.* |
| `league_list_teams(p_league_token)` | `leagueListTeams(token)` | anon, authenticated | Read. Teams across the league's competitions (id/name/colours) — league_get_state has no teams map, so the league app uses this for fixture name resolution + Teams view. resolve_league_caller → `invalid_league_token`. mig 199. *Consumer: `apps/league`.* |
| `league_get_standings(p_league_token, p_competition_id)` | `leagueGetStandings(token, compId)` | anon, authenticated | Read. League table by league_admin_token (same maths as venue_get_standings); ownership via competition→season.league_id → `competition_not_in_league`. mig 200. *Consumer: `apps/league` LeagueTable.* |
| `league_update_fixture_result(p_league_token, p_fixture_id, p_home_score, p_away_score, p_reason)` | `leagueUpdateFixtureResult(token, fixtureId, h, a, reason)` | anon, authenticated | **WRITE** (mig 201, ephemeral-verify gated 8/8 + live UI round-trip). League-admin correction of a **completed** fixture's score (mirrors venue_update_fixture_result). Ownership via competition→season.league_id → `fixture_not_in_league`; rejects non-completed/negative/missing-reason. Audits prev+new; notifies both teams + league + venue. *Consumer: `apps/league` ResultModal.* |
| `league_update_fixture_status(p_league_token, p_fixture_id, p_new_status, p_metadata)` | `leagueUpdateFixtureStatus(token, fixtureId, status, meta)` | anon, authenticated | **WRITE** (mig 202, ephemeral-verify gated). Postpone/void/walkover/forfeit (mirrors venue_update_fixture_status mig 096); same transition rules + reason/winner validation. Ownership via competition→season.league_id. Audits; notifies league+venue+teams. *Consumer: `apps/league` FixtureManageModal.* |
| `league_reschedule_fixture(p_league_token, p_fixture_id, p_scheduled_date, p_kickoff_time, p_reason)` | `leagueRescheduleFixture(token, fixtureId, date, time, reason)` | anon, authenticated | **WRITE** (mig 203, ephemeral-verify gated). Moves a fixture's date/time (from scheduled/allocated/postponed → `cannot_reschedule` otherwise); rescheduling a postponed fixture revives it (→allocated if it has a pitch, else scheduled) and clears postpone_reason. Audits prev+new. *Consumer: `apps/league` FixtureManageModal.* |

---

## OPS / INTERNAL CRON RPCs (no JS wrapper — called directly from api/cron.js)

| SQL function | Grant | Notes |
|---|---|---|
| `expire_staff_dbs()` | service_role only (anon + authenticated **revoked explicitly** — Supabase auto-grants both; requires manual REVOKE in mig 305) | **Migration 305.** **WRITE.** UPDATE `club_staff_dbs` SET status='expired' WHERE status='valid' AND expiry_date < CURRENT_DATE. Returns `{expired: int}`. SECURITY DEFINER, `search_path=public`. No JS wrapper — called via `supabase.rpc()` directly in `api/cron.js` `dbsExpiryJob` (08:00 UK, time-window guard 15min). *Consumer: `apps/inorout/api/cron.js` only.* |
| `get_ops_usage_digest(p_from date, p_to date, p_prev_from date?, p_prev_to date?)` | service_role only (anon + authenticated **revoked**) | **Migration 234.** STABLE, read-only. Operator-only "is the casual app being used?" analytics over a window `[p_from,p_to]` (UK dates, inclusive) + an optional previous window for the wk/wk delta. Returns one jsonb: `squads{total,active,new[]}`, `players{total,new,disabled_now,disabled_in_window,deleted_in_window}`, `activity{total_events,app_opens,active_players,availability_marks,bookings,by_action[]}`, `dormancy[{id,name,last_active,days_since}]`, `prev{total_events,active_players}`. **Real squads only** — demo (`team_demo%`) AND demo-company league seed (`team_dc%`) stripped in SQL per Hard Rule #15, so the numbers are genuine onboarded squads. SECURITY DEFINER, `search_path=public`. No JS wrapper — called via `supabase.rpc()` directly in `api/cron.js` (same pattern as `hq_get_analytics_for_company`/`weeklyDigestJob`), so it never lands in `packages/core`. **Consumers (Hard Rule #14)**: `api/cron.js` `opsDailyDigestJob` (Tue–Sun 08:00 UK, previous day) + `opsWeeklyDigestJob` (Mon 08:00 UK, previous week) → `_mailer.js` `opsDailyDigest`/`opsWeeklyDigest` templates → `OPS_DIGEST_EMAIL` (defaults to operator address); **planned**: Phase 7 Gaffer AI-narration layer reads this same shape for the prose digest. Return-shape changes must check both cron.js templates AND that future consumer. |

| **Phase 0 — Event OS: Account Relationship Routing (mig 314, session 117). Four read-only RPCs. All SECURITY DEFINER, authenticated-only, anon revoked. Security sweep 4/4 PASS.** | | | |
| `get_user_relationships()` | `getUserRelationships()` | **authenticated** (anon REVOKED) | **READ (mig 314).** Routing oracle — called on app load to determine home screen type. Auth: `auth.uid()`. Returns `{squads:[{team_id,name,player_token,game_date_time,game_is_live}], club_memberships:[{club_id,name,cohort_id,cohort_name}], guardian_of:[{player_profile_id,first_name,last_name}], competitions:[{competition_id,name,team_id,status}], admin_roles:[{type:'club_admin',entity_id,team_id,role}]}`. Derives homeScreenType from presence of squads/clubs/guardian_of. *Consumers: apps/inorout App.jsx (auth load), planned UnifiedFeedScreen/ParentHomeScreen.* |
| `get_unified_home_feed()` | `getUnifiedHomeFeed()` | **authenticated** (anon REVOKED) | **READ (mig 314).** 14-day chronological feed (max 30 events) across all active relationships. Returns `{events:[{type, title, subtitle, when_at, entity_id, entity_type, ...type-specific fields}]}`. Four event types: `squad_game`, `club_session` (+ `own_rsvp`), `competition_fixture`, `child_event` (+ `child_name`, `child_profile_id`). Ordered by `when_at` ASC. *Consumer: planned apps/inorout UnifiedFeedScreen.* |
| `get_guardian_home_feed()` | `getGuardianHomeFeed()` | **authenticated** (anon REVOKED) | **READ (mig 314).** Parent-specific feed. Returns `{children:[{profile_id,first_name,last_name,next_session:{session_id,title,scheduled_at,location,club_name,own_rsvp}}]}`. Each child's next upcoming club session only — competition fixtures for children deferred to Phase 2. *Consumer: planned apps/inorout ParentHomeScreen.* |
| `get_child_live_match(p_player_profile_id uuid)` | `getChildLiveMatch(playerProfileId)` | **authenticated** (anon REVOKED) | **READ (mig 314).** Follow Live — returns in_progress fixture for a guardian's child. Guards: auth.uid()→member_profiles→member_guardians edge (raises `not_guardian`). Returns `{ok:false, reason:'no_live_match'}` when child has no auth account or no live fixture (not an error). When live: `{ok:true, fixture:{id,competition,my_team_id,home_team_id/name/colour,away_team_id/name/colour,home_score,away_score}, venue_id, events:[{event_type,minute,period,team_id,player_id}]}`. *Consumer: planned apps/inorout FollowLiveView.* |
| **Phase 1 — Event OS: Tournament management (mig 316, session 119). Three club-manager RPCs. Auth: auth.uid()→member_profiles→club_team_managers (same pattern as mig 314). All SECURITY DEFINER, authenticated-only, anon revoked. Security sweep 3/3 PASS.** | | | |
| `club_admin_create_tournament(p_club_id, p_venue_id, p_name, p_slug, p_event_date, p_event_end_date?, p_entry_fee_pence?, p_entry_fee_payer?, p_registration_deadline?)` | `clubAdminCreateTournament(clubId, venueId, name, slug, eventDate, opts)` | **authenticated** (anon REVOKED) | **WRITE (mig 316).** Creates a tournament event. Validates: caller is active club_team_manager for p_club_id; club_venues link exists for p_venue_id; slug matches `^[a-z0-9][a-z0-9\-]{1,79}$`; event_date not null; end_date not before start; valid entry_fee_payer. Inserts `tournament_events`; audit `tournament_created` (team_id=club_id). Slug uniqueness enforced by DB UNIQUE constraint. Returns `{ok:true, tournament_id:uuid, slug:text}`. *Consumers: apps/inorout SessionsScreen Tournaments tab.* |
| `club_admin_list_tournaments(p_club_id)` | `clubAdminListTournaments(clubId)` | **authenticated** (anon REVOKED) | **READ (mig 316).** Lists all tournaments for a club. Auth: same club_team_managers check. Returns `[{tournament_id, name, slug, status, event_date, event_end_date, entry_fee_pence, entry_fee_payer, registration_deadline, venue_id, created_at}]` ordered by event_date DESC. Returns `[]` when no tournaments. *Consumers: apps/inorout SessionsScreen Tournaments tab.* |
| `club_admin_get_tournament(p_slug)` | `clubAdminGetTournament(slug)` | **authenticated** (anon REVOKED) | **READ (mig 316).** Full tournament detail by slug. Resolves club_id from tournament, then checks caller is an active manager of that club. Returns all tournament_events columns + `performance_events[]` (ordered by display_order NULLS LAST, scheduled_time NULLS LAST) + `competitions[]` (ordered by name). Raises `tournament_not_found` if slug unknown. *Consumers: planned tournament detail page (Phase 2).* |
| **Phase 2 — Event OS: Public read + status lifecycle (mig 317, session 120). Auth: get_tournament_public = anon+authenticated; club_admin_update_tournament_status = authenticated only. Both SECURITY DEFINER, search_path locked.** | | | |
| `get_tournament_public(p_slug)` | `getTournamentPublic(slug)` | **anon, authenticated** | **READ (mig 317; extended mig 318).** Public tournament page read. JOINs `tournament_events` → `clubs` → `venues`. Returns `{ok:false, reason:'not_found'}` for unknown slugs or status='draft'. **Mig 318 adds** `competitions[]` with `{competition_id, name, type, format, status, teams:[{competition_team_id, team_name, registered_at}]}` (active teams only). *Consumers: apps/inorout TournamentScreen.jsx (/tournament/[slug]).* |
| `club_admin_update_tournament_status(p_slug, p_status)` | `clubAdminUpdateTournamentStatus(slug, status)` | **authenticated** (anon REVOKED) | **WRITE (mig 317).** Status lifecycle transition. Free transitions — no ordering enforced; UI restricts choices shown. Valid values: `draft`, `open`, `closed`, `live`, `completed` (matches DB CHECK constraint). Auth: same club_team_managers pattern as mig 316. Raises: `not_authenticated`, `not_authorised`, `invalid_status`, `tournament_not_found`. Audit `tournament_status_changed` (metadata: slug, old_status, new_status). Returns `{ok:true, slug, status}`. *Consumers: apps/inorout SessionsScreen Tournaments tab (status select on each card).* |
| **Phase 3 — Event OS: Team registration & competition roster (mig 318, session 121). Schema: competitions.season_id nullable + identity CHECK; competition_teams.team_id nullable + team_name col + identity CHECK; CREATE TABLE tournament_invitations. Six new RPCs + CREATE OR REPLACE of get_tournament_public + club_admin_get_tournament. All SECURITY DEFINER, search_path locked. Security sweep 8/8 PASS. EV 11/11 PASS + leak 0. Bug caught+fixed by EV: gen_random_bytes unavailable in search_path — fixed to extensions.gen_random_bytes().** | | | |
| `club_admin_add_competition(p_tournament_event_id, p_name, p_type, p_format?)` | `clubAdminAddCompetition(tournamentEventId, name, type, format?)` | **authenticated** (anon REVOKED) | **WRITE (mig 318, EV-covered).** Adds a competition to a tournament. Validates: caller is active club_team_manager for the tournament's club; name not blank; type ∈ {league,cup,playoff}; format ∈ {round_robin,single_elimination,double_elimination,group_stage}. Inserts `competitions` (season_id=NULL, status='setup'). Audit `tournament_competition_added`. Returns `{ok:true, competition_id}`. *Consumer: apps/inorout SessionsScreen Tournaments tab.* |
| `club_admin_register_team(p_tournament_event_id, p_competition_id, p_team_name)` | `clubAdminRegisterTeam(tournamentEventId, competitionId, teamName)` | **authenticated** (anon REVOKED) | **WRITE (mig 318, EV-covered).** Host club registers one of their own teams straight to active. Validates: same club_team_managers auth; competition belongs to tournament; team_name not blank. Inserts `competition_teams` (team_id=NULL, team_name, status='active'). Audit `tournament_team_registered`. Returns `{ok:true, competition_team_id}`. *Consumer: apps/inorout SessionsScreen Tournaments tab.* |
| `club_admin_send_team_invite(p_tournament_event_id, p_competition_id, p_email?)` | `clubAdminSendTeamInvite(tournamentEventId, competitionId, email?)` | **authenticated** (anon REVOKED) | **WRITE (mig 318, EV-covered).** Generates a 12-char hex invite code (loop until unique; `extensions.gen_random_bytes(6)` — pgcrypto lives in extensions schema). Inserts `tournament_invitations` (status='sent', expires_at=now()+14d, created_by=auth.uid()). Audit `tournament_invite_sent`. Returns `{ok:true, code, invite_id}`. *Consumer: apps/inorout SessionsScreen Tournaments tab invite link generator.* |
| `club_admin_approve_team(p_competition_team_id)` | `clubAdminApproveTeam(competitionTeamId)` | **authenticated** (anon REVOKED) | **WRITE (mig 318, EV-covered).** Approves a pending external team: pending → active. Traverses competition_teams→competitions→tournament_events to resolve club_id; validates caller is active club_team_manager. Raises `team_not_found`, `not_authorised`, `team_not_pending`. Audit `tournament_team_approved`. Returns `{ok:true}`. *Consumer: apps/inorout SessionsScreen Tournaments tab pending queue.* |
| `club_admin_reject_team(p_competition_team_id, p_reason?)` | `clubAdminRejectTeam(competitionTeamId, reason?)` | **authenticated** (anon REVOKED) | **WRITE (mig 318, EV-covered).** Rejects a pending external team: pending → rejected + writes rejection_reason. Same auth traversal as approve. Raises `team_not_pending` if already non-pending. Audit `tournament_team_rejected`. Returns `{ok:true}`. *Consumer: apps/inorout SessionsScreen Tournaments tab pending queue.* |
| `tournament_join_via_invite(p_code, p_team_name)` | `tournamentJoinViaInvite(code, teamName)` | **authenticated** (anon REVOKED) | **WRITE (mig 318, EV-covered).** External team joins via invite link. Validates: auth.uid() non-null; team_name not blank; invite exists (`invite_not_found`); invite status='sent' (`invite_already_used`); invite not expired (`invite_expired` — also marks invite expired). Inserts `competition_teams` (status='pending'); marks invite='accepted'. Audit `tournament_team_joined` (actor_type='player'). Returns `{ok:true, competition_team_id, tournament_name, competition_name}`. *Consumer: apps/inorout TournamentJoinScreen (/tournament/join/:code).* |
| `club_admin_get_tournament(p_slug)` | `clubAdminGetTournament(slug)` | **authenticated** (anon REVOKED) | **READ (mig 316; extended mig 318).** Full tournament detail. **Mig 318 adds** `competitions[].teams[]` — each team: `{competition_team_id, team_name, team_id, status, registered_at, rejection_reason, waitlist_position}` for status ∈ {active,pending,rejected}. *Consumers: apps/inorout SessionsScreen Tournaments tab expand panel.* |
| `club_admin_generate_schedule(p_tournament_event_id, p_competition_id, p_slot_minutes, p_start_time, p_start_date, p_playing_area_ids?)` | `clubAdminGenerateSchedule(tournamentEventId, competitionId, slotMinutes, startTime, startDate, playingAreaIds?)` | **authenticated** (anon REVOKED) | **WRITE (mig 319, EV-covered).** Circle-method round-robin fixture generation. Loads active competition_teams ordered by registered_at; odd N → appends NULL bye. Pairs N/2 fixtures per round, skipping NULL slots. Time advances every pitch_count matches (concurrent batches); pitches cycle via modulo on p_playing_area_ids. INSERTs into fixtures (home/away_competition_team_id, week_number=round, round_name='Round N', kickoff_time, playing_area_id, slot_minutes, status='scheduled'). Validates: not_authenticated, not_authorised, tournament_not_found, competition_not_found, fixtures_already_exist, pitch_not_in_venue, not_enough_teams. Audit `tournament_schedule_generated`. Returns `{ok, fixtures_created, rounds}`. *Consumer: apps/inorout SessionsScreen GenerateScheduleModal.* |
| `club_admin_get_schedule(p_tournament_event_id)` | `clubAdminGetSchedule(tournamentEventId)` | **authenticated** (anon REVOKED) | **READ (mig 319).** Full timetable for a tournament. Returns `{ok, tournament_event_id, venue_playing_areas[{id,name}], competitions[{competition_id, name, type, format, status, fixtures[{fixture_id, round, round_name, home_team_id, home_team_name, away_team_id, away_team_name, scheduled_date, kickoff_time, playing_area_id, pitch_name, slot_minutes, status, ref_token, home_score, away_score}]}]}`. JOINs competition_teams ht/att for names, playing_areas for pitch_name. Ordered by week_number, kickoff_time NULLS LAST. *Consumer: apps/inorout SessionsScreen Tournaments tab (per-competition fixture timetable + director timetable).* |
| `club_admin_assign_fixture_slot(p_fixture_id, p_scheduled_date?, p_kickoff_time?, p_playing_area_id?, p_slot_minutes?)` | `clubAdminAssignFixtureSlot(fixtureId, scheduledDate, kickoffTime, playingAreaId, slotMinutes)` | **authenticated** (anon REVOKED) | **WRITE (mig 319, EV-covered).** Updates a single fixture's scheduling slot. COALESCE pattern — only non-null params overwrite. Auth via fixtures→competitions→tournament_events chain to resolve club_id; validates caller is active club_team_manager. Raises `fixture_not_found`, `not_authorised`. Audit `tournament_fixture_slot_updated`. Returns `{ok:true}`. *Consumer: apps/inorout (pre-wired; manual re-assignment UI planned Phase 5).* |
| **Phase 5–7B — Event OS: Match Day Ops + Standings + Public Page + H2H + Cards (migs 320–323, sessions 122–126). All SECURITY DEFINER, search_path locked. Phase 5: ref_ RPCs GRANT anon+authenticated; club_admin_get_standings GRANT authenticated only. Phase 7B: ref_record_tournament_card GRANT anon+authenticated; get_tournament_suspension_list GRANT authenticated only.** | | | |
| `ref_start_tournament_match(p_ref_token, p_client_event_id, p_local_timestamp?)` | `refStartTournamentMatch(refToken, clientEventId, localTimestamp)` | **anon, authenticated** | **WRITE (mig 320; updated mig 323).** Sets fixture status='in_progress', actual_kickoff_at, current_period='1H'. Validates: tournament fixture (home_competition_team_id NOT NULL), status IN ('scheduled','allocated'). Audit `ref_start_tournament_match`. **Mig 323 adds**: post-UPDATE query returns `suspensions[]` of players where auto_suspended=true on either team for this competition. Return: `{ok, fixture_id, status:'in_progress', suspensions:[{competition_team_id, team_name, player_name}]}`. *Consumer: apps/ref PreMatch.jsx — shows SuspensionWarningModal if suspensions.length > 0.* |
| `ref_set_tournament_period(p_ref_token, p_period)` | `refSetTournamentPeriod(refToken, period)` | **anon, authenticated** | **WRITE (mig 320).** Persists the current period ('1H','HT','2H','FT') to fixtures.current_period. Validates: tournament fixture, in_progress. Audit `ref_set_tournament_period`. Returns `{ok, period}`. *Consumer: apps/ref LiveMatch.jsx period buttons.* |
| `ref_record_tournament_goal(p_ref_token, p_side, p_minute, p_period, p_client_event_id, p_player_id?, p_player_name_override?, p_own_goal?, p_local_timestamp?)` | `refRecordTournamentGoal(refToken, {side, minute, period, clientEventId, playerId?, playerNameOverride?, ownGoal?, localTimestamp?})` | **anon, authenticated** | **WRITE (mig 320, EV-covered).** Increments home_score or away_score on fixtures directly (no match_events insert — tournament fixtures have no team_id FK). Validates: tournament, in_progress, valid side. Audit `ref_record_tournament_goal`. Returns `{ok, home_score, away_score}`. *Consumer: apps/ref LiveMatch.jsx TournamentGoalButton.* |
| `ref_undo_tournament_goal(p_ref_token, p_side)` | `refUndoTournamentGoal(refToken, side)` | **anon, authenticated** | **WRITE (mig 320).** Decrements score via GREATEST(score-1, 0). Validates: tournament, in_progress. Audit `ref_undo_tournament_goal`. Returns `{ok, home_score, away_score}`. *Consumer: apps/ref LiveMatch.jsx undo toast.* |
| `ref_confirm_tournament_match(p_ref_token)` | `refConfirmTournamentMatch(refToken)` | **anon, authenticated** | **WRITE (mig 320).** Sets fixture status='completed', winner derived from scores. Validates: tournament, in_progress. Audit `ref_confirm_tournament_match`. Returns `{ok}`. *Consumer: apps/ref LiveMatch.jsx Full Time flow.* |
| `club_admin_get_standings(p_tournament_event_id, p_competition_id)` | `clubAdminGetStandings(tournamentEventId, competitionId)` | **authenticated** (anon REVOKED) | **READ (mig 320; H2H mig 322; bug fix + H2H preserved mig 323).** Authenticated, club_team_managers auth guard. Computes standings via `base_standings` + `h2h` CTEs. ORDER BY pts DESC, h2h_pts DESC, h2h_gd DESC, h2h_gf DESC, gd DESC, gf DESC, team_name ASC. **Bug fixed mig 323**: previously joined non-existent `public.club_admins` — dead since mig 320, now uses `club_team_managers`. Returns `{ok, competition_id, standings:[{team_id, team_name, played, won, drawn, lost, gf, ga, gd, pts, h2h_pts, h2h_gd, h2h_gf}]}`. *Note: not currently called by any UI — director view computes standings client-side in SessionsScreen.jsx.* |
| `get_tournament_public(p_slug)` | `getTournamentPublic(slug)` | **anon, authenticated** | **READ (mig 317; fixtures+standings mig 321; H2H mig 322).** Public tournament page. Returns `{ok, tournament:{...}, competitions:[{competition_id, name, fixtures:[], standings:[]}]}`. Standings use same H2H CTE pattern as club_admin_get_standings. *Consumer: apps/inorout TournamentScreen (/tournament/[slug]).* |
| `ref_record_tournament_card(p_ref_token, p_competition_team_id, p_player_name, p_card_type, p_minute, p_period)` | `refRecordTournamentCard(refToken, competitionTeamId, playerName, cardType, minuteVal, period)` | **anon, authenticated** | **WRITE (mig 323, EV 8/8).** Records a yellow or red card for a named player. Validates: tournament fixture, in_progress, team_in_fixture, player_name non-blank, card_type IN ('yellow','red'). Auto-suspension: counts prior yellows in same (competition_id, competition_team_id, player_name) — 2nd yellow = auto_suspended=true; any red = auto_suspended=true. Inserts `tournament_cards`. Audit `ref_record_tournament_card` (via `referee` actor_type). Returns `{ok, card_id, is_suspended, player_name, card_type}`. *Consumer: apps/ref LiveMatch.jsx TournamentCardModal + doTournamentCard().* |
| `get_tournament_suspension_list(p_tournament_event_id, p_competition_id)` | `getTournamentSuspensionList(tournamentEventId, competitionId)` | **authenticated** (anon REVOKED) | **READ (mig 323).** Auth: auth.uid()→member_profiles→club_team_managers. Returns `{ok, suspensions:[{competition_team_id, team_name, player_name, yellow_count, red_count}]}` for all auto_suspended=true rows in the competition, grouped by player. *Consumer: planned director UI (not yet wired to a screen — future Phase 7C or standalone suspension dashboard).* |
| **Phase 7C — Event OS: Classification Brackets (mig 324, session 127). Schema: fixtures adds `knockout_home_feeder_id`/`knockout_away_feeder_id` (uuid FK self-ref); `competition_teams` adds `group_rank int`; `fixtures_home_identity` CHECK widened to also allow `knockout_home_feeder_id IS NOT NULL` (allocated bracket slots have NULL teams). Two new RPCs + five REPLACE'd. Security sweep 2/2 PASS. EV 10/10 PASS + leak 0.** | | | |
| `_advance_tournament_winner(p_fixture_id)` | *(internal — no JS wrapper)* | **REVOKED from PUBLIC, anon, authenticated** (postgres + service_role only) | **WRITE (mig 324, internal helper).** Called from `ref_confirm_tournament_match` on every knockout FT confirmation. Derives winner (home_score > away_score; draws = no-op). Slots winner into the next-round fixture via `knockout_home_feeder_id`/`knockout_away_feeder_id` self-referential FKs. Promotes that fixture to `status='scheduled'` once both home + away slots filled. No audit entry (called from within an audited RPC). *Consumer: ref_confirm_tournament_match only.* |
| `club_admin_seed_knockout(p_tournament_event_id, p_competition_id)` | `clubAdminSeedKnockout(tournamentEventId, competitionId)` | **authenticated** (anon REVOKED) | **WRITE (mig 324, EV 10/10).** Auth: auth.uid()→member_profiles→club_team_managers. Guards: all group-stage fixtures must be `completed`; total qualifiers (top 2 per group, rank=1+2) must be a power of 2 (2/4/8/16); `competition.config.knockout_seeded` must be false. Stamps `competition_teams.group_rank` via H2H tiebreaker (same Phase 7A CTE). Collects qualifiers ordered by rank then group_label. Seeds bracket serpentine (seed[i] vs seed[n-i+1]) guaranteeing cross-group R1. Round-1 fixtures: teams populated, `status='scheduled'`. Future-round fixtures: teams NULL, `knockout_home/away_feeder_id` set, `status='allocated'`. Round names from ['Final','Semi-Finals','Quarter-Finals','Round of 16']. Sets `competitions.config.knockout_seeded=true`. Audit `tournament_knockout_seeded`. Returns `{ok, total_qualifiers, knockout_rounds}`. Errors: `not_authenticated`, `not_authorised`, `tournament_not_found`, `competition_not_found`, `knockout_already_seeded`, `incomplete_group_fixtures`, `no_groups_found`, `bracket_size_not_supported`. *Consumer: SessionsScreen "Advance to Knockout" button (director view).* |
| `ref_confirm_tournament_match(p_ref_token)` | `refConfirmTournamentMatch(refToken)` | **anon, authenticated** | **WRITE (mig 320; extended mig 324).** Sets fixture `status='completed'`, `current_period='FT'`. **Mig 324 adds:** after marking completed, calls `PERFORM _advance_tournament_winner(fixture.id)` when `fixture.group_label IS NULL` (knockout rounds only — group-stage fixtures unchanged). Audit `ref_confirm_tournament_match`. Returns `{ok, home_score, away_score, status:'completed'}`. *Consumer: apps/ref LiveMatch.jsx Full Time flow.* |
| `club_admin_get_standings(p_tournament_event_id, p_competition_id)` | `clubAdminGetStandings(tournamentEventId, competitionId)` | **authenticated** (anon REVOKED) | **READ (mig 320; H2H mig 322; bug fix mig 323; extended mig 324).** **Mig 324 adds:** `AND fx.group_label IS NOT NULL` filter on the fixtures JOIN (prevents knockout results contaminating group tables); each standings row gains `group_label` + `group_rank`; top-level return gains `knockout_seeded` boolean. Returns `{ok, competition_id, knockout_seeded, standings:[{team_id, team_name, group_label, group_rank, played, won, drawn, lost, gf, ga, gd, pts, h2h_pts, h2h_gd, h2h_gf}]}`. *Consumer: director standings panel (SessionsScreen, grouped by group_label with ADV chips).* |
| `club_admin_get_schedule(p_tournament_event_id)` | `clubAdminGetSchedule(tournamentEventId)` | **authenticated** (anon REVOKED) | **READ (mig 319; extended mig 324).** **Mig 324 adds:** `group_label` on each fixture object; `knockout_seeded` boolean per competition object. *Consumer: SessionsScreen timetable + director knockout bracket display.* |
| `get_tournament_public(p_slug)` | `getTournamentPublic(slug)` | **anon, authenticated** | **READ (mig 317; fixtures+standings mig 321; H2H mig 322; extended mig 324).** **Mig 324 adds:** (a) `AND fx.group_label IS NOT NULL` on group-stage fixtures query; (b) standings rows gain `group_label`/`group_rank`; (c) competition objects gain `knockout_seeded`; (d) new top-level `knockout_fixtures[]` array (fixtures where `group_label IS NULL`, with TBD team names as null). *Consumer: apps/inorout TournamentScreen — knockout bracket section + grouped standings with ADV chips.* |
| `club_admin_get_tournament(p_slug)` | `clubAdminGetTournament(slug)` | **authenticated** (anon REVOKED) | **READ (mig 316; extended mig 318, mig 324).** **Mig 324 adds:** `group_label` + `group_rank` to each team row; `knockout_seeded` per competition object. *Consumer: SessionsScreen Tournaments tab expand panel.* |
| **Phase 7D — Event OS: Double Elimination (mig 325, session 128). Schema: fixtures adds `de_bracket text CHECK('winners','losers','grand_final')`, `de_loser_to_fixture_id uuid FK→fixtures`, `de_loser_to_slot text CHECK('home','away')`; `fixtures_home_identity` widened: `OR (de_bracket IS NOT NULL)`. Two new RPCs + three REPLACE'd. Security sweep 2/2 PASS. EV 15/15 PASS + leak 0.** | | | |
| `_advance_tournament_double_elim(p_fixture_id)` | *(internal — no JS wrapper)* | **REVOKED from PUBLIC, anon, authenticated** (postgres + service_role only) | **WRITE (mig 325, internal helper).** Called from `ref_confirm_tournament_match` when `fixture.de_bracket IS NOT NULL`. Derives winner/loser (home_score > away_score; draws = no-op). Winner → slotted via feeder mechanism into next WB/LB/GF fixture. Loser → slotted via `de_loser_to_fixture_id`/`de_loser_to_slot` into LB. Fixture promoted to `scheduled` once both slots filled. *Consumer: ref_confirm_tournament_match only.* |
| `club_admin_seed_double_elimination(p_tournament_event_id, p_competition_id)` | `clubAdminSeedDoubleElimination(tournamentEventId, competitionId)` | **authenticated** (anon REVOKED) | **WRITE (mig 325, EV 15/15).** Auth: auth.uid()→member_profiles→club_team_managers. Guards: power-of-2 active teams (4/8/16); `competition.config.knockout_seeded` must be false. Builds full bracket: WB R1 seeded pairs (seed[i] vs seed[n-i+1]) + `de_loser_to_fixture_id` wiring to LB R1 (`de_loser_to_slot` = home for first, away for second). Loop WB R2..k: drop round (home_feeder=LB consolidation winner, away via `de_loser_to_fixture_id` of WB round loser) + consolidation round (LB survivors). Grand Final: home_feeder=WB Final, away_feeder=LB Final. Sets `competitions.config.knockout_seeded=true`. Audit `tournament_de_seeded`. Returns `{ok, total_teams, wb_fixtures, lb_fixtures}`. Errors: `not_enough_teams`, `already_seeded`. *Consumer: SessionsScreen "Seed double elimination" button (director view, format=double_elimination).* |
| `ref_confirm_tournament_match(p_ref_token)` | `refConfirmTournamentMatch(refToken)` | **anon, authenticated** | **WRITE (mig 320; extended mig 324, mig 325).** **Mig 325 adds:** branches on `fixture.de_bracket IS NOT NULL` — DE fixtures call `_advance_tournament_double_elim`; all others call `_advance_tournament_winner`. Group-stage filtering unchanged. *Consumer: apps/ref LiveMatch.jsx Full Time flow.* |
| `club_admin_get_schedule(p_tournament_event_id)` | `clubAdminGetSchedule(tournamentEventId)` | **authenticated** (anon REVOKED) | **READ (mig 319; extended mig 324, mig 325).** **Mig 325 adds:** `de_bracket` field on each fixture object. *Consumer: SessionsScreen timetable + WB/LB/GF bracket display.* |
| `get_tournament_public(p_slug)` | `getTournamentPublic(slug)` | **anon, authenticated** | **READ (mig 317; fixtures+standings mig 321; H2H mig 322; extended mig 324, mig 325).** **Mig 325 adds:** `de_bracket` on each `knockout_fixtures[]` entry. *Consumer: apps/inorout TournamentScreen — WB/LB/Grand Final bracket sections.* |
| **Phase 6 — Event OS: Performance Events (mig 326, session 129). Schema: `performance_results.athlete_id` nullable; adds `athlete_name text NOT NULL DEFAULT ''`, `competition_team_id uuid FK → competition_teams`; UNIQUE `perf_results_upsert_key`; `tournament_events.points_config` default 10-8-6-5-4-3-2-1 + backfill. Six new RPCs + one REPLACE. Security sweep 6/6 PASS. EV 8/8 PASS + leak 0.** | | | |
| `club_admin_set_performance_config(p_tournament_event_id, p_points_config jsonb)` | `clubAdminSetPerformanceConfig(tournamentEventId, pointsConfig)` | **authenticated** (anon REVOKED) | **WRITE (mig 326).** Validates points_config is a non-empty object. Blocks if any results already exist for the tournament (prevents retroactive scoring changes). Updates `tournament_events.points_config`. Audit `performance_config_set`. *Consumer: SessionsScreen director panel.* |
| `club_admin_add_performance_event(p_tournament_event_id, p_name, p_measurement_type, p_display_order?, p_scheduled_time?)` | `clubAdminAddPerformanceEvent(tournamentEventId, name, measurementType, displayOrder?, scheduledTime?)` | **authenticated** (anon REVOKED) | **WRITE (mig 326).** `measurement_type ∈ {time_lower_better, time_higher_better, distance, count, height, points}`. Name trim guard. Inserts into `performance_events`. Audit `performance_event_added`. Returns `{ok, event_id}`. *Consumer: SessionsScreen "Add Performance Event" button.* |
| `club_admin_list_performance_events(p_tournament_event_id)` | `clubAdminListPerformanceEvents(tournamentEventId)` | **authenticated** (anon REVOKED) | **READ (mig 326).** Returns `jsonb_agg` of all performance events ordered by `display_order NULLS LAST, scheduled_time NULLS LAST` with a `result_count` per event. *Consumer: SessionsScreen + loadTournamentDetail.* |
| `club_admin_record_result(p_performance_event_id, p_competition_team_id, p_athlete_name, p_value, p_attempt_number, p_unit?)` | `clubAdminRecordResult(perfEventId, competitionTeamId, athleteName, value, attemptNumber, unit?)` | **authenticated** (anon REVOKED) | **WRITE (mig 326, judge re-entry UPSERT).** Resolves tournament from performance_event → tournament_events, checks club_team_managers auth. ON CONFLICT `perf_results_upsert_key` → UPDATE value + unit. Audit `performance_result_recorded`. Returns `{ok, result_id}`. *Consumer: SessionsScreen per-event result entry form.* |
| `club_admin_get_performance_results(p_performance_event_id)` | `clubAdminGetPerformanceResults(perfEventId)` | **authenticated** (anon REVOKED) | **READ (mig 326).** Returns ranked results for an event; `measurement_type=time_lower_better` sorts ASC, all others DESC. Rows: `{result_id, athlete_name, team_name, value, unit, attempt_number, rank, created_at}`. *Consumer: SessionsScreen event leaderboard.* |
| `club_admin_get_sports_day_standings(p_tournament_event_id)` | `clubAdminGetSportsDayStandings(tournamentEventId)` | **authenticated** (anon REVOKED) | **READ (mig 326).** Aggregates results across all performance events: team totals `{team_name, total_points, gold, silver, bronze, events_entered}`. Points from `tournament_events.points_config[rank]`; ties given equal rank. *Consumer: SessionsScreen Overall Sports Day Standings section.* |
| `get_tournament_public(p_slug)` | `getTournamentPublic(slug)` | **anon, authenticated** | **READ (mig 317; … mig 325; extended mig 326).** **Mig 326 adds:** `performance_events[]` (per-event results with athlete/team/rank/value) + `performance_standings[]` (team totals). *Consumer: apps/inorout TournamentScreen — per-discipline results + standings sections.* |
| **Phase 7 Commercial — Event OS: Sponsors, Branding, POT, Equipment (mig 327, session 130). Schema: `tournament_events` + `player_of_tournament_name/team`; `equipment_bookings` + `tournament_event_id uuid FK`; new table `tournament_sponsors`. Nine new RPCs + two REPLACE'd. Security sweep 9/9 PASS. EV 13/13 PASS + leak 0.** | | | |
| `club_admin_add_sponsor(p_tournament_event_id, p_name, p_logo_url?, p_website_url?, p_display_order?)` | `clubAdminAddSponsor(tournamentEventId, name, logoUrl?, websiteUrl?, displayOrder?)` | **authenticated** (anon REVOKED) | **WRITE (mig 327).** name_required guard (trim). Inserts into `tournament_sponsors`. Audit `tournament_sponsor_added`. Returns `{ok, sponsor_id}`. *Consumer: SessionsScreen Sponsors section.* |
| `club_admin_list_sponsors(p_tournament_event_id)` | `clubAdminListSponsors(tournamentEventId)` | **authenticated** (anon REVOKED) | **READ (mig 327).** Returns `jsonb_agg` ordered by `display_order, name`. Rows: `{sponsor_id, name, logo_url, website_url, display_order, active}`. *Consumer: SessionsScreen + loadTournamentDetail.* |
| `club_admin_remove_sponsor(p_sponsor_id)` | `clubAdminRemoveSponsor(sponsorId)` | **authenticated** (anon REVOKED) | **WRITE (mig 327).** Ownership guard: sponsor → tournament_events.club_id → club_team_managers. Hard-deletes from `tournament_sponsors`. Audit `tournament_sponsor_removed`. *Consumer: SessionsScreen Sponsors section.* |
| `club_admin_set_branding(p_tournament_event_id, p_primary_colour?, p_secondary_colour?, p_custom_logo_url?)` | `clubAdminSetBranding(tournamentEventId, {primaryColour, secondaryColour, customLogoUrl})` | **authenticated** (anon REVOKED) | **WRITE (mig 327).** Writes into `tournament_events.branding` jsonb (column from mig 315). Keys: `primary_colour`, `secondary_colour`, `custom_logo_url` (all nullable text). Audit `tournament_branding_updated`. *Consumer: SessionsScreen Branding section.* |
| `club_admin_set_player_of_tournament(p_tournament_event_id, p_name, p_team_name?)` | `clubAdminSetPlayerOfTournament(tournamentEventId, name, teamName?)` | **authenticated** (anon REVOKED) | **WRITE (mig 327).** name_required guard. Updates `tournament_events.player_of_tournament_name` + `_team`. Audit `tournament_pot_set`. *Consumer: SessionsScreen Player of Tournament section.* |
| `club_admin_get_equipment_for_tournament(p_tournament_event_id)` | `clubAdminGetEquipmentForTournament(tournamentEventId)` | **authenticated** (anon REVOKED) | **READ (mig 327).** Returns active equipment catalogue at the tournament's venue (`equipment.active=true` for that `venue_id`). Rows: `{equipment_id, name, category, quantity, default_fee_pence, deposit_pence, hire_unit, condition}`. *Consumer: SessionsScreen Equipment section catalogue picker.* |
| `club_admin_book_equipment_for_tournament(p_tournament_event_id, p_equipment_id, p_qty, p_start_at, p_end_at, p_due_back_at?)` | `clubAdminBookEquipmentForTournament(tournamentEventId, equipmentId, qty, startAt, endAt, dueBackAt?)` | **authenticated** (anon REVOKED) | **WRITE (mig 327).** invalid_quantity / invalid_window guards. Availability check via `_equipment_peak_committed`. Creates `equipment_bookings` row at `status='confirmed'` linked via `tournament_event_id`. No venue_charges row (directors don't bill through venue — staff handle). Audit `tournament_equipment_booked`. Returns `{ok, booking_id}`. *Consumer: SessionsScreen Equipment section booking form.* |
| `club_admin_list_tournament_equipment_bookings(p_tournament_event_id)` | `clubAdminListTournamentEquipmentBookings(tournamentEventId)` | **authenticated** (anon REVOKED) | **READ (mig 327).** Returns active (not cancelled/declined) bookings for a tournament. Rows: `{booking_id, equipment_id, equipment_name, category, qty, start_at, end_at, due_back_at, returned_at, status, amount_pence}`. **Field is `qty`, not `qty_booked`.** *Consumer: SessionsScreen Equipment section bookings list.* |
| `club_admin_cancel_equipment_booking(p_booking_id)` | `clubAdminCancelEquipmentBooking(bookingId)` | **authenticated** (anon REVOKED) | **WRITE (mig 327).** Ownership guard: booking → tournament_event_id → tournament_events.club_id → club_team_managers. cannot_cancel guard for `status IN ('out','returned','cancelled')`. Sets `status='cancelled'`. Audit `tournament_equipment_booking_cancelled`. *Consumer: SessionsScreen Equipment section cancel button.* |
| `get_tournament_public(p_slug)` | `getTournamentPublic(slug)` | **anon, authenticated** | **READ (mig 317; … mig 326; extended mig 327).** **Mig 327 adds:** `branding {primary_colour, secondary_colour, custom_logo_url}`, `sponsors[]`, `player_of_tournament_name`, `player_of_tournament_team`. *Consumer: apps/inorout TournamentScreen — branding header, sponsor strip, POT trophy card.* |
| `club_admin_get_tournament(p_slug)` | `clubAdminGetTournament(slug)` | **authenticated** (anon REVOKED) | **READ (mig 316; extended mig 318, mig 324; extended mig 327).** **Mig 327 adds:** `sponsors[]`, `player_of_tournament_name`, `player_of_tournament_team`. *Consumer: SessionsScreen Tournaments tab expand panel.* |

---

## ADDING A NEW RPC — CHECKLIST

1. Write SQL in Supabase SQL editor first — never via Claude Code
2. Use `SECURITY DEFINER`
3. `REVOKE ALL` from anon if authenticated-only
4. `GRANT EXECUTE` to correct role
5. Authenticate via `auth.uid()` or token param — never trust passed user_id
6. Return `jsonb`
7. Add wrapper in `packages/core/storage/supabase.js`
8. Export from `packages/core/index.js` barrel
9. Import at call site
10. Verify: grep confirms RPC name appears in exactly ONE `supabase.rpc()` call in supabase.js
