node_modules
dist
.turbo
.env
.env.local
.DS_Store

# Claude Code local overrides — per-user permissions, not for sharing
.claude/settings.local.json
.vercel

# Playwright MCP scratch output + ad-hoc verification screenshots (not for the repo)
.playwright-mcp/
verify-*.png
