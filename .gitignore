node_modules
dist
.turbo
.env
.env.local
.DS_Store

# Claude Code local overrides — per-user permissions, not for sharing
.claude/settings.local.json
.vercel

# Playwright MCP scratch output + ad-hoc verification screenshots (not for the repo)
.playwright-mcp/
verify-*.png

# e2e harness: minted sessions + generated reports (never commit)
e2e/.auth/
e2e/report/
test-results/
